# Active ledger

Most-recent ~20 PRs, one short paragraph each — what shipped, why, the
load-bearing test that pins it, and any rebase / scope notes the next
session needs.

Older entries (v1.77.1, v1.78.0, v1.79.0 and earlier) live condensed in
[ledger-archive.md](ledger-archive.md). When this file grows past
~25 entries, fold the bottom rows into a new condensed section in
ledger-archive.md.

CLAUDE.md carries only a `Current release` pin + a pointer to this file —
the verbatim PR-level paragraphs live here.

- **v2.2.13** — four small fixes in one PR off v2.2.12 (`532cd5f`). **(1) Team picker grid 2-col on mobile too.** `TeamPickerSection.tsx:71` was `grid grid-cols-1 md:grid-cols-2 gap-3`; v2.2.13 drops the `md:` prefix → `grid grid-cols-2 gap-3` so the picker is two columns at every breakpoint. Operator preference — single-column on mobile wasted vertical space. **(2) Futsal position pill colours — verified + tested.** Operator reported futsal positions specifically "looked wrong"; audit traced the helper chain `positionPillColor → getPositionBucket → ALL_DEFS` and confirmed FUTSAL_POSITIONS already maps `GK→GK / FIXO→DF / ALA→MF / PIVOT→FW` (correct buckets, correct colour family). Mapping change unnecessary; new `tests/unit/v2213_position_pill_colors.test.ts` pins every futsal code (GK / FIXO / ALA / PIVOT) end-to-end through `positionPillColor` so a future bucket regression breaks loudly. The visible-wrong perception was the same root cause as #3 (light-mode contrast). **(3) Light-mode pill contrast.** `positionPillColor()` pre-v2.2.13 emitted only `text-{c}-300` shades, which are dark-mode-friendly but fall below WCAG AA against light-mode backgrounds — most acute on the MF green chip. v2.2.13 rewrites the helper to emit `text-{c}-800 dark:text-{c}-300` for all four buckets and bumps MF to `bg-green-600/25` (was `bg-green-500/20`) for a slightly heavier chip body so the dark-text contrast lands consistent across GK/DF/MF/FW. Dark mode unchanged (the `dark:` variant restores the v2.2.12 colour). The `dark:` variant works because the public site's theme model is: default `:root` = dark; `:root:not(.dark)` overrides to light only when `prefers-color-scheme: light`. FormationPitch (other consumer of the helper) renders on the dark pitch background; dark-mode shade unchanged. **(4) Cross-league `/id/<slug>` scoping fix (header title + self-link gate).** Operator reported on `/id/t6l-26sp/*`: page header showed "T9L '26 春" (the default tenant's hardcoded fallback), and the account-menu "No player assigned yet" / "Assign to my player" self-link affordance was visible despite t6l having `allowSelfLink` OFF. **Root cause:** `src/app/id/[slug]/page.tsx:103` was passing `<Header leagueTitle={null}/>`, which made `Header.tsx:41` fall back to the hardcoded constant; AND the JWT `allowSelfLink` claim is computed against `getDefaultLeagueId()` (`src/lib/auth.ts:883-906`) — i.e. the SESSION's default league, not the URL's slug — so on a non-default league the button followed the wrong toggle. Same family as v2.2.5 / v2.2.10 / v2.2.11. **Fix:** page now fetches `league.{name, abbreviation, allowSelfLink}` (one extra cached-Prisma read; `generateMetadata` already does the same lookup) and threads them into `<Header leagueTitle={...} allowSelfLinkOverride={...}/>`. `Header` gains an optional `allowSelfLinkOverride?: boolean` prop and forwards it to `<LineLoginButton allowSelfLinkOverride={...}/>`. `LineLoginButton` gains the same optional prop and resolves the gate via `effectiveAllowSelfLink = override !== undefined ? override : (session.allowSelfLink !== false)`. The override drives BOTH the popup `useEffect` (the first-login modal) and the in-dropdown `allowSelfLink` branch (so the "Assign to my player" CTA, the "Need an invite" fallback copy, and the "Change/Unassign player" link all gate against the URL's league). Pages that mount `Header` without a per-URL league (admin shell, `/assign-player`, `/stats`, `/schedule`) leave the prop undefined and keep legacy session-driven behaviour. **Out of scope / deferred.** This patch addresses the two surfaces operator reported on `/id/<slug>`. JWT-level fix (recompute `session.allowSelfLink` against the URL's league for every request) is a wider change — JWT callbacks don't see the URL pathname, so resolving "current league" would need a request-context shim. Flagged as a follow-up rather than rolled in here. Other pages that read `session.allowSelfLink` directly (the modal copy paths inside `LineLoginButton` are now covered via the override; other consumers `src/app/api/assign-player/route.ts:305` and `src/app/assign-player/page.tsx:38` are server-side and ALREADY re-derive from `leagueId` via `getLeagueAllowSelfLink`, so no leak there). **Tests.** New `tests/unit/v2213_fixes.test.ts` (source-pin style): TeamPickerSection grid pinned to `grid grid-cols-2 gap-3` (no `md:grid-cols-2`), `positionPillColor` returns the light/dark dual-shade classes for every bucket (verified via `getPositionBucket` round-trip on GK / FIXO / ALA / PIVOT + the 12 soccer codes), `/id/[slug]/page.tsx` selects `name`/`abbreviation`/`allowSelfLink` and passes the three props to `<Header>`, `Header` accepts `allowSelfLinkOverride` and forwards it to `<LineLoginButton>`, `LineLoginButton` resolves `effectiveAllowSelfLink` with override precedence over `session.allowSelfLink`. `tests/unit/version.test.ts` repinned `2.2.12 → 2.2.13`. No schema change, no migration, no destructive surface — self-mergeable per the auto-merge policy (presentation + bug-fix scoped, no architectural ambiguity).
- **v2.2.12** — onboarding polish pass touching registration / team-picker UI + a new ID-reuse-with-consent path. Six items: **(1)** every onboarding section header promoted from `<h3>` to canonical `<h2>` (`font-display text-2xl font-black uppercase tracking-tight text-fg-high leading-tight`) in `RegistrationFields.tsx` (About you / Positions) and `TeamPickerSection.tsx` (Choose your team) — fixes flat outline and aligns with the site-wide display-h2 vocabulary. **(2)** team-picker cards render in a 2-column grid on `md:` and up (`grid-cols-1 md:grid-cols-2`) so a multi-team league fits without scrolling on tablet/desktop; mobile stays single-column. **(3)** "Share Your ID" heading lifted OUT of the warning-tinted callout box and rendered as a sibling `<h2>` ABOVE it, so the ID section reads with the same header-then-body cadence as the other sections. **(4)** dropped the word "strictly" from the first callout paragraph per the operator's softer-tone preference ("We require your ID to enable more regular league games!"). **(5)** new `IdExamplesModal` (text-only, no images) wired to a "Which IDs are accepted?" trigger below the callout — enumerates Driving license / Zairyu card / Residence card-juminhyo, plus a tinted note that only name + current address + expiry need to be legible (everything else may be covered). Modal mirrors `SuccessConfirmationModal`'s portal + ESC + backdrop + focus-trap + body-lock pattern; no new shared dialog primitive added since none exists in this repo (`ConfirmDialog` is admin-styled, `SuccessConfirmationModal` is task-specific, `ApplyToLeagueModal` is task-specific). The team-picker "Current players:" mini-label sits above the member list per the brief; the balanced-team card stays label-less. Position pill colours in the team-picker continue to reuse `positionPillColor()` from `src/lib/positions.ts` — it was already imported in v2.2.9 and already maps GK→yellow / DF→blue / MF→green / FW→red (which is the same palette used by `PlayerAvailability` via `getPositionBucketByScore` + the same yellow/blue/green/red tokens). **(6)** existing-ID-reuse with explicit consent: when the calling user already has IDs on file (`User.idFrontUrl` + `User.idBackUrl` + `User.idUploadedAt` all populated), the onboarding form now defaults to a single consent checkbox path ("I consent to share my existing ID with the organizers of this league.") instead of re-prompting two file uploads. Trigger to switch back: an "Upload new ID instead" link below the checkbox (mirror on the upload branch: "Use existing ID instead"). Server-side: `completeOnboardingWithId` accepts a new `reuseExistingId?: boolean`; selects `idFrontUrl` / `idBackUrl` / `idUploadedAt` on the User lookup and re-derives `hasExistingIds`; when `reuseExistingId === true` AND the user actually has IDs, the action SKIPS the Blob URL gate, SKIPS the User.idFront/Back/uploadedAt write, and ADDS `idShared: true` to the existing PLM `updateMany` (defaults to `true` schema-side but the explicit write documents intent). Forged `reuseExistingId: true` on a brand-new user (no IDs on file) is rejected server-side with the same friendly error the form would surface ("Please confirm consent to share your ID with this league's organizers."). The v2.2.8 admin-image-proxy already gates on per-league `idShared` so no proxy change is needed. **Scope.** Onboarding-only (per the operator brief — "On onboarding submit"). Recruit flow (`/recruit/[slug]` / `registerToLeague`) is unchanged — `RegistrationForm.tsx` does not pass `hasExistingIds`, defaults to false, behaviour identical to v2.2.11. Test delta: new `tests/unit/v2212_existing_id_reuse.test.ts` (21 cases) source-pins the prop wiring, consent-checkbox path, upload-new affordance, server-side reuse gate, idShared write, and the consent-error message; `tests/unit/version.test.ts` repinned 2.2.11 → 2.2.12; `tests/unit/v168_id_collection_streamlined.test.ts` updated to reflect the new `idGateOk`-driven submit gate; `tests/unit/v194_onboarding_skip_id_and_preferred_cap.test.ts` updated to reflect the v2.2.12 `idRequired && !reusing && !idFrontFile/!idBackFile` shape. No schema change, no migration; the `PlayerLeagueMembership.idShared` column already exists (added in the v2.2.8 audit work) and the dependent v2.2.8 proxy already enforces it. Self-mergeable per the auto-merge policy: presentation + a scoped onboarding gate with a server-side re-verify, no destructive surface, no schema work.
- **v2.2.11** — second follow-up to v2.2.9 / v2.2.10: close the remaining team-picker-bypass surfaces so "all user types see the picker when registering." v2.2.10 patched only the `/join/[code]` resolver branch, leaving three surfaces where a user could still become an active player without seeing the picker even with `allowPlayerTeamPick` ON: **(A) `/join/[code]/id-upload` direct URL** — bookmarks / shared links land here, skipping `/onboarding` where the picker mounts. Fix: mirror the v2.2.10 resolver guard inside `src/app/join/[code]/id-upload/page.tsx`. The league select gains `allowPlayerTeamPick: true`; after the COMPLETED → /welcome short-circuit, when the toggle is on the page redirects to `/onboarding` regardless of `Player.name`. COMPLETED still goes to /welcome (precedence preserved); toggle-OFF behaviour is unchanged. **(B) `LeagueInvite.skipOnboarding=true` invites** — `redeemInvite` short-circuits to `onboardingStatus='COMPLETED'` + redirects to `/welcome` before `/onboarding` is reachable. Operator requirement is unambiguous ("all user types"), so the league toggle now WINS over `skipOnboarding`. Fix in `src/app/join/[code]/actions.ts`: after invite resolution, fetch `league.allowPlayerTeamPick`; derive `effectiveSkipOnboarding = invite.skipOnboarding && !teamPickWins`; use that for both the `newOnboardingStatus` write and the `redirectTo` branch. Semantic shift: `skipOnboarding` now means "skip name/ID" rather than "skip onboarding entirely" when the league has the picker on. Documented in PR description. **(C) `/recruit/[slug]` self-serve apply** — the public application form (`registerToLeague`) creates a PENDING PLM with `leagueTeamId: null`; no picker on the form, so admin always assigns the team on approval. Fix: thread `allowPlayerTeamPick` + `getTeamPickerOptions(leagueId, null, ballType)` through `src/app/recruit/[slug]/page.tsx` → `RegistrationForm.tsx`. Form mounts `TeamPickerSection` ABOVE `RegistrationFields` when the toggle is on (mirrors `OnboardingForm`'s shape — `NO_SELECTION` sentinel, balanced=null, form blocks submit until a card is picked). `registerToLeague` gains `chosenTeamId?: string | null`, selects `allowPlayerTeamPick` on the league lookup, validates a non-null string via `prisma.leagueTeam.findUnique` + cross-league guard (`chosen.leagueId === league.id` — rejects spoofing, mirrors v2.2.5 area), writes the chosen `leagueTeamId` on PLM create (null when balanced or toggle off). **(D) Out of scope / deferred.** `ApplyToLeagueModal` (the `/id/<slug>` RecruitingBanner client modal that calls `applyToLeague`) does not yet expose a picker — it's a client modal that would need server-fetched team options threaded through, larger UX scope; flagged as a follow-up. Admin-driven `adminApproveApplication` + `adminCreatePlayer` left alone — admin agency by design. **(E) Tests.** New `tests/unit/v2211_team_picker_all_paths.test.ts` (structural source-pin style, mirrors `v2210_team_picker_resolver.test.ts`): id-upload page selects `allowPlayerTeamPick` and redirects to `/onboarding` when the toggle is on, COMPLETED guard precedes the new branch, toggle-OFF branches preserved; `redeemInvite` fetches `league.allowPlayerTeamPick`, derives `effectiveSkipOnboarding`, both newOnboardingStatus and redirectTo use the derived value; recruit page selects `allowPlayerTeamPick`, fetches `getTeamPickerOptions`, threads both into the form; `RegistrationForm` imports `TeamPickerSection`, has the `NO_SELECTION` sentinel + picker-error gate, calls `registerToLeague` with `chosenTeamId`; `registerToLeague` accepts `chosenTeamId`, selects `allowPlayerTeamPick`, validates with cross-league guard, writes the chosen leagueTeamId on PLM create. `version.test.ts` repinned 2.2.10 → 2.2.11. Existing `v2210_team_picker_resolver.test.ts` untouched. **(F) Auto-merge.** Self-mergeable per the v1.95.1 policy: no schema change, no migration, no destructive surface, bug-fix-scoped to closing the picker-bypass surfaces the v2.2.10 audit identified.
- **v2.2.10** — bug-fix follow-up to v2.2.9 + h3 section headers on onboarding. **(A) Bug fix — picker never rendered.** Operator turned ON `allowPlayerTeamPick` for league `t6l-26sp`, started onboarding, and the team-picker section did not appear. **Root cause: the existing `/join/[code]/page.tsx` resolver routes invitees whose `Player.name` is already set (the common case — admin pre-fills the name at slot creation) directly to `/join/[code]/id-upload`, skipping `/onboarding`.** The team-picker introduced in v2.2.9 mounts only on `/onboarding`, so any pre-named invitee sailed past it. Fix: select `allowPlayerTeamPick` on the league lookup at `page.tsx:87-91`, and add a new resolver branch — when the toggle is ON and `onboardingStatus !== 'COMPLETED'`, route to `/onboarding` regardless of `Player.name`. COMPLETED → /welcome is unchanged, and the toggle-OFF branches retain the original name-set → /id-upload / no-name → /onboarding split. Minimum-surface fix: no changes to `/id-upload`, no schema change, no migration. **(B) h3 section headers on onboarding.** Operator follow-up: each onboarding form section should carry its own small h3 heading. Canonical join-flow style is `text-fg-mid text-xs uppercase tracking-wider font-bold mb-1.5` (matches `RedeemCodePicker.tsx:76`). Three new h3s land: `<h3>About you</h3>` above name + email, `<h3>Positions</h3>` above preferred + secondary picker, and `<h3>Share Your ID</h3>` replacing the prior `<p className="font-bold...">` heading inside `registration-id-callout`. `TeamPickerSection.tsx` promoted from `<fieldset>`/`<legend>` to `<section>`/`<h3>` so it matches — `aria-labelledby` still points at the heading id, accessible-name semantics preserved; buttons keep `aria-pressed` so selection semantics are unchanged. **(C) Tests.** New file `tests/unit/v2210_team_picker_resolver.test.ts` (8 cases): page-source pin that `allowPlayerTeamPick: true` lands in the league select; pin that the new resolver branch redirects to `/onboarding` when the toggle is on; pin that the COMPLETED → /welcome branch precedes it; pin that the toggle-OFF branches retain the original split; structural pin on `RegistrationFields` `<h3>About you</h3>` and `<h3>Positions</h3>` headers + `registration-section-about` / `-positions` testids; structural pin on `<h3>Share Your ID</h3>` replacing the prior `<p>`; structural pin on `TeamPickerSection` `<section>`/`<h3>` swap. Existing `tests/unit/leagueAllowPlayerTeamPick.test.ts` (39 cases) and `tests/unit/positionsSort.test.ts` (15 cases) are untouched. `tests/unit/v1761_id_callout.test.ts` still matches the "Share Your ID" substring (no `<p>` assertion). `tests/unit/version.test.ts` repinned 2.2.9 → 2.2.10. **(D) Out of scope.** `/recruit/[slug]` still does not surface the picker — the recruit flow is for not-yet-bound players and the picker only makes sense once a PLM exists. Cross-league scoping audit (H1 hypothesis) was checked and ruled out: admin write and onboarding read both key by `League.id`. The empty-eligible-members case (H3) was checked: `getTeamPickerOptions` returns `[]` when no qualifying members exist, and `TeamPickerSection` still renders the "balanced team" opt-out card (no empty-state regression). Per the v1.95.1 auto-merge policy this PR is self-mergeable: bug-fix scoped, no schema/migration, no destructive surface.
- **v2.2.9** — per-league "Allow players to pick their own teams" toggle + onboarding team-picker step + reworded ID-section callout. **(A) Schema.** New `League.allowPlayerTeamPick Boolean @default(false)` (`prisma/schema.prisma`), additive-only migration `20260604000000_league_allow_player_team_pick` adding the column NOT NULL DEFAULT false. Default false preserves the existing onboarding flow for every league. PLM.leagueTeamId was already nullable (`schema.prisma:565`, set in v1.65.0 for PENDING-application memberships) — no nullability migration needed; "balanced team" writes NULL onto the existing PLM. **(B) Admin toggle.** New section in `src/components/admin/SettingsTab.tsx` ("Allow players to pick their own teams") wired with the same optimistic-flip-with-rollback pattern as the v1.60.0 `allowSelfLink` toggle (testids `settings-tab-team-pick-section` / `-on` / `-off`). New server action `setLeagueAllowPlayerTeamPick` in `src/app/admin/leagues/actions.ts` — `assertAdmin` → boolean validation → `prisma.league.update` → canonical `revalidate({ domain: 'admin' })`. **(C) Onboarding flow.** When the league has `allowPlayerTeamPick === true`, `src/app/join/[code]/onboarding/page.tsx` calls a new `getTeamPickerOptions(leagueId, currentPlayerId, ballType)` (`src/lib/onboarding-team-options.ts`) which fetches every LeagueTeam in the league with its roster filtered to active + applicationStatus ∈ {APPROVED,PENDING} + `toGameWeek: null`, excludes the current player from their own card, and sorts members per a new `sortMembersByPrimaryPositionThenName(...)` helper in `src/lib/positions.ts`. The page threads the options through to `OnboardingForm`. `OnboardingForm.tsx` mounts a new client component `src/components/onboarding/TeamPickerSection.tsx` ABOVE `RegistrationFields`, rendering one card per team (team name + color/logo + capped 12-row member list with `+N more` tail) plus a trailing dashed-border "Put me in a balanced team" card that maps to NULL. The form blocks submit until either a team or the balanced card is chosen. **(D) Server-action delta.** `CompleteOnboardingWithIdInput` gains `chosenTeamId?: string | null`. `completeOnboardingWithId` selects `allowPlayerTeamPick` on the invite→league join and only honours `chosenTeamId` when the flag is true (defensive — a stale client cannot nominate a team after admin disabled the toggle). String IDs are validated via `prisma.leagueTeam.findUnique` + `chosen.leagueId === invite.leagueId` to reject cross-league spoofing (mirrors the v2.2.5-area scoping guard); reject string with `Error('Invalid team selection')`; NULL writes `leagueTeamId: null` onto the existing PLM. The write spreads into the existing PLM `updateMany` so onboardingStatus + comments + leagueTeamId all land in one statement; the where-clause is unchanged (`leagueTeam: { leagueId }` — onboarding's pre-condition guarantees a non-null leagueTeamId per redeemInvite's existingAssignment lookup at `actions.ts:204-211`). **(E) ID-section copy.** `src/components/registration/RegistrationFields.tsx` ID callout (`registration-id-callout`) rewritten verbatim per brief: subheader "Share Your ID" + three paragraphs (motivation / scope / access control). Shared by both `/recruit/[slug]` and `/join/[code]/onboarding` flows since the callout lives in the shared component. **(F) Pure helpers.** `src/lib/positions.ts` gains `getPositionSortWeight(code, ballType)` (vocab `sortWeight` with `MAX_SAFE_INTEGER` fallback for empty/unknown) and `sortMembersByPrimaryPositionThenName(members, ballType)` (stable, non-mutating, name tiebreak case-insensitive locale-aware). Soccer GK=1 → RW=12; futsal GK=1 → PIVOT=4. **(G) Tests.** New `tests/unit/leagueAllowPlayerTeamPick.test.ts` (~30 cases): schema pin (default false invariant), additive-migration pins (no DROP/ALTER COLUMN/TRUNCATE/DELETE, rollback recipe in comment), admin action pins (`assertAdmin`, boolean validation, prisma.league.update shape, canonical revalidate), SettingsTab wiring pins (import, prop, testids, optimistic-flip), server-action pins (`chosenTeamId` type, `allowPlayerTeamPick` in select, gate, cross-league guard message, write site), onboarding page pins (threading), OnboardingForm pins (TeamPickerSection import, chosenTeamId pass-through, picker-error gate), TeamPickerSection UI pins (per-team + balanced testids, `MAX_VISIBLE_MEMBERS=12`, overflow), `onboarding-team-options` pins (status filter, applicationStatus filter, `toGameWeek: null`, current-player exclusion, canonical sort helper), and `RegistrationFields` copy pins (new subheader, three paragraphs verbatim, old "Why we need your ID" subheader regression target). New `tests/unit/positionsSort.test.ts` (~18 cases): soccer/futsal weights, unknown → fallback, sort by position then name, case-insensitive tiebreak, non-mutating contract. `tests/unit/version.test.ts` repinned `2.2.8 → 2.2.9`. **(H) Out of scope.** Recruit flow (`/recruit/[slug]`) does NOT get the team picker — admin handles team placement on approval. Cross-league scoping audit on other player-facing actions is its own task. The shared callout copy update lands for recruit flow as a side effect (single component), which is intentional — recruit applicants will see the same wording about ID storage.
- **v2.2.8** — security hardening for player ID images. Closes three findings from the 2026-05 ID-storage audit (sister session `local_a714638f`): **C1** — ID Blobs were `access: 'public'` with `addRandomSuffix: false`, so the path was a guessable function of `playerId + Date.now()` and the URL was the only secret. **C2** — `PlayerLeagueMembership.idShared` consent flag existed in the schema but was never checked in `src/lib/admin-data.ts` or anywhere else; the consent model was decorative. **H1** — admin UI (`IdViewerDialog.tsx`, `UsersList.tsx`) rendered `<img src={publicBlobUrl}>` directly, leaking the bearer URL into browser history, referrer headers, devtools, and any analytics/CDN logs. **Fix.** New authenticated proxy route `src/app/api/admin/id-image/[userId]/[side]/route.ts` — `assertAdmin`, looks up `User.idFrontUrl`/`User.idBackUrl`, enforces consent via `prisma.playerLeagueMembership.findFirst({ where: { playerId, idShared: true } })` (admins are global in this project, so any granted PLM on the player suffices), then `fetch()`-es Blob server-side and streams bytes back with `Content-Type` echoed, `Content-Disposition: inline`, and `Cache-Control: private, no-store`. 401 on anonymous, 403 on non-admin, 403 + `consent_not_granted` on no-consent or no-PlayerLink, 404 on missing user or null `idFrontUrl`/`idBackUrl`, 502 on Blob fetch failure. New uploads switched to `addRandomSuffix: true` in `src/app/join/[code]/actions.ts:499-509` (and the v1.171 client-direct upload-token route at `src/app/api/blob/upload-token/route.ts:126-136` — split so only `isPlayerId` paths get random suffix; team-logo and player-profile paths keep stable paths since they're intentionally public). Kept `access: 'public'` because Vercel Blob `private` mode is not yet a stable API; random-suffix + auth proxy is the documented combo per the audit. **No backfill of existing Blobs** — operator confirmed (`local_a714638f`) old URLs were never accessed externally, PII volume is low, and the public URLs in Blob are a pre-existing exposure that re-uploading wouldn't help. Old paths get served via the proxy (which fetches server-side); future writes get random-suffixed automatically. **Admin UI swap.** `IdViewerDialog.tsx` — props rename `idFrontUrl/idBackUrl` → `hasFront/hasBack` (presence booleans) + new `userId` prop; `<img src>` builds `/api/admin/id-image/{userId}/{side}`. `UsersList.tsx` — thumbnail + modal use `/api/admin/id-image/{user.id}/{side}` (User row already has `id`). `PlayersTab.tsx` — caller passes the new props through. **Plumbing.** `src/lib/admin-data.ts` `idDataByPlayerId` entries gain a `userId: u.id` field; the User select gets `id: true`. `src/app/admin/leagues/[id]/players/page.tsx` builder threads `idUserId` onto each player row from `idDataByPlayerId[…].userId`. PlayersTab's `PlayerRow` interface gains `idUserId: string | null`. **Tests.** New `tests/unit/api/admin-id-image.test.ts` (9 cases): 400 invalid side, 401 anonymous, 403 non-admin, 404 missing user, 404 null side URL, 403 `consent_not_granted` when no PLM has `idShared=true`, 403 `consent_not_granted` when no bound player, 200 + bytes + `Cache-Control: private, no-store` + `Content-Disposition: inline` when consented (pins the consent query shape too), 502 on upstream Blob 500. Existing `tests/unit/idUploadActions.test.ts` updated — the "addRandomSuffix=false so the URL is stable" pin renamed and flipped to assert `addRandomSuffix: true` with a v2.2.8 rationale comment. `tests/unit/v1711_blob_client_upload.test.ts` — the source-level regex `addRandomSuffix:\s*false` still matches the team-logo branch in the upload-token route, so left as-is. Full unit suite: 3901 passed / 2 skipped (delta +9 from the new file). tsc clean (only the pre-existing unrelated `@next/bundle-analyzer` noise). Per the auto-merge policy this PR is self-mergeable: security hardening with no destructive surface, no schema/migration, no backfill, gates already-disabled state behind admin role + consent.
- **v2.2.7** — follow-up to v2.2.6: tighten the "GOING + past matchday → PLAYED" cutoff from JST-midnight to precise kickoff. v2.2.6 compared `Matchday.date` (JST date string) against `formatJstDate(now)` so a 19:00-kickoff matchday flipped to PLAYED only after JST midnight the next day. Operator wanted the boundary to be the moment the match locks: at 13:00 JST on kickoff day a GOING RSVP still shows GOING; at 19:01 it locks to PLAYED. **Fix.** New pure helper `computePastMatchdayIds(gws, matchdays, now)` in `src/lib/rsvpMerge.ts` — compares `gw.startDate < now` (strict less-than, so exactly-at-kickoff is not-yet-past), maps the GameWeek's `weekNumber` to its public `md<n>` id, returns the set. Null `startDate` (TBD) is never past. `src/lib/publicData.ts` calls the helper with `new Date()` and drops the old JST-string comparison + the now-unused `formatJstDate` import. **Schema note.** No `Matchday` table; matchdays are projected from `GameWeek` (`prisma/schema.prisma:804-822`), which has `startDate: DateTime?` carrying minute-precise kickoff. The existing `GameWeekMeta` already plumbs `startDate` through `getRsvpData` (used by the Redis read path), so the helper consumes data already on hand — no schema change, no new query. **Tests.** `tests/unit/rsvpMerge.test.ts` gains a `computePastMatchdayIds` describe block covering 13:00 → not past, 19:01 → past, exactly-at-kickoff → not past, null startDate → never past, orphan GameWeek → skipped. Pre-v2.2.6 string-comparison rule and the `mergeRsvpData` pure-merge tests are untouched (they take `pastMatchdayIds` as input). Version pin → 2.2.7. 24/24 in this file, full unit suite 3897/3897. Auto-merge: self-mergeable — no schema change, no migration, scope-bound to the cutoff derivation.
- **v2.2.3** — follow-up hotfix to v2.2.2: prod user reproduced the same `Error: Beneficiary team is not part of this match` AFTER v2.2.2 deployed (UI-confirmed at v2.2.2). Failing payload was `{ matchPublicId: "md3-m1", beneficiaryTeamId: "fc-torpedo", ... }` (Fenix vs Torpedo, md3 in league `t9l`, goalType OPEN_PLAY). **Root cause: asymmetric round-trip in `src/lib/ids.ts`.** `slugToTeamId(slug)` always prepends `t-` if missing; `teamIdToSlug(id)` only strips `t-` if present. So a Team.id stored *without* the prefix (legacy row, missed by the original PR-6 backfill) surfaces as the bare slug in the dropdown via the public adapter (`teamIdToSlug` returns it unchanged), but the v2.2.2 server resolver round-trips it back to a `t-`-prefixed form and the equality check against `match.homeTeam.teamId` / `awayTeam.teamId` fails. v2.2.2's `submitOwnMatchEvent.test.ts` fixtures use `HOME_TEAM = 't-home'`, so the asymmetry never showed up under test. **Fix.** `src/app/matchday/[id]/actions.ts` resolver now accepts EITHER the prefixed (`beneficiaryDbTeamId`) OR the bare (`beneficiarySlug`) form on each side via a `sideMatches` helper. Minimal, no schema change, no migration. The underlying data inconsistency (some Team.id rows lack the `t-` prefix) is a separate clean-up, not load-bearing for this hotfix. **Caveat.** Does NOT cover hypothesis H3 (the Match references a *different* LeagueTeam row than the current roster — e.g. post-rebound / rename). If that turns out to be the root cause for any future report, the fix is data (repoint `Match.{home,away}TeamId`), not code. **Tests.** New regression target in `tests/unit/submitOwnMatchEvent.test.ts`: "v2.2.3 regression target: accepts slug when match Team.id lacks the `t-` prefix" — `gameWeek` mock's `homeTeam: { teamId: HOME_SLUG }` (bare "home" instead of "t-home"), form posts `HOME_SLUG`, action writes resolved `HOME_LT`. 25/25 in this file, version pin updated. Per the auto-merge policy this PR is self-mergeable: regression-bound, scoped, no schema/migration, no destructive surface.
- **v2.2.2** — hotfix: player-facing Submit-Goal flow rejected every submission with `Error: Beneficiary team is not part of this match` (digest=1030660889 in prod logs, reproed by user submitting an open-play goal on Fenix vs Torpedo with Torpedo as beneficiary). **Root cause: identifier-space mismatch.** The throw at `src/app/matchday/[id]/actions.ts:170` compared the incoming `beneficiaryTeamId` against `match.homeTeamId`/`match.awayTeamId` from Prisma — both of which are `LeagueTeam.id` values (`Match.homeTeam`/`awayTeam` FK `LeagueTeam` per `prisma/schema.prisma:853-854`). But the SubmitGoalForm sources its team ids from the public adapter `src/lib/dbToPublicLeagueData.ts:138-140`, where `ltToSlug` maps `LeagueTeam.id → teamIdToSlug(team.id)` — i.e. a **public team slug derived from Team.id**, NOT the LeagueTeam.id. `Dashboard.tsx:358-367` / `LeagueMatchdayClient.tsx:187` forward those slugs straight into the form (`SubmitGoalForm.tsx:128-129, 242`) and the form posts them to `submitOwnMatchEvent` (line 320). The two id spaces never overlap, so the validator threw on every legitimate submission. Bug has been latent since v1.82.0 (when beneficiary became an explicit form input) — admin StatsTab passes raw LeagueTeam.ids and works fine, so it survived the v1.82.0 + v1.88.0 + v2.2.1 review/test rounds. **Fix (server-side resolution).** `src/app/matchday/[id]/actions.ts` now extends the `gameWeek.matches` select to also pull `homeTeam: { select: { teamId: true } }` / `awayTeam: { select: { teamId: true } }`. The beneficiary check resolves the incoming slug via `slugToTeamId(input.beneficiaryTeamId)` and matches it against `match.homeTeam.teamId` / `match.awayTeam.teamId`, then writes the corresponding `match.homeTeamId` / `match.awayTeamId` (LeagueTeam.id) to `MatchEvent.beneficiaryTeamId`. Two Prisma id-space helpers imported (`slugToTeamId` from `@/lib/ids`). The admin path (`src/app/admin/leagues/actions.ts:2154-2157`) is untouched — admin still passes raw LeagueTeam.id, which keeps working there. **Tests.** Existing `tests/unit/submitOwnMatchEvent.test.ts` updated: input fixtures now pass team SLUGS (`HOME_SLUG = 'home'` / `AWAY_SLUG = 'away'`) as the contract demands; gameWeek mock gains nested `homeTeam: { teamId: HOME_TEAM }` / `awayTeam: { teamId: AWAY_TEAM }` to mirror the new select shape. The "rejects when beneficiaryTeamId is not part of the match" case keeps the slug-form `'third'` to cover the negative branch. Existing assertions that `data.beneficiaryTeamId === HOME_LT` / `AWAY_LT` keep working because the action writes the resolved LeagueTeam.id (not the slug). 24/24 in this file, 3885/3885 in the full suite, tsc clean (only pre-existing unrelated `@next/bundle-analyzer` noise). **Out of scope.** Plumbing LeagueTeam.id through the public `Match` type was the alternative fix — rejected as more invasive (changes the public contract). The server-side resolution is the minimal patch and keeps the public adapter contract intact. Per the v1.95.1 auto-merge policy this PR is self-mergeable: regression-bound, scoped, no schema/migration, no destructive surface.
- **v2.2.1** — hotfix: player-facing `submitOwnMatchEvent` rejected grandfathered LINE-auth sessions. User reported the SubmitGoalForm produced the generic prod RSC error ("An error occurred in the Server Components render…"); the Vercel runtime log captured the underlying throw with digest=1570823256 → `Error: Not signed in` at the gate `if (!session || !userId) throw new Error('Not signed in')` in `src/app/matchday/[id]/actions.ts:81`. Root cause is the same admin-orthogonal-UX anti-pattern v1.80.10 / v1.80.11 fixed elsewhere: `session.userId` is null for LINE-auth JWTs that predate the v1.28.0 NextAuth migration (lineId set, userId never populated). Fix mirrors `requireSelfPlayerSession` in `src/app/account/player/actions.ts`: accept session keyed by `userId` OR `lineId`, resolve canonical `User` row via `User.lineId @unique` fallback, derive `callerPlayerSlug` from `session.playerId` → `User.playerId` → `Player.lineId` lookup chain. `createdById` now sources from the resolved User row's id. Throws "Sign in to submit a goal" (neutral copy) only when both ids are absent. The form's prod error surfaces as the generic RSC wrap because Next.js production builds redact server-action error messages and replace them with a digest; the in-page React error boundary then shows the wrapped message. Tests: 2 new regression-target tests in `tests/unit/submitOwnMatchEvent.test.ts` — (1) LINE-only session (lineId='U-line-stefan', userId=null, playerId=null) submits successfully with `createdById` sourced from the lineId-resolved User row; (2) session.playerId missing falls back to `User.playerId`. Pre-existing "rejects when not signed in" test updated to the new neutral copy + a sibling "rejects when session has neither userId nor lineId" pin added. Existing tests gained a default `userFindUniqueMock.mockResolvedValue({id, playerId})` since the action now consults the User row on every call. Stash-pop verified 4 of the new/updated cases fail on the v2.2.0 source. tsc clean. Full suite: 3885 passed / 2 skipped. No schema change, no migration, single-file source diff in `src/app/matchday/[id]/actions.ts`. Audit follow-up (not in this PR): the same anti-pattern audit hasn't been run on other player-facing server actions (`/api/rsvp`, `/api/guests/actions.ts`, etc.) — they may still gate on `session.userId` alone and reject LINE-only sessions. Per the v1.95.1 auto-merge policy this PR is self-mergeable.
- **v2.2.0** — immediate transfer + approve: admin player-roster actions now resolve the effective game week server-side instead of asking the admin to pick a GW number from a dropdown. Driven by user friction — wanting to transfer a player "today, before the next match" required mentally translating "next match" into a `weekNumber`. **(A) New private helper `computeNextGameWeek(leagueId)` in `src/app/admin/leagues/actions.ts`.** Returns the smallest `weekNumber` GameWeek with no played matches (every `Match.homeGoals === null`, or the GW has no matches yet — a future placeholder). If every GW has at least one played match, returns `maxGameWeek + 1` so the action becomes forward-only and never reattributes historical matches. Empty league → 1. Mirrors the player-facing `findNextMatchday()` semantic in `src/lib/stats.ts` so admin actions and public displays agree on what "next match" means. **(B) `transferPlayer` signature simplified** to `(playerId, toLeagueTeamId, leagueId)` — `fromLeagueTeamId` + `fromGameWeek` removed. Body resolves `nextGW` via the helper, looks up the player's current active PLM by `toGameWeek: null` + `leagueTeam: { leagueId }`, throws on no-active-team or already-on-destination, then either (overwrite branch) deletes the current PLM when `currentActive.fromGameWeek === nextGW` — collapsing two same-GW transfers into one clean `X → B` instead of `X → A (zero-length) → B` — or (normal branch) updates the current PLM with `toGameWeek: nextGW - 1`. Final create has `joinSource: 'ADMIN'`. **(C) `adminApproveApplication` no longer accepts `fromGameWeek`.** It calls `computeNextGameWeek(input.leagueId)` and feeds the result into the existing PLM update. The old `input.fromGameWeek > 0 ? input.fromGameWeek : 1` fallback is gone. **(D) `assignPlayer` signature simplified** to `(playerId, leagueTeamId)`. Looks up the leagueTeam to get `leagueId`, then `computeNextGameWeek`. Currently unused by the UI (the live initial-assign flow is `adminApproveApplication`); kept for parity + future direct-assignment callers, with a comment noting the discovery. **(E) `TransferPanel` UI in `src/components/admin/PlayersTab.tsx`.** Removed the "Effective from GW" `<select>`, the `fromGW` state, the `futureGWs` array, and the `maxGameWeek` prop. Button copy changed from "Confirm Transfer" to "Transfer now". Admin picks destination team → clicks → done. **(F) `ApproveApplicationDialog` UI.** Removed the "Active from GW" `<input>` + the `fromGameWeek` state + the `approve-from-gw` testid + the `maxGameWeek` prop. **(G) `PlayersTab` no longer takes/threads the `maxGameWeek` prop.** Dropped from `PlayersTabProps` + the function signature. **(H) Admin players page (`src/app/admin/leagues/[id]/players/page.tsx`).** Removed the `maxGameWeek = gameWeeks[0]?.weekNumber ?? 1` computation + the prop. The `gameWeeks` slot from `getLeaguePlayers` is left in the tuple position (as an unconsumed comma) so the function's return shape stays stable — no churn in `getLeaguePlayers` or other consumers. **(I) Overwrite semantics — user's explicit decision.** When admin transfers Player from X→A for GW8, then again X→B for GW8, the existing PLM(A, fromGW=8) is DELETED entirely and a fresh PLM(B, fromGW=8) is created. End state: history shows X up to GW7, B from GW8. Team A is erased from this player's history. Confirmed by the user before implementation — chosen over the "reject same-GW double-move with error" alternative. **(J) Tests.** New `tests/unit/v220_immediate_transfer.test.ts`. Coverage: `computeNextGameWeek` source-level pins (Prisma query shape, asc order, unplayed-first semantic, maxGameWeek+1 fallback, empty-league → 1); `transferPlayer` signature pin (3-arg, no fromGameWeek/fromLeagueTeamId), computeNextGameWeek call, currentActive lookup, both error branches, overwrite-delete branch, normal-update branch, create-with-ADMIN-joinSource, revalidate call; `assignPlayer` signature pin + helper call; `adminApproveApplication` signature pin (no fromGameWeek) + helper call + old fallback gone; UI source-level pins (TransferPanel has no `Effective from` / `futureGWs` / `setFromGW`, calls transferPlayer with 3 args; ApproveApplicationDialog has no `approve-from-gw` / `Active from GW`; props interfaces no longer include `maxGameWeek`; page no longer mentions `maxGameWeek`); runtime mocked-Prisma flow tests for transferPlayer covering normal transfer (closes at nextGW-1, creates at nextGW), same-GW overwrite (deletes intermediate), all-played fallback (`maxGameWeek+1`), empty-league fallback (1), empty-GW-as-unplayed, no-active-team rejection, already-on-destination rejection. Version pin updated in `tests/unit/version.test.ts` from `2.1.1` to `2.2.0`. **(K) Out of scope.** `position` / `positions` / `feeOverride` / `paidStatus` on PLM are NOT carried over to the new PLM on transfer (pre-existing behavior; admin re-enters via the Edit panel). No schema change. No migration. Auto-merge policy: self-mergeable — Unit + tsc green, no destructive surface, no architectural ambiguity.
- **v2.1.1** — docs-only: add Redis/cache cost-awareness standing rules. Upstash plan upgraded from free-tier (500K/day hard quota) to pay-as-you-go (per-request billing) in May 2026. Added `docs/redis-state.md § Redis cost awareness` with the Upstash-specific rule (estimate ops before any new Redis read/write; PR #281 incident as the reference case; prefer `unstable_cache`/`cache()` over external Redis for high-frequency read paths; existing RSVP/auth stores are load-bearing and untouched). Added `docs/methodology.md § Cache cost awareness` with the portable version (quota tiers + per-request tiers; quota spikes; prefer process-local caches for high-frequency reads; external cache is for cross-process state). Added cross-reference rule in CLAUDE.md pointing to both sections. No schema change. No code change.
- **v2.1.0** — multi-boundary Suspense streaming on `/id/<slug>`. v2.0.0 shipped a per-(userId, leagueId) Redis dashboard cache and was reverted in v2.0.1 because the upfront cache-fill burst exhausted the Upstash quota; the user then redirected: instead of caching server work, make the streaming MORE visual and progressive. Today's `/id/<slug>` (v1.99.0) hoists the shell `<Header>` outside Suspense and wraps the body in ONE `<Suspense fallback={<DashboardBodySkeleton />}>` — but that one boundary still gates every banner + the matchday surface on the slowest call in the 7-fetch `Promise.all` (`getPublicLeagueData`, the Redis-RSVP fanout). The whole body flips in one go and a single `animate-pulse` skeleton was easy to miss, so the wait felt like a frozen screen. **(A) Two independent Suspense boundaries on `/id/<slug>`.** The body is split into a fast banner wave and a slow matchday wave that paint as soon as THEIR data resolves. **(B) New `<LeagueBannersBlock>` (server component, async).** Fetches `getLeagueFlags` + `getRecruitingViewerState` + `getUnpaidFeeBannerData` + `getPlannedRosterStats` + `getLeagueDetails` in parallel — five lean reads, none of which touch RSVP/matchday work. Renders `<UnpaidFeeBanner>` + optional private-join indicator + `<RecruitingBanner>` (gated on `flags.visibility === 'PUBLIC_OPEN'` or `forceRecruitingBanner`) + preseason-only `<RegistrationCountdown>` + preseason-only `<LeagueDetailsPanel>` / `<PlannedRosterStats>` fallback. Tagged with `data-testid="league-banners-block"`. Returns `null` on read failure so the matchday boundary still paints. **(C) New `<LeagueBannersSkeleton>`.** Inline `<LoadingSpinner size="sm" tone="bold">` + animate-pulse rectangles sized to the banner footprint. Tagged with `aria-busy="true"` + `data-testid="league-banners-skeleton"`. **(D) New `<LeagueMatchdayContent>` (server component, async).** Wraps the heavy `getPublicLeagueData` read in parallel with cached `getLeagueFlags` + `getLeagueDetails` + `getPlannedRosterStats` + request-scoped `getRecruitingViewerState`. The cached/request-scoped calls re-hit the same entries `<LeagueBannersBlock>` warmed (no double round-trip). Renders `<LeagueMatchdayClient>` with the resolved data. On Prisma rejection returns a `data-testid="matchday-data-unavailable"` surface scoped to this boundary so banners survive a matchday-side blip. **(E) New `<LeagueMatchdayClient>` ('use client').** Owns the `selectedMatchdayId` `useState` (was in `<Dashboard>` pre-v2.1.0). Renders `<GuestLoginBanner>` + `<UserTeamBadge>` + `<ClassicLeagueHomepage>` (with `submitGoalSlot` + `leagueDetailsPanelSlot` for in-season mode) OR `<CompressedMatchdaySchedule>` (preseason) + `<RsvpBar>` as the last sibling. State + RsvpBar stay co-located in the same Suspense boundary because both depend on the same heavy fetch — once `getPublicLeagueData` resolves, both mount together. Tagged with `data-testid="league-matchday-client"`. **(F) New `<LeagueMatchdayContentSkeleton>`.** Mirrors `<ClassicLeagueHomepage>`'s footprint (UserTeamBadge band + matchday card + 3-row availability list) with a centred `<LoadingSpinner size="lg" tone="bold">` INSIDE the matchday-card placeholder so the rotating cue sits at the visual centre of the slow-wave area. Tagged with `aria-busy="true"` + `data-testid="league-matchday-skeleton"`. **(G) New `<LoadingSpinner>` (reusable).** SVG ring with an `animate-spin`-driven 90° stroke arc. `size: 'sm' | 'md' | 'lg'`, `tone: 'subtle' | 'bold'`. Pure server-renderable; no client JS. Default `role="status"` + `aria-label="Loading"` + `data-testid="loading-spinner"`. **(H) `/id/<slug>/page.tsx` rewired.** Outer page resolves slug → leagueId, fires `touchUserDefaultLeague` (waitUntil-wrapped, still page-level so it never sits behind a Suspense boundary), then returns: `<Header leagueTitle={null}>` (live LeagueSwitcher chevron paints immediately, same as v1.99.0) + the dashboard column wrapper (moved up from `<Dashboard>`'s outer div so banners + matchday share the same `max-w-lg` column) + `<main>` with two sibling `<Suspense>` nodes around `<LeagueBannersBlock>` and `<LeagueMatchdayContent>` + footer + `<SuccessConfirmationGate>`. Stops using `<Dashboard>` on this route. **(I) `.animate-in` fade dropped on `/id/<slug>`.** Pre-v2.1.0 Dashboard wrapped the body in `<div className="animate-in pt-2">`; that fade-in keyframe's `transform: translateY(0)` (animation-fill-mode: forwards) establishes a containing block for `position: fixed` descendants — v1.63.1 documented the resulting RsvpBar bottom-anchor bug. With multi-boundary streaming the Suspense fallback → resolved swap IS the visual transition, so the fade is redundant and removing it lets `<RsvpBar>` live inside `<LeagueMatchdayClient>` as a normal sibling without breaking its viewport anchor (no transformed ancestor between RsvpBar and `<html>`). The other Dashboard call sites (`/page.tsx`, `/id/<slug>/md/<id>`, `/id/<slug>/join`, `/matchday/<id>`, `/stats`, `<MultiLeagueHub>`) are untouched — they keep `.animate-in` and Dashboard's existing single-boundary streaming. **(J) Architectural answer to the user's question.** Next.js Suspense streaming is designed for exactly this — splitting one boundary into many is the canonical mechanism. The pre-v2.1.0 limitation was contract-level, not runtime: a single `<Suspense>` resolves only when ALL its descendant promises resolve. Splitting the data fetchers across two `<Suspense>` boundaries (with independent `Promise.all` blocks inside each) means React flushes each boundary's HTML the moment its own promises settle. The `'use client'` `<Dashboard>` component was the structural blocker — it took every prop and held `useState`, so the body couldn't be sliced mid-tree. v2.1.0 solves that by lifting matchday state into a dedicated client component (`<LeagueMatchdayClient>`) and rendering banners as a sibling server component, so each Suspense gets a clean async tree to await. **(K) Tests.** New `tests/unit/v210_multiboundary_streaming.test.ts`. Coverage: version pin + CLAUDE.md release pin + ledger top-entry pin; `/id/<slug>/page.tsx` imports two skeletons + two block components + has TWO `<Suspense>` boundaries (one with each skeleton fallback); page no longer renders `<Dashboard>` and no longer imports `DashboardBodySkeleton`; `<LeagueBannersBlock>` is an async server component that fetches the 5 light reads in `Promise.all` + does not import `getPublicLeagueData`; `<LeagueMatchdayContent>` is async + does import `getPublicLeagueData` + renders `<LeagueMatchdayClient>`; `<LeagueMatchdayClient>` has the `'use client'` directive + owns `useState` for matchday selection + renders `RsvpBar`; skeletons include `<LoadingSpinner>` + `animate-pulse` + the canonical testids + `aria-busy="true"`; `<LoadingSpinner>` is `animate-spin` SVG with `role="status"`. Updated `tests/unit/v199_suspense_streaming.test.ts`: the seven `/id/<slug>` pins that asserted single-boundary structure + Dashboard usage + `getPublicLeagueData` inline + Promise.all-in-page were SUPERSEDED (their assertions describe the v1.99.0 architecture, which v2.1.0 replaces on this route). The `<MultiLeagueHub>` section is unchanged (still single-boundary in v2.1.0 — same trade-off v1.99.0 documented for RecruitingHandoff). Dashboard + skeleton + HubTransitionShell sections preserved. **(L) Out of scope.** `<MultiLeagueHub>` multi-boundary split: not done yet — would require pulling `<RecruitingHandoff>` out of `<Dashboard>`'s `topSlot` and lifting its own column wrapper, broader change than the user explicitly asked for. If the user lands on the hub frequently and still feels the wait, that's the next obvious target. The `/id/<slug>/join` + `/id/<slug>/md/<id>` routes still use `<Dashboard>` directly — same reasoning, they're lower-traffic surfaces. Per the auto-merge policy, this is self-mergeable: Unit + tsc green, no schema/migration, no destructive surface, no architectural ambiguity flagged.
- **v2.0.1** — revert of v2.0.0 (Redis dashboard cache). v2.0.0 added per-(userId, leagueId) Redis-cached dashboard payloads keyed on session+league; the cache-fill burst on the first warm cohort blew through the Upstash daily commands quota within minutes, leaving the site degraded for the rest of the day. Rolled back via `git revert` of #281. The revert restores v1.99.0 behavior exactly (Suspense streaming + `getLeaguePageBundle` per request); no schema change to undo. **Lesson:** before opting into Upstash-wide caching, model worst-case command volume under realistic concurrency (the fanout was N players × M leagues × time-to-warm and exceeded the per-day cap). The user redirected scope toward making the existing v1.99.0 streaming MORE visually progressive — see v2.1.0.
- **v1.99.0** — Suspense streaming on `/id/<slug>` + `<MultiLeagueHub>` (PR 2 of the 3-step league-switch-speed plan). User-reported prod times after v1.98.0 shipped: **cold ~10 s, warm 4-6 s of dead screen** before any HTML hits the browser. The v1.98.0 audit-only estimates (600-1500 ms cold / 400-900 ms warm) were optimistic — actual per-query latencies on Neon serverless under real load are much higher. PR 2 doesn't try to make the work faster; it changes WHEN the user sees something. Pre-v1.99.0 both routes were single async functions that awaited `getLeaguePageBundle` (6 Prisma reads via `Promise.all`) BEFORE flushing any HTML, so the user saw a blank tab for the full bundle window. Post-v1.99.0 the shell — `<Header>` (with the live `<LeagueSwitcher>` chevron + interactive pill bar) — paints immediately and the bundle streams in via `<Suspense fallback={<DashboardBodySkeleton />}>`. **(A) New skeleton component: `src/components/DashboardBodySkeleton.tsx`.** Pure server component, no data, no async. Renders the same outer flex column + `max-w-lg mx-auto` wrapper as Dashboard so the fallback → resolved swap is a content replace with no CLS / no Header re-mount. Inner `<main>` reserves `pt-12` for the fixed Header band; three placeholder cards (`h-7 w-40`, `h-48`, `h-64`) wrapped in `animate-pulse`, modelled on the v1.59.0 route-level `loading.tsx` pattern. Tagged with `aria-busy="true"` + `data-testid="dashboard-body-skeleton"`. **(B) Dashboard learns `noHeader`.** New optional `noHeader?: boolean` prop (defaults false). When true, Dashboard skips its built-in `<Header>` render so the page-level shell Header is the only one mounted on streaming routes. Legacy call sites (`/page.tsx`, `/id/<slug>/md/<id>`, `/id/<slug>/join`, `/matchday/<id>`, `/stats`) leave the prop unset and keep their built-in Header. **(C) Body-pulse from v1.97.0 retired.** Dashboard's `useHubTransition` import + the `aria-busy={isHubPending}` + `animate-pulse pointer-events-none` className flip on the body wrapper are GONE. The Suspense fallback IS the loading state during a league switch — doubling them up would flash on top of itself. The `data-testid="dashboard-body"` wrapper element is preserved for downstream selectors but no longer toggles classes. `<HubTransitionShell>` still owns the top-edge progress strip (visible during `useTransition`-driven navigation), and `<LeagueSwitcher>` still reads `useHubTransition()` for its switcher-side cue — both unchanged. **(D) `/id/[slug]/page.tsx` refactor.** Outer page function awaits `params` + `getLeagueIdBySlug(slug)` (a single cached lookup, ~50 ms warm) and then immediately returns `<><Header leagueTitle={null} hideStatsLink={false} /><Suspense fallback={<DashboardBodySkeleton />}><LeagueDashboardContents slug={slug} leagueId={leagueId} /></Suspense></>`. The heavy 7-call `Promise.all` ([getPublicLeagueData, getLeagueFlags, getRecruitingViewerState, prisma.league.findUnique, getUnpaidFeeBannerData, getPlannedRosterStats, getLeagueDetails]) + `touchUserDefaultLeague` + Dashboard render moves verbatim into the async child `LeagueDashboardContents`. The Promise.all shape was kept identical to pre-v1.99.0 (rather than migrating to the v1.85.0 `getLeaguePageBundle` helper) so the scope stays focused on streaming — all 12 existing tests pinning per-call shapes (`v167_planned_roster`, `v166_player_payment`, `v175_league_details`, `v184_homepage_phase1a`, etc.) keep their pins. The page-level Header passes `leagueTitle={null}` rather than blocking on `getLeagueFlags` for the title — Header's existing default ("T9L '26 春") covers the placeholder window; awaiting the cached flags read would delay the shell flush by the same 150-300 ms it saves. **(E) `<MultiLeagueHub>` refactor.** Outer component is no longer `async` — it returns the streaming shell synchronously: `<HubTransitionShell><Header leagueTitle={active.leagueName} /><Suspense fallback={<DashboardBodySkeleton />}><MultiLeagueHubBody ... /></Suspense></HubTransitionShell>`. `leagueTitle` comes from the already-resolved active membership (no DB read). The heavy `await getLeaguePageBundle` + `touchUserDefaultLeague` + Dashboard render moves into the async child `MultiLeagueHubBody`. `<RecruitingHandoff>` stays inside the suspended body via Dashboard's `topSlot` because it's a small (capped at 2 rows, indexed) Prisma query and decoupling it would require pulling Dashboard's column wrapper out — net cost not worth the partial streaming win. **(F) Streaming correctness.** The page's outer async function awaits only the slug → leagueId resolution before returning JSX with the Suspense child. React's RSC streaming renderer flushes the shell HTML the moment it hits the Suspense boundary, then streams the body chunk when the bundle resolves. No `await` of bundle-related calls survives in the outer function (regression target: the v1.99.0 test counts `getLeaguePageBundle(` invocations and asserts exactly one in `/id/[slug]/page.tsx`). **(G) Expected payoff.** Server work unchanged — bundle still takes 4-6 s warm / 10 s cold. Perceived time: shell paints in <500 ms (single cached slug lookup + Header render), body fills in over the next 4-6 s. The "dead screen" window collapses to the slug-resolve window. **(H) Tests.** New `tests/unit/v199_suspense_streaming.test.ts` (33 tests). Coverage: version pin + CLAUDE.md release pin + ledger top-entry pin; DashboardBodySkeleton exists as default export, renders the matching outer wrapper shape, reserves `pt-12`, uses `animate-pulse`, tags aria-busy + data-testid; Dashboard `noHeader?: boolean` prop declared + destructured with default false + skips Header on `!noHeader`; Dashboard no longer imports `useHubTransition` (stripped-comments scan); does NOT bind `aria-busy={isHubPending}`; does NOT toggle the `isHubPending → animate-pulse + pointer-events-none` pattern; keeps the `data-testid="dashboard-body"` wrapper. `/id/[slug]/page.tsx` imports Suspense from react + DashboardBodySkeleton + Header; Header appears BEFORE `<Suspense>` in the stripped source order; the Suspense fallback is `<DashboardBodySkeleton />` literally; `<Dashboard noHeader>` is rendered; `LeagueDashboardContents` is async; bundle call inside child; regression target counts bundle invocations (exactly 1). `<MultiLeagueHub>` mirror coverage: imports + Suspense ordering + `leagueTitle={active.leagueName}`; outer hub is sync (`export default function` not `export default async function`); `MultiLeagueHubBody` is async + awaits bundle; `<HubTransitionShell>` still wraps the streaming subtree. HubTransitionShell still renders its progress strip + still exports `useHubTransition`. **(I) Existing tests relaxed.** `v1971_header_chevron_bar.test.ts` body-skeleton-overlay section (lines 362-398) was pinning the v1.97.0 `aria-busy={isHubPending}` + `animate-pulse pointer-events-none` shape on Dashboard — the v1.99.0 supersession removes that shape, so those four assertions are dropped and replaced with the streaming-era contracts (testid wrapper preserved + LeagueSwitcher still reads useHubTransition for the progress strip + MultiLeagueHub still mounts the shell). The v1.97.x version-pin range regex (`1\.(97\.[1-9]|9[89]\.\d+|\d{3,}\.\d+)`) was already permissive enough to match `1.99.0` so no edits there. The v1.98.0 exact-value pins (`APP_VERSION === '1.98.0'`) were relaxed to a range matching v1.98+ — same pattern v1.97.5 used when bumping past v1.97.1's exact pins. Full suite green. tsc clean. **Out of scope.** PR 3 (per-(userId, leagueId) Redis cache of the dashboard payload) per the diagnostic plan. The user will measure perceived time after this ships and decide whether server-side reduction is still needed. If the heavy bundle still feels slow even with streaming, PR 3 lands. If shell + skeleton + streaming body is enough, PR 3 may not be needed at all. Per the auto-merge policy (docs/methodology.md § Auto-merge), this is self-mergeable: tests green, no schema/migration, no destructive surface, no architectural ambiguity flagged (refactor matches the brief literally).
- **v1.98.0** — viewer-collapse perf refactor (PR 1 of a 3-step league-switch-speed plan). User report: even after v1.97.4 (Prisma pool widening) + v1.97.5 (cookie default) + v1.97.2 (router.prefetch + URL-aware navigation), "it is still extremely slow to change between leagues." Diagnosis (audit-only, no live ms numbers): the static `LeagueData` blob is already wrapped in `unstable_cache` with `revalidate: 300`, so warm switches don't re-fetch the big roster query. What they DO pay on every render is the per-user fan-out — `<HomepageRouter>` → `homepageRouting.getApprovedMembershipsAndDefault`, `getRecruitingViewerState`, `getUnpaidFeeBannerData` each ran their OWN `getServerSession` + `prisma.user.findUnique` + (often) `prisma.player.findUnique` sequence. Three independent flows, identical base work, never deduped. Plus a stray `prisma.league.findUnique` for league.{id,name,abbreviation,ballType} sat inside `getLeaguePageBundle` even though `getLeagueFlags` already cached the same row under the `leagues` tag. **(A) New shared resolver: `src/lib/viewer.ts`.** Single function `getViewer = cache(async () => …)` wrapped in React's `cache()` (from 'react', not Next's `unstable_cache`) so every caller within the same RSC render boundary shares the SAME Promise. Returns `{ hasSession, userId, lineId, user: { id, playerId, defaultLeagueId } | null, player: { id } | null }`. Resolution: `getServerSession(authOptions)` first; if null → `hasSession: false`; if session has neither `userId` nor `lineId` (admin-credentials shape) → `hasSession: true` with both ids null so callers can still distinguish "no visitor" from "authenticated-without-public-identity". User lookup tries `where: { id: userId }` first, falls back to `where: { lineId }` for grandfathered LINE sessions whose JWT predates v1.28.0 stage α.5. Player lookup uses the back-FK `Player.userId` (the same lookup pre-v1.98.0 `getRecruitingViewerState` used) and falls back to `Player.lineId` for any drift where User.playerId was never backfilled. Every Prisma failure degrades gracefully — returns nulls and logs `console.warn`, never throws. **(B) Three readers swung onto the shared helper.** `getRecruitingViewerState` drops its `getServerSession + user.findUnique + player.findUnique` preamble; reads `viewer.hasSession` / `viewer.player`; still runs the per-league `player.findUnique({ where: { id: viewer.player.id }, select: { leagueAssignments } })` because the filter (leagueId-scoped, both APPROVED and PENDING) is genuinely per-league. `getUnpaidFeeBannerData` drops its `getServerSession + user.findUnique` preamble; reads `viewer.user.playerId`; still runs the per-league `plm.findFirst` and `league.findUnique`. `homepageRouting.getApprovedMembershipsAndDefault` drops its `prisma.user.findUnique` (the helper read defaultLeagueId + playerId there); reads from `viewer.user`; still runs the per-player `leagueAssignments` query with the homepage-specific filter (APPROVED + toGameWeek null + leagueTeamId not null). `<HomepageRouter>` drops its `getServerSession` and reads `viewer.userId` / `viewer.lineId` directly; the existing `viewer={{ userId, lineId }}` prop shape into `<MultiLeagueHub>` is preserved verbatim. Note: `getRecruitingViewerState` technically runs one MORE player.findUnique than pre-v1.98.0 (its pre-refactor query did `where: { userId: user.id }, select: { id, leagueAssignments }` in one shot; post-refactor that splits into getViewer's `select: { id }` + the resolver's `select: { leagueAssignments }`). That's offset by the THREE deduped `getServerSession + user.findUnique` calls — net win is large for the multi-league hub (which hits all three readers per render) and break-even for single-page routes that hit only one. **(C) Stray league fetch folded into `getLeagueFlags`.** `getLeagueFlags` select now also pulls `id`, `name`, `abbreviation`, `ballType`. Return shape gains `league: { id, name, abbreviation, ballType } | null`; DEFAULT_FLAGS sets `league: null`. `LeaguePageBundle` is built from `flags.league` instead of a standalone `prisma.league.findUnique` — the bundle file no longer imports `prisma` at all. Same query plan, same row, same 30 s cached TTL — but one fewer round-trip per render on EVERY league-scoped page (apex, `/id/<slug>`, `/id/<slug>/md/<id>`, `/id/<slug>/join`, `/schedule`, `/stats`). **(D) Pre/post Prisma-query count on a warm /test hub render** (multi-league user, static league data cached): pre-v1.98.0 — 3× getServerSession, 3× user.findUnique, 2× player.findUnique (recruiting + homepage; unpaid fee skips), 1× plm.findFirst, 1× league.findUnique (unpaid fee), 1× league.findUnique (standalone in bundle) = 11 round-trips. Post-v1.98.0 — 1× getServerSession (deduped), 1× user.findUnique (deduped), 1× player.findUnique (deduped, base lookup), 1× player.findUnique (recruiting leagueAssignments), 1× player.findUnique (homepage leagueAssignments), 1× plm.findFirst, 1× league.findUnique (unpaid fee) = 7. Saving: 4 round-trips + 2 getServerSession invocations per render. Expected −150 to −400 ms warm. **(E) Tests.** New `tests/unit/v198_viewer_collapse.test.ts` (50 tests). Coverage: version pin + CLAUDE.md release pin; viewer.ts uses React `cache()` + `getServerSession(authOptions)` + Viewer shape including `hasSession`; the userId/lineId fallback chain + `defaultLeagueId: true` selection + `Player.userId` then `Player.lineId` fallback; the `hasSession: false` vs `hasSession: true / userId+lineId null` distinction. Per-reader regression targets: each of recruitingViewerState, unpaidFeeBanner, homepageRouting, HomepageRouter must import `getViewer` from `@/lib/viewer` AND must NOT contain `getServerSession(` in stripped source (pre-v1.98.0 anti-pattern). unpaidFeeBanner must not call `prisma.user.findUnique` (folded into viewer). homepageRouting must call getViewer inside `getApprovedMembershipsAndDefault` (slice-based pin, not regex-based — avoids tripping on the Promise<{ ... }> return-type generic). Bundle must drop the import on `@/lib/prisma` entirely. getLeagueFlags select must pin each new column independently. Runtime tests against vi.doMock for the userId path, lineId fallback path, no-session, admin-credentials, and user-lookup-rejection branches. Updated 5 pre-existing tests: `preseasonRecruitingToggles.test.ts` (select shape relaxed to per-column pin + runtime fixtures gain id/name/abbreviation/ballType/league.null); `v184_homepage_phase1a.test.ts` (visibility-select pin relaxed); `v185_homepage_phase1bc.test.ts` (HomepageRouter `getServerSession + authOptions` pin re-targeted to `getViewer()` import + call); `v166_player_payment.test.ts` (banner-resolver "null on no session / no userId / no playerId" gates collapsed to `!viewer.user` + `!viewer.user.playerId` shapes); `v167_planned_roster.test.ts` (recruitingViewerState `!player && lineId` and `!userId && !lineId` pins re-targeted to `getViewer` delegation + `!viewer.player` gate). Full suite green. tsc clean. **Out of scope.** PR 2 (Suspense streaming + body skeleton overlay refinement) and PR 3 (per-(userId, leagueId) Redis cache of the dashboard payload) per the diagnostic plan — user agreed to "PR 1 → measure → PR 2 only if still slow → PR 3 only if PR 2 not enough". This PR ships PR 1 in isolation so the user can evaluate prod impact before committing to the remaining complexity. Per the auto-merge policy (docs/methodology.md § Auto-merge), this is self-mergeable: tests green, no schema/migration, no destructive surface, no architectural ambiguity flagged (refactor matches the audit-grounded plan literally).
- **v1.97.5** — cookie-backed default-league preference (`t9l_default_league`). User feedback: "can we look at cookie and save settings on which league to show by default". Today's priority chain on `/test` was URL `?league=<id>` > DB `User.defaultLeagueId` > alphabetical-first. The DB read is a Prisma round-trip on every cold render of a returning visitor. **Cookie fast-path.** New `t9l_default_league` cookie (HttpOnly, SameSite=Lax, path=/, 1-year maxAge, `secure` mirrors NEXTAUTH_URL https detection) sits between the URL and the DB. Priority becomes URL > **cookie** > DB > alphabetical-first. Each layer still validates against the viewer's APPROVED memberships, so a stale/tampered cookie silently falls through — same gate URL inputs already pass through. **New lib: `src/lib/defaultLeagueCookie.ts`.** Pure helpers + constants: `DEFAULT_LEAGUE_COOKIE_NAME = 't9l_default_league'`, `DEFAULT_LEAGUE_COOKIE_MAX_AGE = 60*60*24*365`, `defaultLeagueCookieOptions()`, `normaliseDefaultLeagueCookieValue(raw)`. The normaliser is the shape gate: rejects non-strings, empty, >128 chars, anything with characters outside `[A-Za-z0-9_-]` (catches cookie-injection vectors via `;`, `"`, whitespace, newlines). Accepts both cuid-shaped (`cmovaw1jr00000kg8s9r9bnq6`) and slug-shaped (`l-minato-2025`) league ids since both appear in this codebase. **New server actions: `src/app/api/default-league/actions.ts`.** `'use server'` module exporting two async functions: `setDefaultLeagueCookie(leagueId)` runs the normaliser then `cookies().set(...)`; `clearDefaultLeagueCookie()` runs `cookies().delete(...)`. Neither is auth-gated — the cookie value is non-sensitive and the read site validates against the caller's own memberships. **Write site.** `<LeagueSwitcher>` pill-click handler fires `void setDefaultLeagueCookie(m.leagueId).catch(() => {})` BEFORE `startNavigation(router.push(...))` so the cookie write doesn't block the navigation. Fire-and-forget — on failure the next render falls through to the DB (no regression). **Read site.** `<HomepageRouter>` imports `cookies()` from `next/headers`, reads `t9l_default_league` via the normaliser, threads `cookieLeagueId` into `resolveHomepagePersona`. **Sign-out.** Both signOut buttons (LineLoginButton account-menu dropdown + AdminNav dropdown + AdminNav mobile drawer) now `await clearDefaultLeagueCookie()` before NextAuth's `signOut(...)`. Wrapped via a tiny `signOutWithCookieClear` helper inside AdminNav so both AdminNav sites share one definition; LineLoginButton inlines the same shape into its single onClick. **Routing helper changes (`src/lib/homepageRouting.ts`).** `ResolveInput` + `classifyPersona` accept optional `cookieLeagueId?: string | null`. `cookieLeagueId` is null-default so v1.93.0-style callers omitting it keep working (backward compat regression target). The priority chain inside `classifyPersona` is: `preferred ?? cookied ?? stored ?? memberships[0]`. **Tests.** New `tests/unit/v1975_default_league_cookie.test.ts` (41 tests). Coverage: version pin; cookie name + maxAge constants; cookieOptions HttpOnly/SameSite/path/maxAge; secure flag flips on NEXTAUTH_URL https/http; normaliser accepts cuid + slug; trims whitespace; rejects empty/whitespace/non-string/oversized/shape-abusive (space, semicolon, quote, newline); 128-char boundary; cookie name `t9l_` prefix; server actions module `'use server'` directive + only async exports + uses `next/headers` + jar.set/delete with the canonical name; classifyPersona priority order across all four layers; stale cookie falls through; tampered cookie cannot pin a non-member league; backward compat when caller omits cookieLeagueId; resolveHomepagePersona forwards cookieLeagueId; HomepageRouter imports cookies + normaliser + reads-then-normalises-then-forwards; LeagueSwitcher imports setDefaultLeagueCookie + calls it before startNavigation + fire-and-forget (no await); LineLoginButton + AdminNav sign-out wrapped (no naked signOut click). Stash-pop verified the new behavioural tests fail on the v1.97.4 baseline. Full suite green. tsc clean. **Out of scope (still open from v1.97.4).** Same-league-tap UX (clicking the already-active pill is still a no-op) — needs design input. React #418 hydration mismatch — to reassess post-v1.97.4 503 fix; if it persists, the fix is likely wrapping the cookie read or the searchParams hydration in `<Suspense>`. Per the v1.95.1 auto-merge policy, this is self-mergeable: tests green, no destructive surface, no schema/migration, no architectural ambiguity (cookie behaviour matches the brief literally).
- **v1.97.4** — widen Prisma connection pool to fix `?_rsc=` 503 storm. User report (Chrome MCP diagnostics): every `?_rsc=` prefetch request returned HTTP 503, league switches took 2-3 s instead of the v1.94.0 ~50 ms target, React error #418 (hydration mismatch) in console. **Root cause (from prod logs).** `P2024 — Timed out fetching a new connection from the connection pool. (Current connection pool timeout: 10, connection limit: 5)` firing on `prisma.user.findMany`, `prisma.lineLogin.findMany`, `prisma.playerLeagueMembership.findMany`. The pooled `DATABASE_URL` has no `connection_limit` query param so Prisma falls back to its default (~5 on Vercel 1-vCPU serverless). `getLeaguePageBundle` (`src/lib/leaguePageData.ts`) issues 7 queries via `Promise.all` — alone it fills the pool; combined with Next.js's auto-prefetch of every visible `<Link>` (`/?_rsc=`, `/schedule?_rsc=`, etc.) AND the v1.97.2 router.prefetch() loop, queries queue past the 10 s timeout. Each timeout → 503 from Vercel's edge. **Fix (`src/lib/prisma.ts`).** New `withPoolParams(rawUrl)` pure helper appends `?connection_limit=20&pool_timeout=20` to the DATABASE_URL when neither is set (respects operator-supplied values verbatim). The Prisma client is built with `datasourceUrl: withPoolParams(process.env.DATABASE_URL)` via the spread-when-set pattern (omits the option when DATABASE_URL is unset so dev/test fallback to Prisma's schema-resolved URL). Only the POOLED URL is touched — `DATABASE_URL_UNPOOLED` (used by migrations + admin scripts) stays bare since those callers have serial query patterns. 20 + 20 is the Vercel/Prisma/Neon recommendation for serverless + pgBouncer; the Neon `-pooler.` host this project uses is built for high concurrency through transaction pooling, so widening Prisma's local pool just lets one function instance use what Neon's pooler already affords. **Why not "remove the eager prefetch".** Removing the v1.97.2 prefetch loop would mask the symptom on `/test` but Next.js's own `<Link prefetch>` calls (Header navlinks + every other `<Link>` on the page) would still hit the same systemic pool exhaustion. The pool widening is the correct layer to fix at. **Tests.** New `tests/unit/v1974_prisma_pool.test.ts` (16 tests): version pin; `withPoolParams` exported; injects `connection_limit=20` + `pool_timeout=20` on bare URL; preserves `sslmode`/`channel_binding`/host/db; does NOT overwrite operator-supplied values (regression target — `connection_limit=42` survives unchanged); accepts `postgres://` alias; pure no-op on empty/non-postgres/unparseable; handles trailing-newline quirk (prod env var has it); does NOT touch `DATABASE_URL_UNPOOLED`; Prisma client gets `datasourceUrl` via the conditional spread. Stash-pop verified the new behavioural tests fail on the v1.97.3 baseline. Full suite green. tsc clean. **Out of scope.** Same-league-tap UX (clicking the already-active pill is still a no-op) — left as a follow-up needing design input. React #418 hydration mismatch — likely from `useSearchParams()` returning null on SSR vs real value on client hydration; will reassess once 503 storm is gone (the noisy retries make it harder to isolate). Per the v1.95.1 auto-merge policy, this is self-mergeable: tests green, no destructive surface, no schema/migration, no architectural ambiguity (root cause is unambiguous from prod logs).
- **v1.97.3** — combined league-name + chevron picker trigger on the Header (`src/components/Header.tsx` + `src/components/LeagueSwitcher.tsx`). User report: (1) "Clicking on t9l 2026 spring button still does nothing" — v1.97.2 didn't resolve this because v1.97.2 only fixed PILL behaviour, not the Header league-name link. (2) "Remove the hyperlink from the league name text, and instead make it just open the League picker." **Diagnosis.** The "t9l 2026 spring button" the user clicks is the Header league-name span, rendered pre-v1.97.3 as `<Link href="/"><span data-testid="header-league-title">{leagueTitle ?? "T9L '26 春"}</span></Link>`. For a multi-league user viewing `/test` whose default league matches what the page resolves (the dominant case via `User.defaultLeagueId`), clicking the league name navigates to `/`, which renders the SAME default-league dashboard — perceived as "nothing happened". Issues 1 and 2 are the same underlying complaint with a unified fix. **Fix.** When `memberships.length >= 2`, Header renders `<LeagueSwitcher leagueTitle={titleText} />` and LeagueSwitcher absorbs the title rendering into the trigger button so the WHOLE `[Title] [Chevron]` region toggles the picker bar. The trigger picks up the legacy brand-link styling (`font-display + font-black + uppercase + tracking-tight + leading-none + gap-1.5`) when `leagueTitle` is present, so the visual surface is unchanged for users. The `<span data-testid="header-league-title">` lives INSIDE the trigger button (was inside the Link), keeping v1.73.0 + later source-grep tests happy. Single-league users (`memberships.length < 2`) fall through to the legacy `<Link href="/"><span>{leagueTitle}</span></Link>` form — they don't have a picker, so the home-link affordance stays useful. **Header refactor.** New `useMemberships()` import + `const memberships = useMemberships(); const hasPicker = memberships.length >= 2; const titleText = leagueTitle ?? "T9L '26 春"`; ternary `hasPicker ? <LeagueSwitcher leagueTitle={titleText} /> : <Link href="/">...{titleText}...</Link>`. The standalone `<Link href="/">` is GONE from the unconditional render path (regression target — re-introducing it brings back the "click does nothing" bug). **LeagueSwitcher refactor.** New `LeagueSwitcherProps { leagueTitle?: string | null }` interface + destructured default param. The trigger button's className branches on `leagueTitle` presence: with title → brand styling + `gap-1.5` + title span before chevron; without → v1.97.1 chevron-only styling. All v1.97.1 contract preserved verbatim: `data-testid="league-switcher-trigger"`, `aria-expanded/haspopup/controls`, `ChevronDown` with 180° rotate, `memberships.length < 2 → return null`. All v1.97.2 logic preserved: `useSearchParams` URL-aware `currentLeagueId`, eager `router.prefetch` effect, same-league-tap short-circuit (still useful — clicking a pill when its url-aware current id matches still closes the bar without re-fetching). **Tests.** New `tests/unit/v1973_combined_picker_trigger.test.ts` (16 tests) pinning: version + LeagueSwitcher props interface + destructured default + title span conditional render inside trigger + brand styling on trigger when title present + Header useMemberships import/call + hasPicker derivation + multi-league branch + single-league fallback + titleText derivation + regression target on the unconditional `<Link href="/">` (each occurrence must sit within ~200 chars of a ternary fallback marker or `header-home-link` testid) + v1.97.1 trigger contract preserved + v1.97.2 URL-aware + prefetch preserved. Stash-pop verified 16 of 16 fail on the v1.97.2 baseline (all assertions target post-v1.97.3 shape). Three pre-existing tests relaxed: `leagueSwitcher.test.ts` order-after-Link constraint dropped (LeagueSwitcher now IS the title surface in the multi-league branch); `accountPlayerCleanupV162.test.ts` self-closing-tag regex relaxed to accept prop-bearing form; `v1971_header_chevron_bar.test.ts` same; `v173_league_abbreviation.test.ts` `{leagueTitle` regex broadened to also match `leagueTitle ??`. Full suite: 3645 tests pass, 2 skipped, 0 failed. tsc clean. UI-only; no schema, no migration. Per the v1.95.1 auto-merge policy, this is self-mergeable: tests green, no destructive surface, no architectural ambiguity flagged (user explicitly requested the title→picker behaviour in issue #2).
- **v1.97.2** — two-defect fix on the `/test` multi-league hub Header chevron picker (`src/components/LeagueSwitcher.tsx`). User report: (1) "When I click on t9l 2026 Spring button nothing happens." (2) "Loading takes too long between leagues." **(A) Defect 1 — URL/session mismatch.** Pre-v1.97.2 `currentLeagueId = memberships.find((m) => m.isCurrent)?.leagueId`. The `m.isCurrent` flag is SSR-baked in `src/app/layout.tsx:98` via `currentLeagueId: session.leagueId`, which is a fixed value set at JWT-refresh time and does NOT track in-hub `?league=<id>` navigation. A user viewing `/test?league=<other>` would see the pill for their default league (e.g. "t9l 2026 Spring") still styled as "selected" (the v1.94.0 `useOptimistic` initializer uses `currentLeagueId`) and the same-league-tap short-circuit at LeagueSwitcher.tsx:138-144 would silently swallow the click. Pre-v1.97.1 this was masked by the in-page `<LeagueSwitcherTabs>` (URL-aware via `activeLeagueId` prop); v1.97.1 deleted that and made the Header chevron the canonical picker, exposing the SSR/URL drift. **Fix.** New `useSearchParams()` import + read. On the hub (`onHub = pathname === '/test' || startsWith('/test/')`), derive `urlLeagueId = onHub && searchParams ? searchParams.get('league') : null`, then verify against memberships (`memberships.some((m) => m.leagueId === urlLeagueId)`) to neutralise URL tampering. Final priority: `currentLeagueId = verifiedUrlLeagueId ?? sessionCurrentLeagueId`. Off-hub routes (`/id/<slug>`) keep the SSR-baked `isCurrent` behaviour verbatim. Same-league-tap short-circuit is PRESERVED — the issue was the value `currentLeagueId` was wrong, not the short-circuit logic. **(B) Defect 2 — cold prefetch window.** Per v1.94.0 design, `<Link prefetch>` was supposed to warm RSC destinations on hover/visibility so warm taps land in ~50 ms. But the bar's `<Link>`s only mount when `open === true`, so prefetch only fires AFTER the chevron is clicked — a fast pill-tap within ~200 ms misses the warm window. Measured prod `/test` warm TTFB: 200-340 ms cold. **Fix.** New `useEffect(() => { … router.prefetch(`/test?league=${encodeURIComponent(m.leagueId)}`) … }, [onHub, memberships, router])` runs on mount when `onHub && memberships.length >= 2`, iterating every membership. Bounded by the user's roster (typically 2-4 leagues) so the prefetch cost is small even on multi-league users. Subsequent pill taps land inside Next.js's route cache window. Off-hub the effect skips (no benefit prefetching `?league=` URLs from `/id/<slug>`). **(C) Refactor.** The `onHub` declaration moved from inside the function body (below the early-return) to the top of the component so the new `useEffect` deps array can reference it. **(D) Tests.** New `tests/unit/v1972_test_picker_fixes.test.ts` (13 tests) pinning: version; `useSearchParams` import + call; `onHub && searchParams ? .get('league') : null` derivation (comment-stripped regex so the doc-paragraph mention doesn't false-positive); the memberships verification gate; the `verifiedUrlLeagueId ?? sessionCurrentLeagueId` fallback chain; the session-derived fallback via `Membership.isCurrent`; same-league-tap short-circuit preserved; `router.prefetch` inside `useEffect` gated on `onHub` + `memberships.length >= 2`; iterates memberships with `encodeURIComponent`; effect deps `[onHub, memberships, router]`; exactly one `const onHub =` (catches re-introduction of the duplicate declaration). Stash-pop verified 11 of 13 fail on the v1.97.1 baseline (the 2 passing are structural: `onHub` declared-once and the same-league short-circuit still present — both characteristics of the v1.97.1 code shape). Two v1.97.1 test pins relaxed to `/1\.97\.[1-9]/` range so future patches don't re-trigger them. Full suite: 3640 tests pass, 2 skipped, 0 failed. tsc clean. UI-only; no schema, no migration. Per the v1.95.1 auto-merge policy, this is self-mergeable: tests green, no destructive surface, no architectural ambiguity.
- **v1.97.1** — course-correct on v1.97.0. **User clarification:** "the chevron I'm referring to is the chevron in the header navbar, right beside the text containing the league name. When pressed, a horizontal 1-line bar should show that is scrollable." v1.97.0 misread the brief and added a NEW chevron trigger INSIDE the in-page hub picker `<LeagueSwitcherTabs>`, opening a `flex-wrap` dropdown of rectangular pills. The user wanted the EXISTING Header chevron (`src/components/LeagueSwitcher.tsx`, originally v1.52.0) to expand into a HORIZONTAL 1-LINE SCROLLABLE BAR — `overflow-x-auto`, not wrap. **(A) Header chevron rebuild.** `<LeagueSwitcher>` rewritten from "vertical dropdown menu of league names" to "horizontal 1-line scrollable pill bar." Trigger stays as `ChevronDown` next to the brand title (Header.tsx:51 — unchanged mount), now `aria-controls="league-switcher-bar"` + `aria-expanded` + `aria-haspopup="menu"`. Open-state bar is `fixed top-12 left-1/2 -translate-x-1/2 w-full max-w-lg z-40` (anchored to viewport so it lines up with the fixed `h-12` Header column regardless of trigger x-position); inner row uses `pill-scrollbar flex items-center gap-2 overflow-x-auto px-3 md:px-4 py-2`. Each pill is `shrink-0 inline-flex items-center min-h-[36px] h-9 px-3 rounded-lg border-2 whitespace-nowrap no-underline` so the strip stays on one line; active pill carries `bg-primary text-primary-foreground border-primary shadow-[var(--glow-primary-md)]` (v1.96.1 visual preserved), inactive `bg-surface text-fg-mid border-border-default hover:border-primary/40`. `useOptimistic(currentLeagueId)` + `useHubTransition()` + `startNavigation(() => { setOptimisticActiveId(m.leagueId); router.push(href, { scroll: false }) })` ported from v1.93.0 / v1.96.1 / v1.97.0 verbatim — same just-clicked-pill spinner gate (`isPending && selected` against optimisticActiveId, NOT stale currentLeagueId). Same outside-click + Escape close pattern (`mousedown` + `touchstart` + `keydown` listeners gated on `open`). **(B) Pathname-aware navigation.** New `usePathname()` discriminator: on `/test` or `/test/*` the chevron navigates via `/test?league=<id>` so the hub stays mounted and the body-skeleton overlay can pulse; everywhere else it navigates to `/id/<slug>` (the historical v1.52.0+ behaviour). `useHubTransition()` returns its default no-op outside `<HubTransitionShell>`, so cross-page navigations just run synchronously without trying to wrap them in a transition. **(C) LeagueSwitcherTabs deleted.** `src/components/homepage/LeagueSwitcherTabs.tsx` removed entirely (duplicate surface; the Header chevron is canonical). `MultiLeagueHub.tsx` topSlot now renders just `<RecruitingHandoff>`; `memberships` + `activeLeagueId` props still flow through for the `active.slug` lookup + `excludeIds` derivation. `<HubTransitionShell>` wrapper around Dashboard is preserved verbatim — its `useHubTransition()` context now feeds the Header chevron inside the Dashboard tree. **(D) Body-skeleton overlay preserved.** Dashboard's v1.97.0 body wrapper (`data-testid="dashboard-body"` + `aria-busy={isHubPending}` + `animate-pulse pointer-events-none` while pending) is untouched. The driver moved from in-page LeagueSwitcherTabs.startNavigation to Header LeagueSwitcher.startNavigation, but the shared `useHubTransition()` context means the body dim still fires on `/test` in-place navigations. **(E) `.pill-scrollbar` re-added.** v1.97.0 deleted the utility from `globals.css` as part of the now-reverted "no callers post-redesign" cleanup. Re-introduced verbatim from v1.96.1 (scrollbar-width thin + 4px webkit track + rounded surface-md thumb + fg-low hover) — the Header chevron's open bar reuses it. **(F) Tests.** New `tests/unit/v1971_header_chevron_bar.test.ts` (~50 tests) pinning: version + CLAUDE.md header + ledger top-entry; LeagueSwitcherTabs deletion (`existsSync` + MultiLeagueHub no longer imports it); Header chevron trigger structure (testid + ChevronDown + 180° rotate + aria attrs + memberships.length < 2 hide); open-state bar shape (`overflow-x-auto` REQUIRED + `flex-wrap` FORBIDDEN as v1.97.0 regression target + `shrink-0` + `whitespace-nowrap` + `pill-scrollbar` + `fixed top-12 max-w-lg`); outside-click + touchstart + Escape listeners + cleanup + open-gated effect; click handling (power-user gestures, same-league short-circuit against `currentLeagueId`, normal click wraps optimistic + router.push inside startNavigation, pathname-aware href branches); useOptimistic + selected against optimisticActiveId + showSpinner gate (v1.94.0 regression targets carried forward); rectangular pills + primary glow + active:scale-[0.96] + no-underline (v1.96.1 carried forward); Dashboard body-skeleton wiring untouched + Header chevron reads useHubTransition + MultiLeagueHub still wraps Dashboard in HubTransitionShell; `.pill-scrollbar` reintroduced with the four required CSS properties. v1.97.0 test file `tests/unit/v197_chevron_dropdown.test.ts` deleted (superseded — covered the removed in-component chevron). Existing `v185_homepage_phase1bc.test.ts` LeagueSwitcherTabs describe block + `v193_league_picker_perf.test.ts` "prefetch + optimistic flow" describe block retired (their behaviours now pinned in v1971_*); the v193 `setUserDefaultLeague` / `router.refresh()` forbidden-shape pins re-target the new Header chevron source. tsc clean; Vitest green. Per the v1.95.1 auto-merge policy, this PR is self-mergeable: tests green, UI-only (no schema, no migration), no architectural ambiguity. **Out of scope:** the body skeleton currently only pulses for `/test` in-place switches; cross-route navigations (`/id/<slug>` → `/id/<otherslug>`) rely on Next.js's own route transition. Adding a body-skeleton equivalent to the per-league pages would need a separate HubTransitionShell mount + a way to drive it from the Header — left as a follow-up if the user reports the cross-route case still feels broken.
- **v1.97.0** — chevron-collapsible league switcher + body skeleton overlay on the multi-league hub (`/test`). Two coupled UX changes responding to the same user thread that drove v1.96.1: (1) the persistent horizontal-scroll pill strip felt like wasted vertical chrome on every render even when the user wasn't switching leagues, and (2) the v1.93.0 click → `<Link prefetch>` navigation gave no body-level feedback that the dashboard was about to refresh, so warm-cache clicks felt fast but cold ones felt broken. **(A) Switcher redesign.** `<LeagueSwitcherTabs>` collapses from "always-rendered pill strip" to "chevron trigger + on-demand dropdown bar." Trigger row: `<button>` carrying the active league's name + a `<ChevronDown>` (lucide-react) that rotates 180° via `transition-transform duration-200`. Full width inside the dashboard column, 44 px tall (`h-11`), `bg-card border border-border-default`, hover bumps to `bg-surface-md` with a `border-primary/40` tint. Trigger wires `aria-expanded={open}` + `aria-haspopup="menu"` + `aria-controls="league-switcher-bar"` for AT. Click flips `useState<boolean>(false)` via `setOpen((v) => !v)`. **(B) Dropdown bar.** Renders ONLY when open (mount-on-open keeps the closed-state DOM minimal). Positioned `absolute left-0 right-0 top-full mt-1.5 z-30` inside a `relative` container so it overlays the dashboard content below without pushing it down. `role="menu"` + `aria-label="Switch league"` + matching `id="league-switcher-bar"`. Pills: rectangular (`rounded-lg`, not the prior `rounded-full`), `flex-1 min-w-[44%] min-h-[44px] border-2`, wrap on overflow (`flex-wrap gap-1.5`), `bg-surface` on inactive (so they stand out against the bar's `bg-card` panel — `bg-card` would blend), `bg-primary text-primary-foreground border-primary shadow-[var(--glow-primary-md)]` on active (the existing pink-glow shadow already used by the brand button — token reused). Hover bumps inactive to `bg-surface-md text-fg-high border-primary/40`. `active:scale-[0.97]` for tactile press. `no-underline` defends against the global `a { text-decoration: underline }`. **(C) Close behaviors.** New `containerRef` + `useEffect` (gated on `open`): `mousedown` + `touchstart` outside the wrapper → `setOpen(false)`; `Escape` keydown → close. All three listeners cleaned up on cleanup. Same listener shape as `<LeagueSwitcher>` (the existing Header chevron) so the two switchers behave identically. **(D) Click handling.** Same-league click: `e.preventDefault()` + close, no navigation. Power-user gestures (meta/ctrl/shift/alt/non-primary-button): close the dropdown but let the browser navigate natively (cmd-click new tab still works). Normal click: close, then inside the shared `startNavigation`, set the optimistic active id + `router.push(href, { scroll: false })`. The v1.94.0 regression target — `selected` computed against `optimisticActiveId` rather than the stale server-supplied `activeLeagueId` — is preserved verbatim; the just-clicked pill shows the v1.96.1 `animate-spin` ring, not the previously-active one. **(E) Body skeleton overlay.** Dashboard now imports `useHubTransition` from `./homepage/HubTransitionShell` and reads `isHubPending`. The body content inside `.animate-in pt-2` is wrapped in a new `<div data-testid="dashboard-body" aria-busy={isHubPending} data-hub-busy={…} className={isHubPending ? 'animate-pulse pointer-events-none transition-opacity duration-150' : 'transition-opacity duration-150'}>`. Wrapping INSIDE `.animate-in pt-2` (rather than replacing it) keeps the one-shot fade-in animation isolated from the conditional pulse, so toggling `isHubPending` never restarts the fade-in. The topSlot (which holds the switcher) lives ABOVE this wrapper inside `<main>`, so the trigger + dropdown stay interactive during pending. On routes that don't mount `<HubTransitionShell>` (`/id/<slug>`, `/id/<slug>/md/<id>`), `useHubTransition()` returns the default no-op `{ isPending: false }`, so the body dim never fires — those routes render identically to v1.96.1. **(F) Prefetch trade-off.** Pre-v1.97.0 all per-league `<Link prefetch>` mounted on every render, so destinations prefetched on viewport intersection / hover and warm-cache clicks landed inside the route cache window. Post-v1.97.0 the Links only mount while the dropdown is open, so the first navigation after opening pays one cold RSC fetch (~250-400 ms). The body skeleton (which fires on click via `isPending`) communicates this loading state explicitly, which was the v1.92.x user complaint that v1.93.0's tiny pill-pulse never fully addressed. **(G) Dead-code cleanup.** Removed `.pill-scrollbar` utility from `globals.css` — it was added in v1.96.1 specifically for the horizontal-scroll pill strip, which no longer exists. **(H) Tests.** New `tests/unit/v197_chevron_dropdown.test.ts` (54 tests). v1.96.1 test file deleted (superseded — covered the now-removed pill-strip shape). Coverage: trigger button structure + testids + aria attrs + h-11 height + 180° chevron rotation + active-league inline label; dropdown bar conditional render (open ? bar : null) + role/aria/positioning/flex-wrap; outside-click + touchstart + Escape close + listener cleanup; rectangular pills (rounded-lg) + min-h-[44px] + 2 px border + primary glow on active + bg-surface on inactive + no-underline; spinner standardisation preserved (animate-spin, NOT animate-pulse, optimisticActiveId regression target); click handling (same-league short-circuit, power-user gestures, /test?league=<encoded> contract); Dashboard body skeleton (useHubTransition import path + isHubPending alias + aria-busy + conditional className + animation-conflict-free wrapper position); pill-scrollbar removed from globals.css + switcher source. Stash-pop verified 43 of 54 new tests fail on the v1.96.1 baseline. **(I) Hard constraints honored.** No schema change. No migration. UI + behavioral change only. State management is new but localised to `<LeagueSwitcherTabs>` (single `useState`, one `useEffect`, one `useRef`) — no parent layout change beyond the body wrapper inside Dashboard's existing `.animate-in pt-2`. Per the v1.95.1 auto-merge policy, this is self-mergeable: tests green, no destructive surface, no architectural ambiguity flagged.
- **v1.96.1** — four UI fixes to the multi-league hub `<LeagueSwitcherTabs>` (`/test`, mounted by v1.85.0 / v1.93.0). User feedback after the v1.93.0 perf rebuild: pills hugged the bottom of the fixed Header, the horizontal scroll fell back to the browser default scrollbar, the pills read like generic chips with no clear active state, and the in-flight feedback was a tiny pulsing dot inconsistent with the rest of the codebase. **(A) Top spacing.** Nav now carries `pt-2` (matches the Header → `.animate-in pt-2` → RecruitingBanner spacing on `/id/<slug>`); previously the nav had `mb-3` only and butted directly against the bottom of the fixed `h-12` Header inside `<main>`'s `pt-12`. **(B) Custom scrollbar.** New `.pill-scrollbar` utility in `globals.css` — `scrollbar-width: thin; scrollbar-color: var(--surface-md) transparent` + `::-webkit-scrollbar` 4 px height with rounded `var(--surface-md)` thumb, hover-darkens to `var(--fg-low)`. Tokens-only (no new CSS variables, no new colors). The pre-v1.96.1 nav applied a `no-scrollbar` class that **did not exist anywhere in the codebase** (`scrollbar-hide` is the actual hide-scrollbar utility — confirmed by grep), so the browser default scrollbar bled through on overflow — the headline "looks like browser default" report from the user. Nav also picks up `pb-1.5` so the new 4 px scrollbar doesn't visually clip the pills' bottom border. **(C) Visual treatment.** Pills swung from `h-9 px-4 text-[11px] rounded-full` (no border, identical-weight backgrounds) to `min-w-[44px] h-11 px-5 rounded-full border-2 text-xs` — 44 px touch target (WCAG AAA target size), explicit 2 px borders for depth. Active pill: `bg-primary text-primary-foreground border-primary shadow-[var(--glow-primary-md)]` (the existing pink glow shadow already used by the brand button — token-reused, no new design). Inactive pill: `bg-card text-fg-mid border-border-default`, hover `bg-surface-md text-fg-high border-primary/40` (subtle pink border tint on hover signals the pill is interactive). `transition-all duration-150` + `active:scale-[0.96]` retained. `no-underline` added defensively to neutralise the global `a { text-decoration: underline }` from the `@layer base` block (Link renders as `<a>`). **(D) Loading animation.** Codebase audit: the predominant in-flight pattern is `animate-spin` on a small ring (`RsvpBar.tsx:180` — `border-2 border-border-default border-t-fg-mid rounded-full animate-spin`; same shape used by `RsvpButton.tsx`, every admin editor under `src/components/admin/*`). `animate-pulse` is reserved for skeletons (Dashboard chunk-loading skeletons, etc.). Pre-v1.96.1 the switcher used `animate-pulse` on a 1.5 × 1.5 px dot — squarely inconsistent. New spinner: `w-3 h-3 border-2 border-current border-t-transparent rounded-full animate-spin` next to the active pill's text. **(E) v1.94.0 optimistic-pulse fix verified intact.** Pre-v1.85.0 the spinner fired on the OLD active pill because `selected = m.leagueId === activeLeagueId` ran against the not-yet-updated server-supplied id; v1.94.0 (PR #263) swung that to `optimisticActiveId` so the just-clicked pill shows the spinner. Confirmed unchanged: `const selected = m.leagueId === optimisticActiveId` + `const showSpinner = isPending && selected`. New regression-target test asserts the spinner is gated on optimistic id (not stale active id) so this can't silently regress. **(F) Tests.** 22 new tests in `tests/unit/v1961_league_switcher_ux.test.ts`: pill-scrollbar utility defined in globals.css with the four required CSS properties (scrollbar-width thin, surface-md thumb, transparent track, fg-low hover); nav uses the new utility (and NOT the old `no-scrollbar` non-class); top spacing pin (`pt-2` on the nav); 44 px touch target pin (`h-11` AND `min-w-[44px]`); 2 px border pin (`border-2`); active-pill glow + primary surface; inactive-pill border-default + hover; loading animation pin (`animate-spin` AND NOT `animate-pulse`); spinner ring shape pin (`border-current border-t-transparent`); v1.94.0 optimistic-id regression target (spinner gated on `optimisticActiveId`, not `activeLeagueId`); plus version + CLAUDE.md header pins. Stash-pop verified 19 of 22 fail on the v1.96.0 baseline. UI-only PR; no schema, no migration, no server-action surface change. Per the v1.95.1 auto-merge policy, this is self-mergeable: tests green, no architectural ambiguity, no destructive change.
- **v1.96.0** — admin-toggleable suppression of the unpaid-fee banner. **User report:** "is the payment banner coded to not show when preseason mode = off?" — audit confirmed the banner has NEVER been gated on `preseasonMode` (the v1.66.0 resolver `getUnpaidFeeBannerData` short-circuits on session/playerId/PLM/`paidStatus !== 'PAID'`/`fee > 0` only); the user's diagnosis was incorrect but the requested toggle is reasonable UX for casual leagues that don't want to bug players. **(A) Schema.** Additive `League.paymentBannerEnabled Boolean @default(true)` (default-true preserves every existing league's banner-on behavior; admins flip off per league). Migration `20260603000000_league_payment_banner_enabled` generated via `prisma migrate diff --from-schema-datamodel <pre> --to-schema-datamodel <post> --script` (offline, per `docs/migration-sql-lessons.md`); single `ALTER TABLE "League" ADD COLUMN "paymentBannerEnabled" BOOLEAN NOT NULL DEFAULT true;` — no DROP/TRUNCATE/DELETE FROM. **(B) Resolver gate.** `getUnpaidFeeBannerData` selects `paymentBannerEnabled: true` from the league row and short-circuits to `null` immediately after the existing `if (!league) return null` line. Player-level gates (paid status, fee resolution) unchanged — paid players still bypass the league fetch entirely (verified by runtime test `expect(leagueFindUniqueMock).not.toHaveBeenCalled()`). **(C) Admin UI.** New toggle in `LeagueDetailsEditor` between the unlimited-subs cluster and the ID-required toggle: label "Show payment reminder banner to unpaid players" / helper "When off, the unpaid-fee banner never appears for any player in this league." `data-testid="league-details-payment-banner-toggle"` + `data-testid="league-details-payment-banner-button"`. Admin-token styled (matches the v1.93.0 idRequired toggle). **(D) Server action.** `updateLeagueDetails` accepts optional `paymentBannerEnabled?: boolean`; rejects non-boolean with `'paymentBannerEnabled must be a boolean'`. `SettingsTab` widens its `League` prop type and threads `initialPaymentBannerEnabled={league.paymentBannerEnabled}` (`getLeagueSettings` already returns the full Prisma row so no select change needed). **(E) Tests.** 19 tests in `tests/unit/v196_payment_banner_toggle.test.ts` — version + CLAUDE.md header pins, schema column structural pin, migration additive-only pin (no DROP/TRUNCATE/DELETE FROM), resolver source pins (`select.paymentBannerEnabled`, short-circuit gate, regression-target that `preseasonMode` does NOT appear in the resolver), admin action validation pins, editor source pins (testids + label + state binding + save-call wiring), and 5 runtime regression tests covering the four toggle×paid-status quadrants plus a `select` shape pin. Brief audit findings: brief speculated banner was gated on preseason mode — false (v1.75.1 stripped a different `preseasonMode` gate from `LeagueDetailsPanel`, but the unpaid-fee banner has been preseason-orthogonal since v1.66.0 inception). Per the v1.95.1 auto-merge policy, this PR is self-mergeable: tests green, additive migration, no architectural ambiguity. **Rebase note:** v1.95.2 (PR #268) merged mid-flight (docs-only "report after merge" extension); CLAUDE.md ledger + version-pin conflicts resolved by keeping both entries in order (v1.96.0 above v1.95.2) and adopting v1.96.0 as the active release. Source-tree was conflict-free.
- **v1.95.2** — extend auto-merge policy with an explicit reporting requirement. After the v1.95.1 rule landed, self-merges could still end silently — the executor merged but never surfaced a final report (PR #, merge SHA, version, what shipped, post-deploy verification result), leaving the orchestrator and user without confirmation. New **"Always report back on merge"** paragraph added to the `## Auto-merge policy` section in `docs/release-and-ship.md`: mandates a final report after every self-merge, forbids silent exit after a clean merge, and covers the partial-deploy / timeout case ("merged at \<SHA\>, deploy still in progress" rather than going silent). One-line mention appended to the CLAUDE.md standing-rule entry. `tests/unit/v1951_auto_merge_policy.test.ts` regex updated (two occurrences): the `(?=\*\*[A-Z])` lookahead tightened to `(?=\n\n\*\*[A-Z])` so the new `**After every self-merge` inline-bold text no longer truncates the captured block. New regression-target test `tests/unit/v1952_merge_report_requirement.test.ts` (9 tests): pins "Always report back" heading, required fields (PR #, merge SHA, version, post-deploy verification), silent-exit prohibition, partial-deploy case, and CLAUDE.md mention. Stash-pop verified all 9 of 9 fail on the v1.95.1 baseline. Docs-only; no schema, no migration, no source-code changes outside tests. Self-merged per the policy it extends.
- **v1.95.1** — codify "Auto-merge policy" as a standing rule. After the v1.95.0 ship cycle, the operator observed the executor was pausing for explicit user confirmation before each `gh pr merge --squash --admin` even when (a) Unit + tsc were green, (b) post-push self-verification surfaced no issues, and (c) no architectural ambiguity was flagged — adding a round-trip that didn't gate anything. The new rule formalises the existing autonomy: when those three conditions hold, the executor SHOULD merge without waiting; surface for confirmation ONLY when ambiguity/risk was flagged (security trade-off, destructive migration, scope question, mid-execution stop-condition trigger). Symmetric to the v1.65.x plan-then-ship autonomy — the audit-grounded plan + green tests + clean self-review IS the merge gate. Two-file change: one-line summary in `CLAUDE.md` "Standing rules" placed adjacent to the existing **Plan-then-ship** rule (with a pointer to the full doc), plus a new **## Auto-merge policy** section in [`docs/release-and-ship.md`](release-and-ship.md) sitting between Plan-then-ship autonomy and the Backups & rollback runbook. New regression-target test `tests/unit/v1951_auto_merge_policy.test.ts` (10 tests) pins (a) the CLAUDE.md standing-rules entry, (b) the docs section anchor + three-condition formulation, (c) the four explicit confirmation-required cases, (d) APP_VERSION = 1.95.1, (e) section placement between Plan-then-ship and Backups, (f) the policy is conditional (no "unconditional auto-merge" / "bypass all checks" phrasing). Stash-pop verified all 10 of 10 fail on the v1.95.0 baseline. Docs-only; no schema, no migration, no source code changes. Per the new policy this PR itself was self-mergeable.
- **v1.95.0** — admin RSVP override section in the Add Guests modal (the v1.93.0 per-team modal). ADDITIVE admin capability layered on top of the user-facing modal — the existing guest-row paths are unchanged for all auth'd users. **(A) Schema.** Two additive nullable columns on `Availability`: `overriddenById String?` (FK → User, `onDelete: SetNull` so historical overrides survive admin User deletion) + `overriddenAt DateTime?` (distinct from `updatedAt`, which fires on every row touch). Back-relation `User.overriddenAvailabilities` (named relation `"AvailabilityOverriddenBy"`). **Lifecycle.** Admin override write sets both fields to (adminUserId, now()). User re-RSVP through `/api/rsvp` POST clears both back to `null` — the player taking ownership back means the row is no longer "currently overridden" in the audit sense. Migration `20260602000000_availability_admin_override_audit` generated via `prisma migrate diff --from-schema-datamodel <pre> --to-schema-datamodel <post> --script` (offline, per `docs/migration-sql-lessons.md`); purely additive — two ADD COLUMN + one ADD CONSTRAINT, no DROP/TRUNCATE/DELETE FROM anywhere. **(B) Server action.** `setMatchdayGuests` extended with optional `rsvpOverrides?: Array<{ playerPublicId, status: 'GOING' | 'UNDECIDED' | 'NOT_GOING' | null }>`. Admin gate fires ONLY when `rsvpOverrides` is present + non-empty — `if (!session.isAdmin) throw new Error('Unauthorized: RSVP overrides require admin')`. The guest CRUD branch above it stays admin-orthogonal (gates on `userId` OR `lineId` per v1.80.10 pattern). Per-override validation: `prisma.playerLeagueMembership.findMany({ where: { leagueId, leagueTeamId: leagueTeam.id, playerId: { in: dbIds } } })` rejects any player not rostered on this team in this league (no cross-team / cross-league bleed). Status validation rejects anything outside the four-state union. Override upserts run in the SAME `prisma.$transaction` as the matchdayGuest deleteMany/createMany so a partial-write surface is impossible. After the transaction commits, `setRsvp(gameWeek.id, gameWeek.startDate, slug, status)` propagates each override into the Redis-canonical RSVP store (silent variant — Redis drift falls through Prisma on next read per the v1.7.0 read-fallback). New `getAdminRosterRsvp({ leagueSlug, matchdayPublicId, teamPublicId })` server action — admin-gated (throws on non-admin), reads from Prisma `Availability` directly (not Redis — admin domain, staleness OK, AND Prisma is the only store that carries the audit fields), surfaces NOT_GOING which the public read path (`src/lib/rsvpMerge.ts`) deliberately collapses to null. Returns roster (active APPROVED non-retired memberships) sorted by name with `currentRsvp` + `overriddenById` + `overriddenAt` ISO string. **(C) /api/rsvp clear-audit-fields.** `persistRsvpToPrisma.update` now includes `overriddenById: null, overriddenAt: null` so every user-side RSVP write clears the override flag. No-op when the row was never overridden. `create` branch deliberately omits the fields (column default is null). **(D) Modal UI.** `AddGuestsModal` gains optional `isAdmin?: boolean` prop. When true, renders a new `<AdminRsvpOverrideSection>` below External Guests with `data-testid="admin-rsvp-override-section"`. The section calls `getAdminRosterRsvp` on open (gated `if (!open || !isAdmin) return`) and lists each roster row with three toggle buttons: **Going** (sets GOING) / **Not going** (sets NOT_GOING) / **Clear** (sets null). Display collapses UNDECIDED ↔ "—" — admins flip confirm/deny/clear; nuance is for the player. Changes batch via `overrides: Record<playerPublicId, AdminRsvpStatus>` — only diffs are submitted. Submit handler bundles `rsvpOverrides` into `setMatchdayGuests` ONLY when `isAdmin && Object.entries(overrides).length > 0` (avoids tripping the admin gate on a no-op admin save). New types `AdminRsvpStatus`, `RsvpOverrideInput`, `AdminRosterRsvpEntry`, `GetAdminRosterRsvpInput`, `GetAdminRosterRsvpResult` exported from `src/app/api/guests/actions.ts` (type-only — safe under the `'use server'` no-value-export rule). **(E) MatchdayAvailability threading.** `const isAdmin = Boolean(session?.isAdmin)` from `useSession()`, passed as `isAdmin={isAdmin}` into the dynamic-imported `<AddGuestsModal>`. No other user-facing path gates on admin. **(F) Admin-orthogonal-UX allowlist.** `tests/unit/v167_planned_roster.test.ts` allowlist extended with `src/app/api/guests/actions.ts` + `src/components/MatchdayAvailability.tsx` (the only two new isAdmin-referencing files); v1.93.0 test relaxed to allow isAdmin in the action source when scoped to the `rsvpOverrides` / `getAdminRosterRsvp` admin-only paths via a windowed-context check (no isAdmin allowed near the user-facing guest CRUD path). **(G) Tests.** 43 new tests in `tests/unit/v195_admin_rsvp_override.test.ts`. Stash-pop verified 38 of 43 fail on the v1.94.1 baseline (the 5 that still pass cover the new migration file, which is untracked and survives `git stash push` on tracked files only). Full suite: 3534 tests pass, 2 skipped, 0 failed. tsc clean. Rebased onto v1.94.2 (PR #265 metadata leak hotfix) without source-tree conflicts; CLAUDE.md ledger + version-pin conflicts resolved by keeping both ledger entries in order (v1.95.0 above v1.94.2) and adopting v1.95.0 as the active release.
- **v1.94.2** — hotfix for the v1.94.0 / v1.94.1 private-join `generateMetadata` league-existence leak. The page handler at `src/app/id/[slug]/join/page.tsx:79` correctly 404s when `League.privateJoinLinkEnabled === false` (Dashboard never mounts, no league data leaks). However, `generateMetadata` (lines 20-31) ran BEFORE that gate, so the response `<title>` for any *existing* slug whose owner had not enabled the link disclosed `Join <League Name> | <League Name>`. Bogus slugs returned the generic `Join | T9L` title — so an attacker could distinguish "league exists, link disabled" from "league does not exist" purely from the document title, turning slug-guessing into an existence enumerator (low-severity but real). **Fix.** `generateMetadata` now also selects `privateJoinLinkEnabled` and short-circuits to `{ title: 'Join | T9L' }` when the league row is missing OR the toggle is off. Byte-equal to the unknown-slug branch, so guessers can no longer distinguish the two states. The single-line gate change keeps the two existing returns (generic title) vs the third return (admin-opted-in disclosure: `Join <abbr> | <name>`) clean. **Out of scope (codebase-wide quirk surfaced in the v1.94.1 verification, NOT addressed here):** the page response itself comes back as HTTP 200 with the Next.js 404 body — the standard `/id/<slug>` page exhibits the same behavior for unknown slugs (verified during the v1.94.1 post-deploy: `curl -sI https://t9l.me/id/nonexistent-league` → 200). That's a separate, project-wide `notFound()` quirk worth a follow-up but not part of this hotfix. **Tests.** New `tests/unit/v1942_private_join_metadata.test.ts` (7 tests): two source-grep structural pins (the new `privateJoinLinkEnabled: true` select + the gate clause both anchor inside the metadata function block via a `split('export default async function')[0]` slice) plus five runtime assertions (disabled-league non-disclosure with literal name + abbrev non-containment regression target; byte-equal "exists+disabled" vs "missing" indistinguishability; missing-league-row race; enabled-league disclosure of `Join TL25 | Test League 2025`; abbreviation-null fallback to full name). Mocks `@/lib/leagueSlugServer.getLeagueIdBySlug` + `prisma.league.findUnique` via `vi.hoisted` + `vi.mock`, mirrors the existing v1.85.0 / v1.80.10 server-action runtime-test patterns. No schema change. No migration. Single-file source diff: 7 lines added, 2 lines changed. Identified during the v1.94.1 post-deploy verification (check #11 caveat); no other v1.94.x PR collides with this surface so the rebase was empty (my v1.94.0 squash-merge was correctly skipped).
- **v1.94.1** — onboarding-flow tweaks bundled with a new private-join-link route: (A) per-league `idRequired` toggle, (B) preferred + secondary positions on the onboarding form with a max-3 cap on preferred across all surfaces, (C) admin-toggleable `/id/<slug>/join` route that forces the recruiting banner on regardless of `League.visibility`. **(A) Skip-ID.** New `League.idRequired Boolean @default(true)` column — default-true preserves every existing league's UX (ID still required). Admin UI: new "Require ID document on onboarding" toggle in `<LeagueDetailsEditor>` (placed alongside the offside / unlimited-subs / show-on-homepage cluster); `updateLeagueDetails` accepts `idRequired?: boolean`. `RegistrationFields` gains an `idRequired?: boolean` prop (default `true`); when false: ID front/back fields + "Why we need your ID" callout are hidden, the submit-gate drops the required-ID predicate, and the form submits empty `idFrontUrl`/`idBackUrl` strings. Server-side gate is the canonical word — `registerToLeague` + `completeOnboardingWithId` re-fetch `league.idRequired` (NOT a form-supplied flag) and conditionally skip the `isOwnedBlobUrl` checks + the `User.idFrontUrl/idBackUrl/idUploadedAt` writes when false. **(B) Preferred/secondary on onboarding + 3-cap.** `MAX_PREFERRED_POSITIONS = 3` exported from `src/lib/positions.ts`. `validatePreferredSecondary` now rejects `preferred.length > 3` with `"Preferred positions: pick at most 3."`. `RegistrationFields` split single `positions[]` → preferred (required, capped) + secondary (uncapped, disjoint). `RegistrationFieldsSubmit` shape replaces `positions: string[]` with `preferredPositions: string[]` + `secondaryPositions: string[]`. `PositionMultiSelect` gains an optional `maxSelected?: number` prop — additional toggles on unselected chips at cap are no-op and the chips render `disabled` (visible at-cap signal). Cap applied to all picker surfaces: onboarding form, account-settings per-league card, admin EditPlayerPanel (split into preferred + secondary pickers), AddPlayerDialog (also split), ApplyToLeagueModal (also split). Server actions accept either the new `preferredPositions`/`secondaryPositions` shape or the legacy `positions` (treated as preferred); both funnel through `validatePreferredSecondary` so the cap is enforced server-side regardless of caller version. `adminUpdatePlayerPosition` / `adminCreatePlayer` / `applyToLeague` / `registerToLeague` / `completeOnboardingWithId` / `updatePlayerLeague` all migrated. PlayerLeagueMembership writes: `positions[]` now mirrors preferred; `preferredPositions` + `secondaryPositions` populated independently. UI requires ≥1 preferred at the form level; server allows 0 (cap is upper-bound only). **(C) Private join link.** New `League.privateJoinLinkEnabled Boolean @default(false)` column. Admin UI: "Enable Private Join Link" checkbox in `<LeagueDetailsEditor>`; when on, displays the resulting `t9l.me/id/<slug>/join` URL with a copy-to-clipboard button. New server-component route `src/app/id/[slug]/join/page.tsx`: 404 when `privateJoinLinkEnabled === false`; otherwise renders the standard league page with `forceRecruitingBanner` so the recruitment banner mounts regardless of `League.visibility` (PRIVATE / PUBLIC_CLOSED / PUBLIC_OPEN all surface it). Subtle "Private join link" indicator above the banner so admins know the URL is the privileged path. `Dashboard` / `RecruitingBanner` accept an optional `forceRecruitingBanner` prop that bypasses the `visibility === 'PUBLIC_OPEN'` gate; the `RecruitingBanner` itself ALSO loosens its internal `recruiting !== false` short-circuit when the force flag is set so PRIVATE leagues still mount the banner. `applyToLeague` server action unchanged: PRIVATE leagues still return "invitation-only" via the modal (the join page can show the banner but PRIVATE applies still need an invite — admins should pair the toggle with PUBLIC_CLOSED for self-serve apply). **Security note (surface for operator).** Slug-based URL means anyone who guesses the slug + tries `/join` on a league with the toggle on gets the recruitment page. For PRIVATE leagues this is a real exposure. Alternative not built this PR: add `League.privateJoinToken String?` (cuid) and require `/id/<slug>/join/<token>`; toggle on regenerates, toggle off clears. Stronger but admin must copy the full URL with token. Implemented slug-based per the literal spec; operator can request the token-based variant in a follow-up. **Migrations.** Two: `20260601000000_league_id_required` (`ALTER TABLE "League" ADD COLUMN "idRequired" BOOLEAN NOT NULL DEFAULT true`) and `20260601000001_league_private_join_link_enabled` (`ALTER TABLE "League" ADD COLUMN "privateJoinLinkEnabled" BOOLEAN NOT NULL DEFAULT false`). Both generated via `prisma migrate diff --from-schema-datamodel <pre> --to-schema-datamodel <post> --script` (offline; `prisma migrate dev --create-only` would have been the canonical path but requires DATABASE_URL the worktree doesn't have — diff-script is the documented fallback per `docs/migration-sql-lessons.md`). **Tests.** New `tests/unit/v194_onboarding_skip_id_and_preferred_cap.test.ts` (41 tests, originally written as v193_*) and `tests/unit/v194_private_join_link.test.ts` (private-join schema + admin UI + route presence). Pre-existing tests updated: `playersTabEditMode.test.ts` (preferred/secondary picker testids + validatePreferredSecondary call); `playersTabAddInvite.test.ts` (split-picker testids); `v164_application_workflow.test.ts` (ApplyToLeagueModal split); `v168_id_collection_streamlined.test.ts` (RegistrationFields split + `idRequired &&` gate); `v178_email_field.test.ts` (registration-preferred ordering); `v18010_admin_orthogonal_apply.test.ts` (widened slice — function body grew); `v183_multi_league_account_player.test.ts` (validatePreferredSecondary in updatePlayerLeague). **Brief audit findings (M1 — phase log):** brief claimed `League.idRequired` was added in v1.81.0 (false — v1.81.0 was the success-popup feature). Brief claimed positions don't ship in the onboarding form today (false — single-array `positions[]` was already there; v1.86.0 only added the preferred/secondary split to `/account/player`). Added the additive `idRequired` column anyway since the brief's behavioural goal cannot be reached without it. Original ship target was v1.93.0; collided with the in-flight Guest rework (#261) that landed first. Re-targeted to v1.94.0; collided again with the league-switcher rebuild (#263) that landed v1.94.0 first. Re-rebased onto v1.94.0 and re-targeted to v1.94.1. Both rebases were CLAUDE.md-only conflicts (ledger entry + version-pin); no source-tree overlap.
- **v1.94.0** — multi-league hub league switcher: replace server-action + `router.refresh()` with `<Link prefetch>` + searchParam-driven active league. v1.92.x user feedback was "no animation, no loading, takes too long" — every click sat on three sequential Neon round-trips (`setUserDefaultLeague` → `user.findUnique` → `player.findFirst+memberships` → `user.update`) before navigation began, with no visual cue. Profiled cold/warm: ~150–300 ms server action + ~220–300 ms RSC re-fetch + ~30–100 ms paint = ~400–700 ms warm / ~900–1500 ms cold. **(A) Architecture swap.** `<LeagueSwitcherTabs>` pills become `<Link prefetch href="/test?league=<id>">`; Next.js prefetches the destination RSC payload on hover/visibility so warm clicks land inside the route cache window. `<HomepageRouter>` accepts `preferredLeagueId?: string | null` and forwards it into `resolveHomepagePersona`; `classifyPersona` validates the id against the viewer's APPROVED memberships before honouring it (priority: searchParam → `User.defaultLeagueId` → alphabetical-first). Unknown / stale / array / empty-string values silently fall through — a malicious `?league=` param can never pin a viewer to a league they aren't in. `/test/page.tsx` reads `searchParams.league`, normalises to a single string, and forwards. **(B) Persisted "last selection" via `touchUserDefaultLeague`.** `<MultiLeagueHub>` now fires the existing `waitUntil`-wrapped helper (same `/id/<slug>` pattern) for the active league on every render — short-circuits when `defaultLeagueId === activeLeagueId`. The 3-query `setUserDefaultLeague` server action + entire `src/components/homepage/actions.ts` module are deleted; `<HomepageRouter>` gains a `viewer={{ userId, lineId }}` prop so `<MultiLeagueHub>` can call the helper without a second `getServerSession` round-trip. **(C) UX feedback.** New `<HubTransitionShell>` client wrapper owns a single `useTransition`, renders a 2 px top-edge progress strip (`.animate-hub-progress` keyframe in `globals.css`, `z-[60]` to clear the sticky header) while pending, and exposes `useHubTransition()` via React context so the switcher dispatches into the same transition (no duplicate `useTransition` in the switcher — single owner keeps the strip in sync). Switcher uses React 19 `useOptimistic(activeLeagueId)` + `setOptimisticActiveId(leagueId)` inside `startNavigation` so the tapped pill jumps to active styling IMMEDIATELY (pre-v1.94.0 the `animate-pulse` fired on the OLD active pill — verified UX bug because `selected` was computed from server-supplied `activeLeagueId` which only updates after refresh). New per-pill spinner dot only shows when `isPending && selected (via optimistic id)` — i.e. the just-clicked pill, not the previously-active one. `active:scale-[0.96]` for tactile press (CSS-only). Power-user gestures preserved (`metaKey` / `ctrlKey` / `shiftKey` / `altKey` / `e.button !== 0` skip the preventDefault so cmd-click new tab still works). Same-league taps short-circuit (no needless RSC re-fetch). Rebase note: cleanly merged with v1.93.0 (PR #261 Guest rework) — `MultiLeagueHub` Dashboard prop wiring picked up the renamed `guests={bundle.data.guests}` (was `guestCounts`); rest of the v1.93.0 hub structure is independent of the switcher rework. **Expected payoff:** click → first paint collapses from ~400–700 ms warm to ~50 ms (URL change + reconcile, RSC payload prefetched on hover); cold from ~900–1500 ms to ~250–400 ms (just the bundle fetch, no awaited action). 35 new regression-target tests in `tests/unit/v193_league_picker_perf.test.ts` (security gate for `?league=` tampering, optimistic active swap pin, `setUserDefaultLeague` complete-removal pin, `router.refresh()` removed pin via comment-stripper helper, full call-shape pin for `touchUserDefaultLeague`, top-strip + keyframe pins). Stash-pop verified: stashing src/ leaves the new test file failing at type-check on the missing `preferredLeagueId` field (5 errors), proving the new tests are real regression targets. 11 v1.85.0 assertions retired/relaxed: switcher action-call pins gone (action deleted), `resolveHomepagePersona` call-shape regex relaxed for the new third arg, `passes both userId AND lineId` allows trailing fields, narrative comment updated to point at the new file. **Out of scope (kept as-is):** `getLeaguePageBundle`'s 4-sequential pattern inside `dbToPublicLeagueData` (already cached by `unstable_cache(... revalidate: 300)`); adding the switcher to `/id/<slug>` pages (separate UX call); promoting the `/test` apex to `/`. See [phase-league-picker-perf.log](../phase-league-picker-perf.log) for the full M1 measurements + design rationale.
- **v1.93.0** — Guest feature rework on the player-availability widget: per-row + typed + positions. Replaces the v1.91.0 count-only `MatchdayGuestEntry` (single row per `(gameWeekId, leagueTeamId)` with `externalCount` + `leagueCount` integers) with per-guest rows on a new `MatchdayGuest` model. **(A) Schema.** New `GuestType` enum (`EXTERNAL` | `LEAGUE`) + `MatchdayGuest` model: `id` cuid, `gameWeekId` (FK → GameWeek ON DELETE CASCADE), `leagueTeamId` (FK → LeagueTeam ON DELETE CASCADE), `type GuestType`, `positions String[] @default([])` (per-guest preferred positions validated against league `ballType` vocab at write), `displayOrder Int @default(0)` (0..N-1 per (matchday, team, type) section for stable label numbering), `createdById String?` (FK → User ON DELETE SET NULL, audit trail), `createdAt`/`updatedAt`. Indexes: compound `@@index([gameWeekId, leagueTeamId, type])` + single-leg `@@index([gameWeekId])` + `@@index([leagueTeamId])` for cascade scans. NO `@@unique` — multiple rows per `(gameWeekId, leagueTeamId, type)` by design. Back-relations renamed: `User.authoredGuestEntries → authoredGuests`, `GameWeek.guestEntries → guests`, `LeagueTeam.guestEntries → guests`. Migration `20260522000000_add_matchday_guest_rows_replace_counts` generated via `prisma migrate diff --from-schema-datamodel <pre> --to-schema-datamodel <post> --script` (offline; per `docs/migration-sql-lessons.md`). Statement order REORDERED from Prisma's natural `DROP OLD → CREATE NEW` to `CREATE NEW → INSERT BACKFILL → DROP OLD` so the backfill (each `MatchdayGuestEntry.externalCount + leagueCount` expanded via `CROSS JOIN LATERAL generate_series(1, e."externalCount")` into per-type rows with `'EXTERNAL'::"GuestType"` / `'LEAGUE'::"GuestType"`) runs atomically inside the same migration. Prod has 0 rows in `MatchdayGuestEntry` at v1.92.0 deploy time, so backfill is a no-op on prod — kept for non-prod env safety + replayability. DDL statements byte-for-byte unchanged from `prisma migrate diff` emit; only INSERT statements + statement ordering are manual additions (permitted exception per migration-sql-lessons.md). **(B) Server action.** `setMatchdayGuestEntry` GONE; replaced by `setMatchdayGuests({ leagueSlug, matchdayPublicId, teamPublicId, guests: { type, positions }[] })` in `src/app/api/guests/actions.ts`. Replacement-by-set semantics: `prisma.$transaction([deleteMany({gameWeekId, leagueTeamId}), createMany(rows)])` — submitted list FULLY replaces existing rows for (matchday, team). Per-row `positions[]` validated via `normalizePositions(raw, league.ballType)`; cross-format codes throw. `displayOrder` is server-re-derived per type section (`externalCounter` + `leagueCounter` independent), so a modal "delete row 2" path never leaves persisted gaps. Auth + admin-orthogonal-UX pattern unchanged: gates on `userId` OR `lineId`, never on `session.isAdmin`. Cap: 50 guests per team via `MAX_GUESTS_PER_TEAM`. **(C) Modal UI.** `AddGuestsModal` rebuilt as a sectioned table. Title reads "Guests for <Team Name>". Two `<GuestSection>` components rendered in fixed order: **League Guests** above **External Guests** (matches modal-row presentation; data-side `dbToPublicLeagueData` orders EXTERNAL-first via `orderBy: [{type: 'asc'}, {displayOrder: 'asc'}]`). Each section is a list of rows; each row carries (i) a row-label ("League Guest N" or "Ext Guest N" via `index + 1`), (ii) a per-row `<PositionMultiSelect>` (chip-based, per league `ballType` vocab), (iii) a per-row trash-icon delete (`onRemove(key)`). "+ Add row" button per section adds an empty row at the bottom. Empty section shows "No league guests yet." / "No external guests yet." placeholder. Submit re-orders LEAGUE-first then EXTERNAL and sends to `setMatchdayGuests`; server re-stamps `displayOrder`. **(D) Pitch view.** `synthesizeGuestPlayers(teamId, guests: MatchdayGuestEntry[])` (new signature; was `(teamId, count: number)`). Each `MatchdayGuest` row becomes one pseudo-Player with `id = 'guest-' + sourceRow.id` (NEW prefix `guest-`; replaces v1.91.0 `guest-pseudo-<team>-<n>`), `name = 'Ext Guest N'` or `'League Guest N'` (1-indexed via `displayOrder + 1` per type), `preferredPositions = sourceRow.positions`, `position = positions.join('/')` for legacy reader compat. Pseudo-players with positions flow through the 6-pass formation algorithm passes 1a/1b/2a/2b like real players (so a `LEAGUE` guest with `positions=[CB]` lands at a CB slot, not just any back-most non-GK slot). Guests with empty `positions[]` still fall through to v1.89.1 pass 2.5 (back-most non-GK fill; GK hard-excluded). **(E) List view.** Single `GUEST` bucket SPLIT into two sub-buckets: `LEAGUE_GUEST` (rendered first per the brief) and `EXTERNAL_GUEST` (second). Bucket order: `GK → DF → MF → FW → UNB → LEAGUE_GUEST → EXTERNAL_GUEST`. Guest pills now render their joined-position string (or "Any" for empty positions) instead of the v1.91.0 hardcoded "GUEST" label. Within each guest bucket, players sort with `numeric: true` so "Ext Guest 10" comes after "Ext Guest 2" (not lex-sorted). Guest type derivation from synthesised name prefix ("League Guest" / "Ext Guest") — `synthesizeGuestPlayers` is the only writer of guest pseudo-Players so the convention is authoritative. **(F) Type changes.** `MatchdayGuestCounts` → `MatchdayGuests` (interface rename); new `MatchdayGuestEntry` interface for per-row payload (`id, type, positions, displayOrder`); `LeagueData.guestCounts → guests`. All four call-sites updated (`Dashboard`, `ClassicLeagueHomepage`, `MultiLeagueHub`, three page.tsx routes). **(G) Tests.** 72 new tests in `tests/unit/v193_guest_rework.test.ts`. v1.91.0 test file deleted (superseded). 3 helper-call edits in `tests/unit/v192_availability_list_view.test.ts` for the new synth signature + bucket-name split + version-pin softened to "1.9x.x or higher". `tests/unit/dbToPublicLeagueDataEvents.test.ts` mock entry swung from `matchdayGuestEntry` to `matchdayGuest`. Rebase note: cleanly rebased onto v1.92.2 (PR #262 read-side priority chain in `dbToPublicLeagueData.ts`) — guest read path is independent of the avatar priority chain, so both edits sit in adjacent IIFEs in the same file without semantic overlap. Stash-pop verified **45 of 72** new tests fail on the v1.92.0 baseline. Full suite: 3388 tests, 2 skipped, 0 failed.
- **v1.92.2** — profile-picture pill fix for LINE-auth users. v1.92.0 added a `User.image`-sourced avatar to the MatchdayAvailability list-view pill, but the bug-report user (Stefan, and ~33 of 37 LINE-auth Users in prod) saw initials instead. Root cause: the LINE sign-in callback in `src/lib/auth.ts` writes `LineLogin.pictureUrl` via `trackLineLogin` but **never** writes `User.image` — the PrismaAdapter creates the User row with `image=null` on first LINE login (LINE picture is outside NextAuth's canonical User shape) and pre-v1.92.2 nothing ever updated it. Players linked to LINE-only Users got `image=null` from the read path. Two-part fix: **R1 — read-side priority chain** in `src/lib/dbToPublicLeagueData.ts`. `Player.image` now resolves via `User.image ?? Player.profilePictureUrl ?? Player.pictureUrl ?? LineLogin.pictureUrl ?? null`, mirroring the `pickPlayerAvatarUrl` / `sessionPictureUrl` pattern already used elsewhere. Adds one extra `prisma.lineLogin.findMany({ where: { lineId: { in: <relevantIds> } }, select: { lineId: true, pictureUrl: true } })` round-trip — gated on `lineIdsForLookup.length > 0`, so non-LINE-having leagues pay zero cost. `User.findMany` select expanded with `lineId: true` to drive the LineLogin lookup. Map-merged on the server, IIFE-wrapped in the public-data emit. **R2 — write-side root cause** in `src/lib/auth.ts`. New private helper `syncUserImage(userId, pictureUrl)` calls `prisma.user.updateMany({ where: { id, NOT: { image: pictureUrl } }, data: { image: pictureUrl } })` — idempotent (`NOT` clause short-circuits redundant writes), no-op when `pictureUrl` is null/empty (mirrors trackLineLogin's "never overwrite with null"), non-fatal on error (same `console.error` pattern as `syncUserLineId`). Wired into the LINE branch of the JWT callback right after `syncUserLineId`. Every future LINE login refreshes `User.image`, organically growing coverage. R1 covers Stefan + 5 other LINE users whose `LineLogin.pictureUrl` is already stored (the 6 of 39 LineLogin rows with pictureUrl); R2 covers everyone else on their next LINE login. Skipped the google/email JWT branch — the PrismaAdapter already writes `User.image` from those OAuth profiles. No schema change. 18 new regression tests in `tests/unit/v1922_image_priority_chain.test.ts` (7 runtime priority-chain fixtures including the Stefan rescue case + 11 source-level pins on R1 + R2). Two pre-v1.92.2 pin updates in `tests/unit/v192_availability_list_view.test.ts`: APP_VERSION + CLAUDE.md header now expect v1.92.2; the `image: pla.player.userId` regex relaxed to match the IIFE-wrapped chain.
- **v1.92.1** — broadened `.claude/settings.json` allowlist to reduce permission prompts during spawned-session workflows. Added: `Bash(git push *--force-with-lease*)` (promoted from gitignored `settings.local.json` to the committed file so all worktree spawns inherit it); `Bash(psql:*)` (psql was absent from allow despite existing deny guards for DROP/TRUNCATE/DELETE FROM); `Bash(gh release:*)` (release tagging in the ship workflow); `Bash(sort:*)`, `Bash(uniq:*)`, `Bash(tr:*)`, `Bash(cut:*)`, `Bash(xargs:*)`, `Bash(which:*)` (common shell text-processing utilities). No deny rules changed.
- **v1.92.0** — PlayerAvailability list-view refresh: score-based bucketing + profile-picture pill. **(A) Score-based bucketing.** New pure helper `getPositionBucketByScore(positions)` in `src/lib/positions.ts`: averages a forward-score across `preferredPositions[]` (soccer GK=0/LB-CB-RB=1/DM=2/CM-LM-RM=3/CAM-LW-RW=4/ST=5; futsal GK=0/FIXO=1/ALA=3/PIVOT=5). Rule: any GK → 'GK' (short-circuit); else avg <1.5 → 'DF', [1.5, 3.5) → 'MF', ≥3.5 → 'FW'; empty/all-unknown → null (caller surfaces as 'UNB' / "Other"). Worked examples (per spec): `[CB,CM,ST]`→3.0→MF (boundary inclusive lower), `[CAM,ST,LW]`→4.33→FW, `[CB,DM]`→1.5→MF, `[GK,CB]`→GK, `[ST]`→FW. `MatchdayAvailability.bucketConfirmedPlayers` swung from `positions[0]` over the joined `position` string to the new helper over `preferredPositions[]` (falls back to splitting `position` for legacy memberships not re-saved since v1.86.0). New `'UNB'` list bucket between FW and GUEST for players with no recorded preferred positions — replaces the previous silent "drops into MF" path. `BUCKET_ORDER`/`BUCKET_LABEL`/`BUCKET_DOT` extended. Old `getPositionBucket(code)` helper kept verbatim for non-list callers (SquadList colours, `groupedPositionLabel`, `positionPillColor`). **(B) Profile picture in player pill.** List-view pill only — pitch view unchanged. Source: NextAuth `User.image` (Google avatar / LINE picture), threaded onto `Player.image?: string | null` in `src/types/index.ts` and populated by `src/lib/dbToPublicLeagueData.ts` via `player: { include: { user: { select: { image: true } } } }` on the PLM query. Fallback when `image` is null: initials avatar — first letter of first name + first letter of last name (single-token names use one letter), `bg-fg-mid/20 text-fg-mid` circle. New helpers `playerInitials(name)` + `<PlayerPillAvatar>` colocated in `MatchdayAvailability.tsx` (private React tree, 22px circular, `inline-flex align-middle`). Pill layout flipped to `inline-flex items-center gap-1.5` so `[avatar] [position pill] [name]` reads cleanly. Guests stay as plain "Guest #N" pills with NO avatar (no auth-linked User; the synthesised guest pseudo-Players have `image: undefined`). 27 new regression tests in `tests/unit/v192_availability_list_view.test.ts` covering all five worked bucket examples + initials variants + null/missing-image fallback + guest no-avatar invariant.
- **v1.91.0** — Add Guests feature on the player-availability widget. Each team header gets an `+ Guests` trigger (visible only to authenticated users in a league context); click → modal with two integer inputs (`External guests` for non-T9L people + `League guests` for T9L users not on this team's roster); submit upserts a single `MatchdayGuestEntry` row per `(gameWeekId, leagueTeamId)`. Effects: (a) team's "going" / "played" count includes the guest total; (b) pitch view synthesises `Guest #1..#N` positionless pseudo-Players that pass 2.5 (v1.89.1) places into back-most non-GK slots — GK strictly excluded; (c) list view gains a fifth `GUEST` bucket beneath GK/DF/MF/FW with neutral grey styling (`bg-fg-low` dot, `bg-surface-md text-fg-mid` pill). Schema: new `MatchdayGuestEntry` model — `id` cuid PK, `gameWeekId` (FK → GameWeek, ON DELETE CASCADE), `leagueTeamId` (FK → LeagueTeam, ON DELETE CASCADE), `externalCount Int @default(0)`, `leagueCount Int @default(0)`, `createdById String?` (FK → User, ON DELETE SET NULL — audit trail), `createdAt`, `updatedAt`, `@@unique([gameWeekId, leagueTeamId])`. Migration `20260521000000_add_matchday_guest_entries` generated via `prisma migrate diff --from-schema-datamodel <pre> --to-schema-datamodel <post> --script` (offline; `--create-only` couldn't run without a non-prod DATABASE_URL). Server action `setMatchdayGuestEntry({ leagueSlug, matchdayPublicId, teamPublicId, externalCount, leagueCount })` in `src/app/api/guests/actions.ts`: gates on auth state (accepts `userId` OR `lineId` per the v1.80.10 / v1.59.1 grandfathered-LINE pattern) — does NOT gate on `session.isAdmin` per the v1.67.0 admin-orthogonal-UX rule; validates counts as non-negative integers ≤ 50; resolves leagueId via `getLeagueIdBySlug`, gameWeekId via `weekNumber` parsed from `md<n>`, leagueTeamId scoped to `(leagueId, teamId)` (no cross-league bleed); upserts; revalidates `domain: 'public'`. Pure helper `synthesizeGuestPlayers(teamId, count)` + `isGuestPseudoId` in `src/lib/guestSynthesis.ts` (id prefix `guest-pseudo-`); `bucketConfirmedPlayers` extended with the `GUEST` bucket; `FormationPitch.playersWithoutPositions` filter excludes guest pseudo-IDs so the "fill in your profile" hint doesn't fire on synthetic guests. UI threading: `data.guestCounts` from `dbToPublicLeagueData` (one extra Prisma round-trip indexed by `gameWeekId` per league) flows page.tsx → Dashboard → ClassicLeagueHomepage → MatchdayAvailability; `AddGuestsModal` lazy-loaded via `next/dynamic` (chunk fetched only on first click, mirrors the v1.80.8 auth-modal pattern). Toggle button refactored from `<button>` to `<div role="button" tabIndex={0} onKeyDown=...>` so the inner `+ Guests` real-button can stop-propagation cleanly without nesting `<button>` (HTML invalid). 53 new regression tests in `tests/unit/v191_add_guests.test.ts`. Existing test `tests/unit/dbToPublicLeagueDataEvents.test.ts` updated with a `matchdayGuestEntry: { findMany: vi.fn().mockResolvedValue([]) }` mock entry to keep its 7 assertions green against the new fetch.
- **v1.90.0** — step 2 of legacy `Goal` / `Assist` table cleanup: drop the orphaned tables. Step 1 (PR #255 / v1.89.0) already removed every writer + reader, leaving the tables dead in the DB but still in the schema. This PR removes `model Goal` + `model Assist` from `prisma/schema.prisma` and the four back-relation fields that pointed at them (`LeagueTeam.goals`, `Player.goals`, `Player.assists`, `Match.goals`, `Match.assists`). Migration `20260520000000_drop_legacy_goal_assist` generated via `prisma migrate diff --from-schema-datamodel <pre> --to-schema-datamodel prisma/schema.prisma --script` (offline; no DB connection needed) per the v1.86.1 post-mortem rule (`docs/migration-sql-lessons.md`) — never hand-author migration SQL. Generated SQL is exactly: 6 `ALTER TABLE … DROP CONSTRAINT` (3 FKs each on Goal + Assist), then `DROP TABLE "Goal"` + `DROP TABLE "Assist"`. NO other table touched. **Acknowledged orphan rows that vanish with the tables:** the `wk1-mariners-fc-vs-fenix-fc-7` Goal row + the 4 mirror Goal rows + 3 mirror Assist rows that pointed at the legacy `p-guest` Player (Sheets-import backfill remnants) — operator confirmed (option 2 from cleanup brief: keep the recomputed score 2-2 from MatchEvent, not from these legacy rows). 11 new regression tests in `tests/unit/v190_legacy_goal_assist_tables_dropped.test.ts`: schema has no `model Goal` / `model Assist` / `Goal[]` / `Assist[]` field declarations; migration directory + SQL exist; SQL drops both tables + their 6 FKs; no `ALTER TABLE`/`DROP TABLE` statement targets any table other than Goal or Assist (catches v1.86.1-style accidents).
- **v1.89.1** — extend `assignPlayersToFormation` from 5-pass to 6-pass: new **pass 2.5** places empty-positions players (both `preferredPositions=[]` AND `secondaryPositions=[]`) into back-most non-GK empty slots before falling through to subs. Slot fill priority via new private `emptySlotFillPriority(slotCode)` helper in `src/lib/formations.ts`: Soccer `CB > LB/RB > DM > CM > CAM > LM/RM > LW/ST/RW`; Futsal `FIXO > ALA > PIVOT`. **Hard rule:** GK is never auto-filled by an empty-positions player — slot stays empty if no GK candidate. Tie-break among same-priority slots is slot-array index (deterministic). Iteration order over empty-positions players is input order. Empty-positions players that pass 2.5 can't fit (e.g. only GK slot remains, or fewer non-GK slots than empty players) overflow into `unassignedPlayers` (subs) alongside positioned-but-displaced players — the `unassignedPlayers` and `playersWithoutPositions` buckets may now overlap (the latter remains an informational flag for "no positions on file"). The legacy v1.83.0 regression test `players with empty positions go to playersWithoutPositions, not the slot pool` was rewritten to assert the new placement (CB-first in 4-3-3); previously it pinned the now-superseded "never placed" behavior. 15 new tests in `tests/unit/v1891_empty_positions_pass.test.ts`; stash-pop verified 14/15 fail on the v1.88.0 baseline.
- **v1.89.0** — step 1 of legacy `Goal` / `Assist` table cleanup: remove every remaining writer + dead reader from src/. Pre-v1.89.0 the only live writer was `addGoal` / `deleteGoal` in `src/app/admin/actions.ts` (called from the legacy `/admin/matches/[id]` edit page); admin event CRUD has lived on `MatchEvent` via `adminCreateMatchEvent` / `adminUpdateMatchEvent` / `adminDeleteMatchEvent` in `src/app/admin/leagues/actions.ts` since v1.43.0 (PR γ), and player self-report goes through `submitOwnMatchEvent` in `src/app/matchday/[id]/actions.ts`. The match-score recompute is automatic via `recomputeMatchScore()` after every MatchEvent write — the legacy manual `updateMatchScore` action was redundant. **Decision:** delete the entire `/admin/matches/*` route (both `page.tsx` files) plus the sidebar link rather than convert — the pages exclusively read the legacy `Goal` relation that drops in step 2, and admin event editing has fully moved to `/admin/leagues/[id]/stats`. **Removed from src/:** `addGoal` + `deleteGoal` + `updateMatchScore` server actions; `getMatch` + `getMatchesWithGoals` readers in `src/lib/admin-data.ts`; `prisma.goal.findMany` from `getLeagueStats` (the dead `void goals` consumer in `StatsTab.tsx:299` confirmed it was orphaned); the `goals: GoalRow[]` prop + `GoalRow` interface + `buildScorerStats` legacy helper from `StatsTab.tsx`; the "Matches" link from `AdminSidebar.tsx`; the `/admin/matches/${matchId}` revalidate paths from all three MatchEvent CRUD actions in `admin/leagues/actions.ts`. **No schema migration in this PR** — the `Goal` and `Assist` tables stay (orphan row `wk1-mariners-fc-vs-fenix-fc-7` is being dropped with the table in step 2). 10 new regression tests in `tests/unit/v189_legacy_goal_writers_removed.test.ts` walk every `.ts`/`.tsx` under `src/` (comments + string literals stripped) and assert: zero `prisma.{goal,assist}.{create,update,delete,deleteMany,upsert,createMany,updateMany}` callsites; no `addGoal`/`deleteGoal`/`updateMatchScore` references; `/admin/matches` directory + sidebar link both gone; `getLeagueStats` no longer hits `prisma.goal.findMany`; StatsTab no longer carries `void goals` or `GoalRow[]` prop; `admin/leagues/actions.ts` no longer revalidates `/admin/matches/${id}`. Stash-pop verified 6/10 fail on the pre-PR state. One existing test updated (`tests/unit/adminMatchEventActions.test.ts:172` — drop the `/admin/matches/${MATCH}` expectation from the revalidate-paths assertion + add a `not.toContain` regression-target).
- **v1.88.0** — refactor "Guest" from a per-team pseudo-Player on the roster to an event-level concept on `MatchEvent`. Pre-v1.88.0 each team had a seeded `p-guest-<lt-id>` Player + `PlayerLeagueAssignment` (4 in prod from the v1.46.1 Sheets backfill, plus a legacy single `p-guest`); they bloated rosters by 1 + showed 4 "Guest" entries in scorer/assister dropdowns. Schema: `MatchEvent.scorerId String?` (was `String`), new `MatchEvent.isGuestScorer Boolean @default(false)` and `MatchEvent.isGuestAssister Boolean @default(false)` flags. Two flags (not one `isGuest`) so a real-scorer-with-guest-assist event isn't ambiguous against the existing `assisterId IS NULL` "no assist" semantic. `goalType=OWN_GOAL` composes naturally — a guest scoring an OG is `isGuestScorer=true + goalType=OWN_GOAL`, recompute trusts the explicit `beneficiaryTeamId` (already opposing-team for OG per write-side). Migration `20260519000000_match_event_guest_flags`: Prisma-generated schema deltas + UPDATE backfills (sets `isGuestScorer=true` + nulls `scorerId` for any event whose scorer is a guest pseudo-Player; same for assister). DELETE FROM excluded per the `docs/migration-sql-lessons.md` checklist; the destructive cleanup of seeded Guest Player + PLM rows lives in operator-run script `scripts/v188CleanupGuestPseudoPlayers.ts` (--dry-run default; --apply gated behind two checks: `MatchEvent.isGuestScorer` column must exist + no MatchEvent may still reference a guest playerId). Server actions `adminCreateMatchEvent` / `adminUpdateMatchEvent` / `submitOwnMatchEvent` accept `isGuestScorer` + `isGuestAssister` and enforce XOR (flag set ⇒ id null + beneficiaryTeamId required); the membership-lookup gate is skipped when `scorerId` is null. `computeScoreFromEvents` widened to handle `scorerId: string | null`. `dbToPublicLeagueData` filters guest pseudo-Players via prefix-check (`p-guest`) so any not-yet-cleaned-up rows don't bleed into the public Squad list, and renders `"Guest"` as the scorer/assister label when the flag is set. UI: admin EventEditor + player SubmitGoalForm gain a `Scored by guest (off-roster)` checkbox above the scorer picker (and `Assisted by guest` above the assister picker); when checked the picker is hidden and the event records the flag with id NULL. End-of-season query for "guest goals for team X" is `WHERE isGuestScorer=true AND beneficiaryTeamId=<lt>`; for OGs against team X it's `WHERE isGuestScorer=true AND goalType=OWN_GOAL AND match.<other team>=X`. Out-of-scope: no new UI surface for guest-stats display this PR — data is queryable. Removed: `GUEST_ID` constant from `src/lib/ids.ts` (unused post-refactor). 41 new regression tests in `tests/unit/v188_guest_event_refactor.test.ts`. Prod audit: 5 Guest Player rows + 4 PLMs + 3 MatchEvents to migrate (all OPEN_PLAY guest scorers, beneficiaryTeamId=null pre-migration). See [phase-guest-refactor.log](../phase-guest-refactor.log).
- **v1.87.0** — per-league player retirement. Schema: `PlayerLeagueMembership.retiredAt: DateTime?` (nullable timestamp, additive migration `20260518000000_plm_retired_at`, every existing row defaults null = active). Roster-size readers gain `retiredAt: null` filter at four sites: `src/lib/plannedRosterStats.ts` (drives "spots left" on `/id/<slug>`), `src/lib/admin-data.ts` `_count.playerAssignments` (admin teams-all per-team count), `src/lib/unpaidFeeBanner.ts` (retired players don't owe fees). The delete-team blocker count in `src/app/admin/teams-all/actions.ts:133` deliberately keeps NO retiredAt filter (retired data still references the team — delete must remain blocked). New admin server actions `retirePlayer({ membershipId, leagueId })` + `unretirePlayer({ … })` in `src/app/admin/leagues/actions.ts` (assertAdmin + IDOR check identical to `updateMembershipPaidStatus`, both revalidate admin + public). Admin PlayersTab kebab menu adds "Retire from league" / "Unretire" item (only when `applicationStatus === 'APPROVED'` AND has a current Assignment); confirm dialog only on retire path. Mobile + desktop player rows gain `opacity-60` when retired plus a "RETIRED" pill (admin-token styled — `bg-admin-surface2 border-admin-border text-admin-text3`) next to the SignInStatusBadge. Public `Player.retiredAt?: string | null` added to `src/types/index.ts` and threaded through `dbToPublicLeagueData` (ISO string). `<SquadList>` sorts retired players to the bottom of their team (sort key: `retiredAt ? 1 : 0` first, then existing position-bucket / alphabetical), greys them with `opacity-50`, shows a "RETIRED" pill (public-token styled — `bg-surface-md border-border-subtle text-fg-low`), and hides the availability badge for retired rows; team header count uses `activeMemberCount` (excludes retired). `<MatchdayAvailability>` filters retired ids out of UPCOMING-matchday `goingIds` defensively against stale Redis RSVPs from before retirement (past-matchday `playedIds` deliberately untouched — players who actually played past matchdays still appear in "Who Played"). Historical stats untouched: `src/lib/matchScore.ts` reads PLM regardless of retiredAt so past goals continue to attribute to their team. Account/player page, recruiting banner state, homepage routing, multi-league hub, JWT mapping all keep retired memberships visible (per-league semantics). 27 new regression tests in `tests/unit/v187_retire_player.test.ts`; stash-pop verified the load-bearing tests fail on the v1.86.0 baseline. See [phase-retire-player.log](../phase-retire-player.log).
- **v1.86.1** — hotfix: correct migration SQL in `20260510000000_plm_preferred_secondary_positions`. The hand-written `ALTER TABLE` and `UPDATE` statements used the Prisma model name `"PlayerLeagueMembership"` instead of the `@@map`-ed SQL table name `"PlayerLeagueAssignment"`, causing a `42P01 relation does not exist` error on `prisma migrate deploy` against production (prod table has always been `PlayerLeagueAssignment`). Fixed by replacing both table references with `"PlayerLeagueAssignment"`. The failed migration row was rolled back on prod via `prisma migrate resolve --rolled-back` before shipping this fix. Root cause: hand-written migrations must use the `@@map` table name, not the Prisma model name — Prisma's auto-generated migrations do this correctly, but manual SQL does not.
- **v1.86.0** — split `PlayerLeagueMembership.positions[]` into `preferredPositions[]` + `secondaryPositions[]`. Schema: two new `String[] @default([])` columns; migration backfills `preferredPositions ← positions` (conservative — players opt-in to secondary by editing their profile). `validatePreferredSecondary(preferred, secondary, ballType)` added to `src/lib/positions.ts`: validates each array independently, dedupes across sets (a code in preferred is silently removed from secondary). Account page (`/account/player`) now shows two stacked `<PositionMultiSelect>` pickers per league card: **"Preferred positions"** (formation fills these first) and **"Also plays"** (fills fallback slots when needed). `updatePlayerLeague` action upgraded to accept `{ preferredPositions?, secondaryPositions? }` fields; legacy `positions?` still accepted for backward compat (both paths dual-write all three columns). `AssignmentInput` in `formations.ts` gains `preferredPositions?` + `secondaryPositions?`; the 5-pass algorithm uses these directly when present, falls back to index-split on legacy `positions[]`. `FormationPitch.tsx` and `dbToPublicLeagueData.ts` thread the new fields into the public data pipeline. All four position-write sites (account actions, join-flow, admin create, admin update) dual-write `preferredPositions = positions; secondaryPositions = []` on new writes. 13 new tests in `tests/unit/v1860_preferred_secondary_positions.test.ts` + 9 updated regression tests.
- **v1.85.2** — three formation refinements: (A) `<PositionPill>` on starters now shows the SLOT's primary code (not player's `positions[0]`) — a CM in an LM slot shows `[LM]`; alternates/bench pills unchanged (show player's own primary since they are candidates, not placed). (B) `groupedPositionLabel(positions[])` helper added to `src/lib/positions.ts` — collapses 12-code soccer codes to GK/DF/MF/FW buckets with dedup (`[CM,CAM]→"MF"`); futsal codes FIXO/ALA/PIVOT pass through unchanged; applied to admin players table (mobile + desktop), SquadList row chips, any text-mode position display outside the pitch. Formation pitch + PositionMultiSelect picker + admin filter dropdowns preserve 12-code precision. (C) `assignPlayersToFormation()` upgraded from 3-pass to 5-pass: 1a primary→slot-primary, 1b secondary→slot-primary, 2a primary→slot-fallback, 2b secondary→slot-fallback, 3 subs. `positions[]` array convention documented in `src/lib/positions.ts`: positions[0] = primary, positions[1..] = secondary. **⚠ Selection-order caveat:** `PositionMultiSelect` stores codes in vocabulary order (GK→LB→CB→…→RW), NOT click order — positions[0] is always the vocab-earliest code, which is a proxy for primary role but not explicit user intent. An explicit `primaryPosition` schema field would be needed for true user-expressed primacy. 27 new tests in `tests/unit/v1852_formations_refinements.test.ts`.
- **v1.85.1** — admin players page: expose existing invite code in the kebab menu. `activeInviteCountByPlayerId` replaced with `activeInviteByPlayerId` (returns full `{ code, expiresAt, skipOnboarding }` object instead of a count) so admins can re-display an existing personal invite without regenerating it.
- **v1.85.0** — homepage redesign phase 1b/1c: persona-aware apex mounted at `/test` (production `/` untouched). New `<HomepageRouter>` (server component) reads session and routes by persona: unauth + 0-membership users see the new `<LeagueDirectory>` (every `League.visibility !== 'PRIVATE'` row, status pill differentiates "Recruiting" from "Closed", linked to `/id/<slug>`); single-APPROVED-membership users get a server-side `redirect('/id/<slug>')`; ≥2-membership users see `<MultiLeagueHub>` — full classic Dashboard for the active league + a pill-strip switcher (Option A — at-a-glance multi-league discovery, beats Option B's hidden-behind-a-chevron dropdown for 2–4-league users; horizontal scroll on overflow) + capped recruiting-handoff (≤2 PUBLIC_OPEN cards excluding the user's own leagues, sorted by `League.updatedAt DESC` as the recency proxy). Active league derived from `User.defaultLeagueId` ∩ memberships with stale-default fallback to alphabetical-first APPROVED — re-introducing the stale-default crash is a regression target. Last-selected tracking via `touchUserDefaultLeague(...)` wired into `/id/[slug]/page.tsx` + `/id/[slug]/md/[id]/page.tsx`; fire-and-forget via `waitUntil` from `@vercel/functions` (same shape as v1.79.0's application-received email), with three short-circuits (no session ident → return; defaultLeagueId === leagueId → no-op; no APPROVED PLM in this league → no-op). New `setUserDefaultLeague(leagueId)` server action mirrors the same membership gate; `<LeagueSwitcherTabs>` calls it then `router.refresh()`es so the server-rendered hub re-evaluates the persona with the new default. New `topSlot?: ReactNode` prop on Dashboard threads the switcher + handoff INSIDE the existing `max-w-lg` column (rendering them outside would put them under the fixed Header). Architectural rule: ALL new homepage logic lives in `src/components/homepage/*` + `src/lib/homepageRouting.ts` so the eventual production swap is a one-line edit (replace `/page.tsx` body with `<HomepageRouter />`). Test pin update: `tests/unit/v1755_league_details_full_consolidation.test.ts` `getServerSession` pin scoped to apex `/page.tsx` only — the `/id/[slug]/*` pages legitimately re-introduce the call to drive `touchUserDefaultLeague`. 57 new tests in `tests/unit/v185_homepage_phase1bc.test.ts`; stash-pop verified 4/57 fail when load-bearing logic (classifyPersona stale-default fallback, both membership gates, single-redirect dispatch) is reverted. See [phase-homepage-1bc.log](../phase-homepage-1bc.log) for the full file-by-file diff and the swap-step diff.
- **v1.84.0** — formations follow-up to v1.83.1: widened slot-compat with a primary + fallback model + depth-chart layout + position pills. Canonical data direction flipped from player→slots to **slot→{primary, fallback}** (`SlotCompat` in `src/lib/formations.ts`). Soccer 12-code map: GK strict; LB primary {LB} fallback {CB,LM,DM}; CB primary {CB} fallback {DM}; RB primary {RB} fallback {CB,RM,DM}; DM/CM/CAM/LM/RM/LW/RW/ST per phase log M1 table. Futsal 4-code map **tightened**: GK + FIXO strict (no fallback); ALA fallback {PIVOT}; PIVOT fallback {ALA}. v1.83.1's permissive `FIXO ↔ ALA` cross-fill is gone — affects the futsal 4-0 formation (a FIXO-only player now lands in subs instead of slotting into an ALA outfield). `assignPlayersToFormation()` rewritten as **two-pass scarcity-first**: Pass 1 placements use slot.primary only; Pass 2 covers leftover slots via slot.fallback among unplaced players; Pass 3 = subs. Function is pure → re-running on roster change yields the new placement, so a ST joining a 3-CM roster automatically takes ST primary without displacing CMs in CM/LM/RM. New helpers `playerCodeFillsSlotPrimary` + `playerFillsSlotPrimary` exposed; existing `playerCodeFillsSlot` semantics unchanged (true via primary OR fallback). New `assignAlternatesToSlots()` greedy-single-listing: each sub goes under the FIRST slot they primary-match (slot-array order) else first fallback-match else `noFitOverflow`. `<FormationPitch>` rewired: renamed `<SlotDot>` → `<SlotColumn>` rendering starter + up-to-3 alternates stacked vertically beneath each slot anchor (max-width 78px column, 7px alternate font); v1.83.1 "Subs (N)" pill row replaced with "Other subs" capturing only no-fit + cap-overflow players. New `<PositionPill>` component renders the player's first-in-array position code on every starter/alternate/sub tile (`bg-black/55` for starters, `bg-white/10` for alternates) — visually obvious when (e.g.) a CM is filling an LM slot. 15 new `[v1.84.0 regression]` tests + 3 v1.83.1 futsal tests rewritten for the tightened map; total 43 formation tests green. Stash-pop verified all 14 of the new behavioural-regression tests fail on the v1.83.1 baseline. Spec discrepancy surfaced in [phase-formations-followup.log](../phase-formations-followup.log) M1: user's narrative example "3 CMs fill LM/RM/ST" implies CM ∈ ST.fallback, but the literal table they gave does NOT include CM there — implemented the literal spec; ST stays empty when only CMs are available, leftover CM goes to subs. Out of scope: clicking an alternate name to swap them into the slot (today opens the existing slot picker for that slot — same UX as v1.83.1).
- **v1.84.0** — homepage redesign phase 1a: schema foundations for the multi-league homepage hub. New `LeagueVisibility` enum (PRIVATE / PUBLIC_CLOSED / PUBLIC_OPEN) + `League.visibility` column (default PUBLIC_CLOSED); migration backfills existing `recruiting=true` rows to PUBLIC_OPEN so legacy banner-on leagues see no UX change. New `User.defaultLeagueId String?` FK with `onDelete: SetNull` for the upcoming per-user pin; phase 1a ships the column with no writes (1b/1c wires them). Admin SettingsTab grew a three-button visibility radio with optimistic state + server-reject rollback; new `setLeagueVisibility(leagueId, value)` server action validates against the literal set and revalidates admin + public domains. Banner-gate read site swung from `flags.recruiting` to `flags.visibility === 'PUBLIC_OPEN'` across apex `/`, `/id/[slug]`, `/id/[slug]/md/[id]`. `applyToLeague` accepts PUBLIC_OPEN + PUBLIC_CLOSED and rejects PRIVATE with an "invitation-only" message (legacy `!league.recruiting` rejection on this path is gone — PRIVATE wins over stale `recruiting=true`). `League.recruiting` + `League.isDefault` columns kept for the transition; both carry removal TODOs. 28 new regression-target tests; stash-pop verified 9/28 fail on broken state. See [phase-homepage-1a.log](../phase-homepage-1a.log) for M1–M4.
- **v1.83.1** — formation-visualization rebuild on the v1.82.0 multi-position model. New `src/lib/formations.ts` (pure module) ships per-format catalogs of 5 canonical formations each — SOCCER 5..11-aside (e.g. 9-aside: 3-3-2 / 3-4-1 / 4-3-1 / 2-4-2 / 3-2-3) and FUTSAL 5-aside (1-2-1 / 2-2 / 1-1-2 / 2-1-1 / 4-0). Every formation slot carries a position code (12-code soccer or 4-code futsal) and normalised `(x, y) ∈ [0,1]` coordinates. New `<FormationPitch>` component replaces the in-file `<TeamFormation>` from v1.69.0: horizontal-scroll picker → SVG pitch with absolutely-positioned slots → multi-role assignment + bottom-sheet manual override. Greedy scarcity-first assignment (`assignPlayersToFormation()`): for each unfilled slot pick the one with fewest candidates first, then within that slot pick the player with fewest other compatible slots; players with empty `positions[]` go to a `playersWithoutPositions` bucket; overflow players land in a "Subs (N)" pill row. Slot-position compat map (player → slot codes): `LB→{LB}` strict per spec, `CM→{DM,CM,CAM}`, `LM→{LM,LW}`, plus football-common shifts (`CB→{CB,DM}`, `CAM→{CAM,CM,ST}`, `LW→{LW,LM,ST}`, etc.); futsal `FIXO/ALA/PIVOT` permissive on neighbours. Picker selection persists per-(ballType, playerCount) under `t9l-formation:<ballType>:<n>` localStorage key. Manual override stored as a slot→playerId map merged with the auto-assignment; resets when the formation or roster changes. New `ballType` + `playerFormat` props threaded `Dashboard → ClassicLeagueHomepage → MatchdayAvailability → FormationPitch` (Dashboard pulls them from `league?.ballType` and `leagueDetails?.playerFormat`). 28 new regression-target tests pin the catalog shape, slot-compat decisions, and assignment behaviour; stash-pop verified the suite fails to load when `formations.ts` is removed. See [phase-formations.log](../phase-formations.log) M3 for the full slot-compat rationale.
- **v1.83.0** — multi-league redesign of `/account/player`. Pre-v1.83.0 the page resolved `getDefaultLeagueId()` and surfaced exactly ONE PlayerLeagueMembership; v1.82.0's `updatePlayerSelf` then iterated active memberships and wrote the same submitted positions[] to EVERY one. A player playing GK in League A and FW in League B couldn't represent that. v1.83.0 splits the surface: top section (profile picture + name + ID-upload state) saves player-level via `updatePlayerProfile({ name })`; one per-league card per active PLM (server-sorted: default-league → APPROVED alphabetical → PENDING last) saves per-league via `updatePlayerLeague({ leagueId, positions, idShared })` with an owner gate scoped to `(playerId, leagueId, toGameWeek === null)` — no cross-league bleed. Each card shows team, application-status badge, membership-status badge (when not ACTIVE), per-league `PositionMultiSelect` driven by THAT league's `ballType` (so a soccer card shows the 12 soccer codes and a futsal card shows the 4 futsal codes), jersey number (read-only), resolved fee + paid-status badge (via existing `resolvePlayerFee`), `idShared` toggle (per-league consent for the player's uploaded ID being viewable by THIS league's admins), and applicant comments quoted back read-only. The monolithic `updatePlayerSelf` is gone — re-introducing it would re-introduce the multi-league overwrite bug (regression-target tests guard against it). Added empty-active-memberships branch with friendly "you're not currently rostered in any league" notice. 24 new regression tests + 13 reshaped existing tests; stash-pop verified 21/24 of the new tests fail on the v1.82.0 broken state. See [phase-edit-details.log](../phase-edit-details.log) for the audit + execution log and [handover-edit-details-audit.md](../handover-edit-details-audit.md) for the full audit. Out of scope (deferred): DOB editor, jersey-number editability, skill-level field (no schema column exists).
- **v1.82.2** — extend the v1.81.0 success popup pattern to the live join-flow server actions in `src/app/join/[code]/actions.ts`. Pre-v1.82.2 the popup only fired for the public recruiting paths (`applyToLeague`, `registerToLeague`); invite-redemption and the single-page onboarding form (`completeOnboardingWithId`) — plus the legacy two-step `/id-upload` path's `submitIdUpload` + `skipIdUpload` — landed users on `/welcome` with no acknowledgement modal. v1.81.0's "skipped on the 5 join-flow paths" rationale was a stale model: ID submission is now part of the main onboarding form, so those paths are terminal submits, not continuation flows. Wiring: each of the 4 live actions appends `?submitted=<descriptor>` to its existing `/welcome` redirect (no `originPath` plumbing needed because the destination IS the popup-mounting page); `<SuccessConfirmationGate>` is mounted on `/join/[code]/welcome/page.tsx` under `<Suspense>` (server-component page → `useSearchParams()` boundary). `redeemInvite` only attaches the param on its terminal skipOnboarding=true arm; the skipOnboarding=false arm still routes to `/onboarding` unchanged so the popup fires after `completeOnboardingWithId` instead. Four new MESSAGES descriptors: `redeemInvite` ("Invite redeemed" / "You're now a member of the league."), `completeOnboardingWithId` (mirrors recruiting copy), `submitIdUpload` ("ID uploaded"), `skipIdUpload` ("Application complete"). 18 new regression-target tests in `tests/unit/v1812_join_success_modal.test.ts`; stash-pop verified 16/18 fail on broken state. Five pre-existing redirect pins (`idUploadActions`, `redeemInvite`, `v168_id_collection_streamlined`) updated to the `?submitted=` shape. **Out-of-scope follow-up:** `submitOnboarding` is dead code (no caller in src/) — pre-v1.68.0 multi-step flow superseded by `completeOnboardingWithId`. Surfaced as a deletion candidate; left in place this PR.
- **v1.82.1** — cross-team scorers/assisters for stats events. Casual leagues let players guest for other teams; pre-v1.82.1 the goal-submission UIs (player-facing `SubmitGoalForm` + admin `EventEditor`) only let users pick scorers from one team's roster, and the server validation rejected anything else. Both UIs now list every league player with the beneficiary team's roster sorted to the top via `<optgroup>` and "Other players" below; selecting a non-beneficiary-team player surfaces an inline "(guest from <Team>)" hint. Server validation loosens scorer + assister scope from "on a match team" / "on the beneficiary team" to "any active member of this league" — `playerLeagueMembership.findFirst({ where: { playerId, leagueId, leagueTeamId: { not: null } } })`. Beneficiary team is now an explicit input on the player path (the form's new "Goal counts for" picker) since the pre-v1.82.1 implicit derivation (= scorer's team for non-OG, opposite for OG) breaks once cross-team scorers are legal. New `MatchEvent.beneficiaryTeamId` column (nullable, FK ON DELETE SET NULL, indexed) records explicit beneficiary so the score recompute attributes cross-team scorers correctly; legacy rows with null fall back to the v1.42.0 scorer-team derivation. New helper `src/lib/playerOrdering.ts#groupPlayersByPrimaryTeam` is shared by both forms. 25 new tests; stash-pop verified 18 fail on broken state.
- **v1.82.0** — multi-position support per format. New `PlayerLeagueMembership.positions String[]` (Postgres native array) replaces the single `position PlayerPosition?` column as the canonical store; legacy column kept for one cycle with dual-write (`legacyPositionFromArray()` buckets new codes back to GK/DF/MF/FW). Per-format vocabulary in `src/lib/positions.ts`: SOCCER → 12 codes (GK / LB CB RB / LM DM CM CAM RM / LW ST RW); FUTSAL → 4 codes (GK / FIXO / ALA / PIVOT). Server actions (`applyToLeague`, `registerToLeague`, `submitOnboarding`, `completeOnboardingWithId`, `updatePlayerSelf`, `adminCreatePlayer`, `adminUpdatePlayerPosition`) all pull `league.ballType` and validate against per-format vocab via `normalizePositions()`; cross-format codes return `{ ok: false, error: 'Invalid position "FW" for FUTSAL …' }`. New chip multi-select component (`src/components/PositionMultiSelect.tsx`, public + admin variants) replaces every single-select dropdown — RegistrationFields (recruit + onboarding), ApplyToLeagueModal, AccountPlayerForm, AddPlayerDialog, EditPlayerPanel. Read sites: public `Player.position` is now `positions.join('/')` (joined string like `"CB/CM"`); SquadList + MatchdayAvailability colour/sort by role bucket via new `getPositionBucket()` helper (handles both new and legacy codes; falls back to MF on unknown). Migration `20260516000000_plm_positions` adds `positions text[]` (default `'{}'`) and backfills from the legacy enum with mapping `GK→GK, DF→CB, MF→CM, FW→ST`. 35 new tests (29 vocabulary unit tests + 6 server-action regression-target tests for futsal/soccer enforcement); stash-pop verified 2/6 fail on broken per-format dispatch. See [phase-multi-position.log](../phase-multi-position.log) for the full file-by-file diff. **Migration callout:** existing rows with `position = 'DF'` / `'MF'` / `'FW'` are mapped to `'CB'` / `'CM'` / `'ST'` respectively in `positions[]`; the legacy column stays untouched for the one-cycle dual-write window. Two follow-ups queued: formation viz redesign for the 12-code vocab, and edit-details page audit.
- **v1.81.0** — post-submit success confirmation popup for the recruiting flow. New `<SuccessConfirmationModal>` (animated SVG check via stroke-dashoffset keyframes in `globals.css`, `role=dialog` + focus-trap + body-scroll-lock + ESC/backdrop-as-OK, `<Link href={okHref} replace scroll={false}>` for clean URL handoff). Origin-tracking pattern: form captures `pathname + search` at mount → server action redirects to `<originPath>?submitted=<descriptor>` via `buildSuccessRedirect()` (with `safeOriginPath` validation: rejects `//`, full URLs, traversal, control chars, >512 chars; falls back to `/id/<league.subdomain>`). `applyToLeague` now redirect()s on all 3 success branches (existingPlm idempotent / new-PLM / fresh-Player); `registerToLeague` redirect target moved into the helper. `<SuccessConfirmationGate>` reads `useSearchParams()` and lazy-mounts via `dynamic()` from Dashboard so the chunk only fetches on the success path. Modal callers (`ApplyToLeagueModal`, `RegistrationForm`) drop `update()` / `router.refresh()` post-success because the server-side redirect re-renders fresh; new `'digest' in err` re-throw pattern in the modal's catch block lets NEXT_REDIRECT propagate. Skipped on the 5 join-flow paths (`redeemInvite`, `submitOnboarding`, `submitIdUpload`, `skipIdUpload`, `completeOnboardingWithId`) — they all redirect to `/welcome` (already a thank-you page) or `/id-upload` / `/onboarding` (continuation forms where popup at form mount blocks immediate use); see [phase-success-modal.log](../phase-success-modal.log) M3 for the per-path table. 49 new regression-target tests; stash-pop verified 20/49 fail on broken state.
- **v1.80.11** — admin-orthogonal-UX follow-up: applied the v1.80.10 fix shape to 5 same-pattern sites in `src/app/join/[code]/actions.ts` flagged in v1.80.10's audit (`redeemInvite`, `submitOnboarding`, `submitIdUpload`, `completeOnboardingWithId`, `skipIdUpload`). All gated on raw `session.userId`, all threw "Admin sessions cannot..." copy for any session lacking `userId` — same anti-pattern that rejected legacy LINE sessions. Each site now accepts `userId` OR `lineId`, resolves the User row by either identifier (User.lineId @unique), and routes the canonical `user.id` downstream. Admin-shaming copy replaced with neutral guidance. Joined-up coverage with v1.80.10 closes the v1.65.4 admin-orthogonal-UX rule for the entire join flow.
- **v1.80.10** — admin-orthogonal-UX bug fix: `applyToLeague` / `registerToLeague` / `/recruit/[slug]` page / `upload-token` route all gated on raw `session.userId` and threw `"Admin sessions cannot submit applications"` for any session without `userId` — rejecting grandfathered LINE sessions whose JWT predates v1.28.0 stage α.5 (lineId set, userId never populated on token refresh) and LINE-auth admins whose admin role is orthogonal to player binding. Fix mirrors the v1.59.1 pattern in `account/player/actions.ts:requireSelfPlayerSession`: gate now accepts `userId` OR `lineId`, resolves the User row by either identifier (User.lineId @unique), and uses the canonical `user.id` for downstream Player.create / upload-pathname / etc. Admin-shaming copy replaced with neutral "Sign in with a player account to apply". Reported by user trying to apply to a new league via apex banner. 14 new regression-target tests pin the fix shape; stash-pop verified 12/14 fail on broken state. Audit findings (out of scope, follow-up): `redeemInvite`, `submitOnboarding`, `submitIdUpload`, `completeOnboardingWithId`, `skipIdUpload` in `src/app/join/[code]/actions.ts` use the same anti-pattern.
- **v1.80.9** — perf phase 4d: lazy-load sonner's `<Toaster />` from `app/layout.tsx` via `next/dynamic` in a `'use client'` wrapper (`components/ToasterMount.tsx`). Pre-v1.80.9 the static `import { Toaster } from "sonner"` pulled the full sonner bundle (~36 KB raw / ~10 KB gz) into the public root chunk on every page load — even though the Toaster is DOM-empty until a `toast(...)` call fires. Same lazy-load pattern as the v1.80.8 modal fix. Per-route post-v1.80.8→v1.80.9 first-load JS measurements (`route-bundle-stats.json`, gzipped sum of `firstLoadChunkPaths`): `/_not-found`, `/[slug]`, `/league/[slug]`, `/matchday/[id]`, `/recruit/[slug]`, `/join/[code]/onboarding`: -8,252 gz each (~31 KB raw); `/stats`: -8,985 gz; `/`, `/id/[slug]`, `/id/[slug]/md/[id]`: +594 gz (sonner is preloaded as an async chunk for routes that share Recruiting/Header dependency clusters — Turbopack chunking trade-off; net loss is small versus the 7-route win). Total weighted savings across public routes: ~5-7 KB gz on average. Stash-pop verified: regression-target tests in `perfPhase4d.test.ts` fail when `<Toaster>` is re-injected into `app/layout.tsx`
- **v1.80.8** — perf phase 4c: lazy-load auth modals (`SignInLightbox`, `ApplyToLeagueModal`) via `next/dynamic` — they were statically imported by `Header → LineLoginButton`, `RecruitingBanner`, and `GuestLoginBanner` but only mount after user click. Header chunk 1347: 27,577 → 21,509 parsed (-6,068 / -1,059 gz) on every public page. Dashboard chunk 7206: 33,725 → 26,174 parsed (-7,551 / -2,203 gz) on the landing page. Modals now ship as their own async chunks (~9 KB SignInLightbox, ~5 KB ApplyToLeagueModal) that fetch only when opened. JSX gated on the open-state booleans so the deferred chunks don't fetch on mount.
- **v1.80.7** — perf phase 4b: bundle analyzer audit — `leagueSlug.ts` and `leagueDetails.ts` were dragging `@prisma/client/runtime/index-browser.js` (~47 KB parsed / ~17 KB gzip) into the public bundle on every route because client components legitimately imported pure exports (`DEFAULT_LEAGUE_SLUG`, `BALL_TYPE_LABELS`, etc.) from files that also `import { prisma }`. Split DB-cached lookups into `leagueSlugServer.ts` + `leagueDetailsServer.ts`; pure types/constants/helpers stay in the original modules. Total client bundle: -49,225 bytes parsed / -17,632 bytes gzip. Zero `@prisma/*` references remaining in `.next/static/chunks` (was 47 KB)
- **v1.80.6** — perf phase 4: LCP fix (Barlow Condensed `display: 'optional'` — kills the late font-swap re-paint that pinned LCP to the matchday `<h2>`) + admin-only fonts (Barlow Sans, DM Mono) lifted out of public root layout (~50 KiB woff2 transferred saved per first load) + drop unused weight 400 from Barlow Condensed (~10 KiB saved) + `@next/bundle-analyzer` wired (`ANALYZE=true npx next experimental-analyze`)
- **v1.80.5** — perf phase 3: Google Translate gated behind locale (EN visitors skip the GT bundle entirely — removes el_main_css from PSI critical path) + browserslist config drops SWC legacy polyfills (Array.prototype.at, Object.fromEntries, etc.); LCP fix deferred via [docs/perf-phase3-lcp-handoff.md](perf-phase3-lcp-handoff.md)
- **v1.80.4** — perf phase 3: `sizes=` on 6 `<Image fill />` callers (PlayerAvatar, UserTeamBadge, MatchdayCard, SquadList, LeagueTable, TopPerformers) so next/image stops serving 3840px variants for 12-64px slots; defensive `take: 5000` on goal/matchEvent/leagueInvite findMany in admin-data
- **v1.80.3** — perf phase 2: `next/dynamic` for below-fold widgets — Dashboard (MatchdayAvailability/LeagueDetailsPanel/PlannedRosterStats/RsvpBar/SubmitGoalForm/CompressedMatchdaySchedule) + /stats (TopPerformers/SquadList); H2 font pruning deferred (every candidate weight has callers)
- **v1.80.2** — perf phase 1: bfcache reload deleted, /stats fetches parallelized, public-data revalidate 30s→300s, 3 oversized PNGs re-encoded (~966 KB saved)
- **v1.80.1** — sign out preserves current page via getCurrentCallbackUrl()
- **v1.80.0** — comments field on onboarding form + admin display
- **v1.79.3** — combine Season Fee + Register By onto one dt/dd row in LeagueDetailsPanel
- **v1.79.2** — fix Register By row alignment (split onto own table-aligned row)
- **v1.79.1** — send approval email when admin approves application

Older entries (v1.77.1, v1.78.0, v1.79.0, etc.) condensed in [ledger-archive.md](ledger-archive.md).
