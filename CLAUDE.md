# CLAUDE.md

> **Stale-worktree protocol — FIRST ACTION OF EVERY FRESH SESSION.**
>
> **Parent first, worktree second.** The parent repo's local `main` needs to track `origin/main` so future worktrees inherit fresh state; THEN the worktree's branch rebases on `origin/main` to pick up everything since spawn. Skipping the parent step fixes the current worktree but leaves the next worktree spawn on the same stale base.
>
> ```
> git -C /Users/stefan/t9l-website fetch --all --prune
> git -C /Users/stefan/t9l-website pull --ff-only origin main
> git rebase origin/main   # in the worktree
> ```
>
> Every Claude Code session is spawned in a worktree branched off the parent repo's *local* `main`, which routinely lags behind `origin/main` as PRs merge upstream. The worktree's HEAD on session start is therefore frequently weeks behind reality. Without the rebase, the entire CLAUDE.md / file structure / git log / dependency surface you observe is a stale snapshot — diagnostics like "the file the plan references doesn't exist" can be artifacts of the lag, not real prompt injection. Multiple prior sessions have flagged plan inputs as fictional and stopped, when in fact rebasing onto `origin/main` revealed all the referenced files were already shipped.
>
> **Procedure:**
> 1. **Fast-forward the parent's `main` to `origin/main` first** via the `git -C` commands above. If `pull --ff-only` fails (parent has uncommitted changes, or local `main` has diverged from `origin/main`), STOP and surface to the user. Do NOT force, do NOT `reset --hard`, do NOT switch branches inside the parent — divergent parent state means the operator has uncommitted work or an in-progress branch and needs to resolve manually.
> 2. **Then rebase the worktree onto `origin/main`.** Run *before* any reading or analysis of the briefing — do not skip even if the parent message looks self-contained.
> 3. **If `git rebase` reports conflicts in the worktree, STOP and surface to the user.** Do not attempt to resolve, force, abort-and-reset, or otherwise paper over divergent state — the conflict means the orchestrator gave the worktree commits that diverge from upstream, and the right move is operator intervention.
> 4. **If both succeed (or are no-op fast-forwards), proceed with the task as briefed.** Re-read CLAUDE.md from scratch — frontmatter, file structure, ledger — because everything you "remembered" from the pre-rebase state may be wrong.
> 5. **If the rebase reveals files / tags / version constants the briefing references that you previously couldn't see, that's the expected path — execute the briefing.** A briefing matching post-rebase state is the strongest signal it's legitimate, not the weakest.
>
> This rule exists because diagnostic agents have repeatedly mis-classified legitimate work as prompt injection on the basis of a stale worktree. The cost of pausing to rebase is seconds; the cost of refusing real work in front of a backlog of merged PRs is multi-session drag.
>
> **Recent ledger (top 20 PRs in full; older entries condensed below).**
>
> **Current release:** v1.71.0 — **Retire the Google Sheets surface entirely.** App fully cut over to Postgres at v1.0.x; the legacy Sheets parser path (`dataSource='sheets'`) and the Sheets RSVP dual-write (`writeMode='dual'`) had been dormant since 2026-04-27 (operational `dataSource='db'` flip). v1.71.0 deletes the dead weight. **Files deleted:** [`src/lib/sheets.ts`](src/lib/sheets.ts) (`fetchSheetData` / `writeRosterAvailability` / `RawSheetData` — 127 lines); [`src/lib/mock-data.ts`](src/lib/mock-data.ts) (Sheets-credentials fallback fixtures — 149 lines); [`scripts/importFromSheets.ts`](scripts/importFromSheets.ts) (legacy one-shot importer — 374 lines). **Files archived (not deleted) for operator reference:** [`scripts/_archive/sheetsToDbBackfill.ts`](scripts/_archive/sheetsToDbBackfill.ts) and [`scripts/_archive/backfillMatchEventsFromSheet.ts`](scripts/_archive/backfillMatchEventsFromSheet.ts). Both `tsconfig.json` and `tsconfig.scripts.json` gain `exclude: ["scripts/_archive"]` so archived scripts no longer typecheck against the live tree. **Refactors:** [`src/lib/data.ts`](src/lib/data.ts) collapses to just `slugify()` (the load-bearing helper used by `/api/assign-player`'s LINE-CDN mirror); the Sheets parser family (`parseTeams` / `parsePlayers` / `parseSchedule` / `parseGoals` / `parseAllData` / `computeMatchScores` / `normalizeTeamName` / `normalizeDate` / `resolveMatchdayId`) is gone. [`src/lib/publicData.ts`](src/lib/publicData.ts) drops the `getFromSheets` cache wrapper and the dispatcher; `getPublicLeagueData` / `getPlayerByPublicId` go straight to the DB reader. [`src/app/api/rsvp/route.ts`](src/app/api/rsvp/route.ts) drops the `writeRosterAvailability` import + the `writeMode === 'sheets-only'` branch + the dual-write call inside `persistRsvpToPrisma`; Postgres is now the only durable secondary. [`src/lib/settings.ts`](src/lib/settings.ts) loses `DataSource` / `WriteMode` types, `SETTING_IDS`, `resolveDataSource`, `getDataSource`, `getWriteMode` (the `Setting` rows on prod become harmless dead data). [`src/components/admin/SettingsTab.tsx`](src/components/admin/SettingsTab.tsx) drops the entire "Public site source-of-truth" section (data-source toggle + write-mode 3-button group). [`src/app/admin/leagues/[id]/settings/page.tsx`](src/app/admin/leagues/[id]/settings/page.tsx) stops fetching `getDataSource` / `getWriteMode` and stops threading `initialDataSource` / `initialWriteMode` props. [`src/app/admin/leagues/actions.ts`](src/app/admin/leagues/actions.ts) drops `setDataSource` and `setWriteMode` server actions + the `SETTING_IDS` / `DataSource` / `WriteMode` import from settings. [`src/lib/dbToPublicLeagueData.ts`](src/lib/dbToPublicLeagueData.ts) and [`src/lib/revalidate.ts`](src/lib/revalidate.ts) get docstring polish dropping stale `parseAllData` / `dataSource` references. **package.json:** `googleapis: ^171.4.0` removed from dependencies; `db:import` script removed. `npm install` confirms no transitive dep restored — `next-auth/providers/google` is OAuth-only. **Tests:** new [`tests/unit/sheetsRemoved.test.ts`](tests/unit/sheetsRemoved.test.ts) (34 cases — files removed (`src/lib/sheets.ts` / `src/lib/mock-data.ts` / `scripts/importFromSheets.ts` are gone; `sheetsToDbBackfill.ts` + `backfillMatchEventsFromSheet.ts` live in `_archive/`); package.json has no `googleapis` dep + no `db:import` script (regression target — re-adding either re-introduces the dependency); `lib/settings.ts` exports neither `DataSource` / `WriteMode` types nor `getDataSource` / `getWriteMode` / `resolveDataSource` / `SETTING_IDS`; admin actions exports neither `setDataSource` / `setWriteMode`; SettingsTab does not import `setDataSource`/`setWriteMode`/`DataSource`/`WriteMode` and does not render the legacy "Public site source-of-truth" section heading or the "Google Sheets" button label; rsvp/route.ts does not import `writeRosterAvailability` or `getWriteMode` and does not branch on `writeMode === 'sheets-only'`; publicData.ts does not import `fetchSheetData` / `parseAllData` / `getDataSource` and does not declare `getFromSheets`; data.ts exports `slugify` and does NOT export the parser family; admin Settings page does not import `getDataSource` / `getWriteMode` and does not pass `initialDataSource` / `initialWriteMode` props). [`tests/unit/settings.test.ts`](tests/unit/settings.test.ts) reduced to the surviving `resolveIdentityReadSource` + `SETTING_ID_IDENTITY_READ_SOURCE` cases (the `SETTING_IDS` / `resolveDataSource` blocks removed). [`tests/unit/rsvpRouteWriteInversion.test.ts`](tests/unit/rsvpRouteWriteInversion.test.ts) and [`tests/unit/rsvpRouteIntegration.test.ts`](tests/unit/rsvpRouteIntegration.test.ts) drop their `@/lib/sheets` + `@/lib/settings#getWriteMode` mocks and the `sheets-only` mode branch case. [`tests/unit/publicDataLeagueScope.test.ts`](tests/unit/publicDataLeagueScope.test.ts) drops the same three mocks. Deleted: [`tests/unit/backfillHelpers.test.ts`](tests/unit/backfillHelpers.test.ts) and [`tests/unit/backfillMatchEvents.test.ts`](tests/unit/backfillMatchEvents.test.ts) (imported the now-archived backfill scripts). [`tests/unit/adminUiCompatAudit.test.ts`](tests/unit/adminUiCompatAudit.test.ts) drops the "data-source helper copy" case (the section the case asserted on no longer exists). 2039 passed | 2 skipped. Type-check clean. Minor bump (1.70.4 → 1.71.0) — meaningful surface change (admin loses the data-source/write-mode toggles; new dependency-removal regression targets). **Operator follow-up (queued in TODO.md):** delete the now-orphan `Setting` rows (`s-public-dataSource-global` + `s-public-writeMode-global`) when convenient — both rows are read by no code path post-v1.71.0; cosmetic-only cleanup. **Out of scope (deferred):** removing the `GOOGLE_SHEET_ID` / `GOOGLE_SERVICE_ACCOUNT_EMAIL` / `GOOGLE_PRIVATE_KEY` env vars from Vercel project settings (no code reads them anymore — leaving them is harmless; cleanup at operator's discretion); deleting the Setting seed migration file (rolling back unused migrations is invasive — keep it for historical record).
>
> v1.69.1 — **Bug fix: `/recruit/[slug]` and `/join/[code]/onboarding` form submissions failed with "An unexpected response was received from the server" because Next.js's server-action body limit (6MB, set in v1.62.0) was below v1.68.0's max payload (21MB).** User-reported: filling the recruit form with name + position + ID front + ID back + (optional) profile picture and submitting → server error → no Player or PLM created. **Diagnosis:** [`RegistrationFields.tsx:42-44`](src/components/registration/RegistrationFields.tsx) caps each ID file at 8MB and the optional profile picture at 5MB, so a complete submission can be up to 21MB; even a single ID file above 6MB triggers the framework's body-limit rejection BEFORE the server action runs, surfacing the exact "unexpected response" error message [`next.config.ts`](next.config.ts) already documents in its v1.62.0 docstring. Same bug shape as v1.62.0 (which had bumped the limit from 1MB → 6MB to fit the `/account/player` 5MB picture upload); v1.68.0 changed the workload without bumping the limit. NOT a `'use server'` non-async-export bug (every export in `src/app/api/recruiting/actions.ts` and `src/app/join/[code]/actions.ts` is either an async function or an erased TypeScript type/interface — Next 16's serialization contract is satisfied). NOT a Vercel Blob upload bug (`BLOB_READ_WRITE_TOKEN` is set on prod; the action's gate runs but never gets reached). **Fix:** [`next.config.ts`](next.config.ts) bumps `experimental.serverActions.bodySizeLimit` from `'6mb'` to `'25mb'` (21MB max payload + multipart/FormData overhead headroom). One-line change. **Tests:** new `v1.69.1 — body limit raised again for /recruit/[slug] multi-file upload` block in [`tests/unit/accountPlayerCleanupV162.test.ts`](tests/unit/accountPlayerCleanupV162.test.ts) (4 cases — limit ≥ 21MB; regression target that the literal `'6mb'` value is gone; `ID_MAX_BYTES` stays at 8MB so the 21MB floor is the right one; `PIC_MAX_BYTES` stays at 5MB same). Existing v1.62.0 test relaxed from literal `'6mb'` to flexible `'<digits>mb'` shape (the v1.62.0 floor check `>= 5MB` still passes at 25MB so the original architectural intent stays pinned). [`tests/unit/v169_availability_toggle.test.ts:158-163`](tests/unit/v169_availability_toggle.test.ts) version-pin floor relaxed from literal `1.69.0` to "any v1.69.x or later" — the pinned-literal pattern conflicts with patch bumps inside the same minor version (a v1.69.0 test pinning the literal would force v1.69.1 to also touch that file, fanning the fix across an unrelated test). Stash-pop sanity check confirmed: reverting just `next.config.ts` makes 2 of 4 new cases fail. 2023 passed | 2 skipped. Type-check clean. Patch bump (1.69.0 → 1.69.1) — bug fix on existing config; no schema change, no contract change to any server action, no UX flow change. **v1.70.0 queued (separate PR):** move `Player.idFrontUrl` / `idBackUrl` / `idUploadedAt` to User-level (one person, one ID — identity proof is a User-level attribute, not per-league; mirrors profile-picture being on User).
>
> v1.70.0 — **Move ID images from Player to User.** Identity proof (driver's license / passport photos) is per-person, not per-league. One person, one ID. Pre-v1.70.0 `Player.idFrontUrl` / `Player.idBackUrl` / `Player.idUploadedAt` could differ across a User's Players in different leagues, but the post-v1.65.x model has Player as global identity (1:1 with User via `Player.userId @unique`); ID belongs at the User level for consistency. **Schema:** new migration [`20260509000000_move_id_to_user`](prisma/migrations/20260509000000_move_id_to_user/migration.sql) — single transaction: ADD `User.idFrontUrl String?` / `idBackUrl String?` / `idUploadedAt DateTime?` → BACKFILL via `UPDATE "User" ... FROM (SELECT DISTINCT ON ("userId") ... FROM "Player" WHERE "userId" IS NOT NULL AND "idUploadedAt" IS NOT NULL ORDER BY "userId", "idUploadedAt" DESC NULLS LAST) src WHERE u."id" = src."userId"` (DISTINCT ON guards the defensive multi-Player-per-User edge case; the v1.65.x `User.playerId @unique` + `Player.userId @unique` pair already enforces 1:1, so realistic backfill is one-to-one) → DROP the three columns from Player. Rollback recipe inline in the migration SQL header. **Write paths flipped to User:** [`submitIdUpload`](src/app/join/[code]/actions.ts) and [`completeOnboardingWithId`](src/app/join/[code]/actions.ts) write `tx.user.update({ where: { id: userId }, data: { idFrontUrl, idBackUrl, idUploadedAt } })` instead of `tx.player.update`. [`registerToLeague`](src/app/api/recruiting/actions.ts) (the State C recruit flow) creates Player WITHOUT the ID columns then puts them on the User.update alongside `playerId: created.id`. **Read paths flipped to User:** [`adminPurgePlayerId`](src/app/admin/leagues/actions.ts) resolves `Player.userId → User.id` then DELs Blob assets + nulls the User columns (no-op when Player has no User binding). [`/join/[code]/id-upload`](src/app/join/[code]/id-upload/page.tsx) reads `idUploadedAt` from User. [`/account/player`](src/app/account/player/page.tsx) `hasUploadedId` derives from User. [`getLeaguePlayers`](src/lib/admin-data.ts) gains a 7th tuple element `idDataByPlayerId` — built from `prisma.user.findMany({ where: { playerId: { not: null }, idUploadedAt: { not: null } }, select: ... })` and keyed on `User.playerId` so the page-level synthetic-row builder can surface ID state per Player without changing the in-memory `PlayerRow` shape (callers continue to read `idFrontUrl/idBackUrl/idUploadedAt` off the row). The PlayersTab + IdViewerDialog client components are unchanged — they consume the in-memory row, which now sources from User instead of Player. **Tests:** new [`tests/unit/v170_id_to_user_migration.test.ts`](tests/unit/v170_id_to_user_migration.test.ts) (20 cases — schema asserts User has the three columns + Player no longer does (regression target — re-adding `Player.idFrontUrl` would fail); migration ADD-then-BACKFILL-then-DROP ordering pinned via index comparisons; backfill copies all three columns via `Player.userId` and uses `DISTINCT ON ("userId") ... ORDER BY "userId", "idUploadedAt" DESC` for the multi-Player-per-User defense; write paths assert `tx.user.update` block carries the URLs while `tx.player.update` does NOT (regression target — restoring the old write target would re-introduce the per-league split); read paths assert `getLeaguePlayers` exposes `idDataByPlayerId`, the players page consumes `idDataByPlayerId[a.player.id]?.idFrontUrl` not `a.player.idFrontUrl`, the id-upload page reads `prisma.user.findUnique({ select: { idUploadedAt: true } })`, account/player reads `idUser?.idUploadedAt`, and `adminPurgePlayerId` resolves the User and writes via `prisma.user.update`). Updated [`tests/unit/idUploadActions.test.ts`](tests/unit/idUploadActions.test.ts) (rewritten to mock `prisma.user.findUnique` + `prisma.user.update`; happy paths assert writes go to User not Player; new no-op case for unlinked Player). Updated [`tests/unit/idUploadSchema.test.ts`](tests/unit/idUploadSchema.test.ts) — keeps the historical assertion that the v1.35.0 migration file is purely additive (still true on disk; that file is unchanged), drops the schema-state assertion that referenced the now-moved Player columns. Updated [`tests/unit/v164_application_workflow.test.ts`](tests/unit/v164_application_workflow.test.ts) and [`tests/unit/v165_1_dual_write_state_d.test.ts`](tests/unit/v165_1_dual_write_state_d.test.ts) for the new 7-element tuple shape from `getLeaguePlayers`. Updated [`tests/unit/adminLineDataJoins.test.ts`](tests/unit/adminLineDataJoins.test.ts) — `prisma.user.findMany` mock added; default `[]`. Updated [`tests/unit/identityUnificationAudit.test.ts`](tests/unit/identityUnificationAudit.test.ts) — tightened the "no standalone tx.user.update({...playerId})" regex to require `playerId` as the FIRST data key (matches the pre-λ regression target shape exactly; post-v1.70.0 the file does carry `tx.user.update` blocks for ID image writes which the looser regex would have falsely matched). 2043 passed | 2 skipped. Type-check clean. Stash-pop sanity check confirmed: reverting just the source changes makes 14/20 of the new v170 cases fail (regression target catches the broken state). Minor bump (1.69.1 → 1.70.0) — schema migration that drops three columns from Player + meaningful behavior change to the read/write paths. **Out of scope (deferred):** any UI surface that lets users *replace* their ID directly (admin still purges + user re-uploads through the existing `/join/[code]/id-upload` flow when needed); migration of `User.image` ↔ `Player.pictureUrl` consolidation (still queued at "stage 4" in the auth chain).
>
> v1.61.0 — **Self-link gate unified: any authenticated session (LINE / Google / email) can claim a roster slot via `/assign-player` when `League.allowSelfLink === true`.** User-reported reframe of v1.60.1's copy fix: "no the problem is why can't this user select to assign themselves to a player?" The actual bug was two-layered: (1) the v1.39.2 LINE-only gate on `/assign-player` page + API blocked Google/email users entirely, regardless of league setting; (2) the account menu dropdown's no-player branch surfaced a dead-end "Need an invite to join" message instead of the picker CTA, even when the league had self-linking enabled. **Provider type is no longer a gate. The toggle is.** v1.61.0 collapses the two-axis gate (provider + toggle) to one axis (toggle only). **Page gate change** ([`src/app/assign-player/page.tsx`](src/app/assign-player/page.tsx)): the v1.39.2 `NeedInviteSurface` early-return for `session && !session.lineId` is gone (regression target — `function NeedInviteSurface` and `data-testid="assign-player-need-invite"` removed entirely). The v1.60.0 `SelfLinkDisabledSurface` gate stays as the sole gate; it fires only when `allowSelfLink === false`. **API gate change** ([`src/app/api/assign-player/route.ts`](src/app/api/assign-player/route.ts)): POST + DELETE now accept any session with EITHER `lineId` OR `userId` — the 401 gate is `!session || (!session.lineId && !session.userId)`. Provider-specific write paths branch internally: LINE users continue on the v1.5.0 + v1.8.0 path (Redis-canonical synchronous write via `setMappingOrThrow` → Prisma deferred via `waitUntil`); non-LINE users (Google / email) take a synchronous Prisma transaction via two new helpers `persistAssignmentToPrismaForUser` + `persistUnassignmentToPrismaForUser` that wrap the v1.39.0 `linkUserToPlayer` (User.id-keyed) and the new v1.61.0 `unlinkUserFromPlayer` helper. **Why synchronous Prisma for non-LINE users:** for LINE users the JWT callback's read path hits Redis-canonical so the deferred Prisma write doesn't matter for read-your-own-writes. Non-LINE users have no Redis-canonical store today; the JWT callback resolves their `playerId` via Prisma `User.playerId @unique`. If the Prisma write deferred via `waitUntil`, the immediate `await update()` on the client (next-auth refreshSession) would race the deferred write and surface stale (orphan) state in the session. Cost: ~50-300ms warm and 1-3s cold per link, paid only by non-LINE users (a minority today). v1.62.0+ may add a userId-keyed Redis namespace to invert this back. **New helper** [`src/lib/identityLink.ts#unlinkUserFromPlayer(tx, { userId })`](src/lib/identityLink.ts) — mirror of `unlinkPlayerFromUser` but keyed on `User.id @unique` instead of `User.lineId @unique`. Returns `{ unlinkedPlayerId }` so the caller can drop downstream artifacts (picture-mirror Redis key etc.) without a second query. Non-LINE users typically have `User.lineId === null` so the legacy `unlinkPlayerFromUser` would no-op for them; this helper closes that gap. **JWT callback update** ([`src/lib/auth.ts`](src/lib/auth.ts)): the v1.58.0 `if (!token.lineId)` short-circuit (skip `getDefaultLeagueId` + `getPlayerMapping` for non-LINE sessions) is REVERTED in v1.61.0. The callback now always reaches `getDefaultLeagueId` (cached, ~ms warm) AND a new `getLeagueAllowSelfLink(leagueId)` read so `session.allowSelfLink` is populated for every session regardless of provider. New resolver `getPlayerMappingByUserId(userId, leagueId)` — same chain as `getPlayerMappingViaUser` but keyed on `User.id` — runs in the non-LINE branch so `session.playerId` populates for Google/email users post-link. **Cost on the non-LINE auth path:** one extra cached read (`unstable_cache(getLeagueAllowSelfLink, 30s, ['leagues'])`) and one Prisma round-trip per JWT callback for non-LINE users WITH a User.playerId binding. Pre-link non-LINE users still fall through quickly (User has no playerId, the resolver returns null without hitting Player). **`session.allowSelfLink` surfaced** ([`src/types/next-auth.d.ts`](src/types/next-auth.d.ts)) so the account menu can render the right CTA without a fetch. **Account menu** ([`src/components/LineLoginButton.tsx`](src/components/LineLoginButton.tsx)): the v1.39.2 `hasLine` derived flag is gone; replaced by `allowSelfLink = session.allowSelfLink !== false` (default-true on missing field). The dropdown's no-player branch flips to `needsSetup ? (allowSelfLink ? <picker CTA> : <need-invite>) : ...` — provider-agnostic. The linked-player branch's "Change/Unassign player" link drops the v1.39.2 `hasLine && (...)` gate and shows for any linked session; clicking lands on `/assign-player` which either renders the picker (allowSelfLink === true) or the SelfLinkDisabledSurface (false). The `AssignModal` first-login popup gates on `allowSelfLink && !playerId && (lineId || userId)` instead of `lineId && !playerId` — popups only fire when the user can actually act on them. **`linkedPlayers.ts` extension** — `getLinkedPlayerIds(viewer: { lineId, userId } | null)` now filters players linked via EITHER `lineId` OR `userId`, with the viewer's own bindings excluded so they can re-confirm or unassign. Pre-v1.61.0 the helper only checked `lineId`. **Tests:** rewritten [`tests/unit/assignPlayerNonLineGate.test.ts`](tests/unit/assignPlayerNonLineGate.test.ts) (16 cases pinning v1.61.0 contracts: `NeedInviteSurface` GONE, API 401 gate accepts lineId or userId, branching write paths preserved, account menu gates by `allowSelfLink` not `hasLine`, modal useEffect gates on `allowSelfLink`); updated [`tests/unit/adminLoadPerf.test.ts`](tests/unit/adminLoadPerf.test.ts) for the v1.58.0 short-circuit revert (4 new structural cases: getDefaultLeagueId always reached, getPlayerMappingByUserId reached on non-LINE branch, getLeagueAllowSelfLink + token.allowSelfLink surfaced); updated [`tests/unit/leagueSelfLinkToggle.test.ts:201`](tests/unit/leagueSelfLinkToggle.test.ts) ordering assertion (v1.39.2 NeedInviteSurface gate removed); new [`tests/unit/linkedPlayers.test.ts`](tests/unit/linkedPlayers.test.ts) (7 cases pinning the v1.61.0 viewer-exclusion seam — both lineId and userId NOT clauses, OR-shape lineId/userId filter, defensive empty-Set on Prisma rejection); new cases on [`tests/unit/identityLink.test.ts`](tests/unit/identityLink.test.ts) for `unlinkUserFromPlayer` (4 cases — null on no-User, clears both pointers, idempotent, keys lookup on User.id). Stash-pop sanity check confirmed: reverting just the page edit makes 2/16 of the new test fail (regression target catches the broken state). 1579 passed | 2 skipped (1572 baseline + 11 — 16 rewritten/4 added/4 added/7 new minus 16 obsolete v1.39.2 cases minus net-new structural reshuffles). Type-check clean. Minor bump (1.60.1 → 1.61.0) — provider gate change is a meaningful behavior shift (Google/email users gain a feature they didn't have); JWT callback semantics change (allowSelfLink now on session; non-LINE users now get playerId resolved on every callback). **Out of scope:** Redis caching for the userId resolver path (deferred to v1.62.0); admin UI to surface "X non-LINE users linked" stats (no admin pain point yet).

> v1.67.2 — **Critical bug fix: drop the v1.67.0 synthetic-invite State C path; new `/recruit/[slug]` route for user-initiated registration.** User-reported: clicking the State C recruiting CTA showed "This invite has been used. It's been redeemed 1 of 1 allowed times." The v1.67.0 [`recruitToLeagueWithOnboarding`](src/app/api/recruiting/actions.ts) action created a synthetic PERSONAL `LeagueInvite` with `usedCount = maxUses = 1` (intent: "pre-redeem so it can't be reused") then routed to `/join/<code>` expecting the page's existingBinding-detection branch to forward to `/onboarding`. **Two bugs:** (1) [`validateInvite`](src/lib/joinValidation.ts) rejects `usedCount >= maxUses` as `'used-up'` BEFORE the existingBinding branch can fire — every State C user landed on the error surface; (2) the empty `Player { name: null }` + PLM(PENDING) rows persisted in prod regardless, leaving orphan applications in the admin view. **v1.67.2 design:** NO synthetic invite. NO Player or PLM upfront. The State C CTA navigates to a new `/recruit/[slug]` route which renders an empty form; on submit, the existing [`applyToLeague`](src/app/api/recruiting/actions.ts) action creates Player + PLM(PENDING) atomically with the user-supplied data — same atomicity contract as admin invites, no invite gymnastics. **New route** [`src/app/recruit/[slug]/page.tsx`](src/app/recruit/[slug]/page.tsx) — server component, gates on (a) league exists, (b) league is recruiting, (c) session present, (d) session is NOT admin-credentials, (e) user has NO existing Player (true State C). Each rejected gate renders a friendly inline surface (404 / not-recruiting / sign-in / admin-session). State A/B/D users (already have a Player) redirect to `/id/<slug>` where the existing RecruitingBanner shows the right surface for their state. **New form** [`src/app/recruit/[slug]/RegistrationForm.tsx`](src/app/recruit/[slug]/RegistrationForm.tsx) — `'use client'`, mirrors the field shape of [`OnboardingForm`](src/app/join/[code]/onboarding/OnboardingForm.tsx) (name + position with the same five-option enum including "Prefer not to say"). Client-side validation (trim + non-empty + ≤100 chars) before calling `applyToLeague`; server-side validation in `applyToLeague` is the load-bearing gate. On success → `router.push('/id/<slug>')` so the apex banner shows State B "Application submitted". **`recruitToLeagueWithOnboarding` removed** — function body deleted from [`src/app/api/recruiting/actions.ts`](src/app/api/recruiting/actions.ts), replaced by an explanatory doc comment about why. Imports `generateInviteCode` + `computeInviteExpiry` removed. **`RecruitingBanner` updated** — drops the `recruitToLeagueWithOnboarding` import, drops `useTransition` (no async work on the click handler), accepts a new `leagueSlug?: string` prop (defaults to `DEFAULT_LEAGUE_SLUG`); State C handler flips from `recruitToLeagueWithOnboarding({...})` to `router.push('/recruit/<slug>')`. Stale "Starting your application…" pending UI removed (navigation is instant). Dashboard threads `leagueSlug` through to RecruitingBanner. **Orphan cleanup** — new [`scripts/cleanupV167SyntheticInviteOrphans.ts`](scripts/cleanupV167SyntheticInviteOrphans.ts) one-time script: matches the synthetic-invite signature (`PERSONAL` + `maxUses=1` + `usedCount=1` + `skipOnboarding=false` + `targetPlayer.name === null` + `createdById === Player.userId`); `--dry-run` (default) reports per-row; `--apply` deletes in this order per orphan: clear `User.playerId` → delete `LeagueInvite` → deleteMany PLM → delete Player. Order matters — `User.playerId @unique` and `Player.userId @unique` mean we clear the User pointer FIRST so neither unique trips during deletion. Idempotent. **Operator follow-up:** run `npx tsx scripts/cleanupV167SyntheticInviteOrphans.ts --dry-run` against prod to review orphans, then `--apply` to delete. **Tests:** new + rewritten in [`tests/unit/v167_planned_roster.test.ts`](tests/unit/v167_planned_roster.test.ts) — the v1.67.0 "State C full-onboarding flow" describe block is replaced by v1.67.2 contracts: `recruitToLeagueWithOnboarding` is gone (regression target — re-introducing it would re-introduce the bug); `computeInviteExpiry`/`generateInviteCode` no longer imported by recruiting/actions; RecruitingBanner navigates to `/recruit/<slug>` with the threaded `leagueSlug` prop; new `/recruit/[slug]/page.tsx` exists with all five gate testids; new RegistrationForm uses `applyToLeague` (NOT a synthetic-invite action) and lands on `/id/<slug>`; orphan-cleanup script matches the synthetic signature, only deletes `name === null` rows, clears `User.playerId` BEFORE `Player.delete`, defaults to dry-run. Existing v1.64.0 banner-state test updated for the v1.67.2 navigation shape. 1938 passed | 2 skipped (1929 baseline + 9 new contracts; some v1.67.0 cases removed/replaced). Type-check clean. Patch bump (1.67.1 → 1.67.2) — bug fix + new route, no schema change. **Out of scope (deferred):** ID upload + profile pic on the new registration form (the user's brief mentioned these but the minimal viable fix is name + position via the proven `applyToLeague` contract; ID upload can land as `/recruit/[slug]/id-upload` in a follow-up using the existing Blob plumbing); the duplicate position-enum literal in `RegistrationForm.tsx` + `OnboardingForm.tsx` could be extracted to a shared module but the cost/value tradeoff doesn't favor it at two call sites.

> v1.67.1 — **Player fee row in PlannedRosterStats panel + diagnosis of Stefan apex-vs-preseason confusion.** Two-fer patch. **(1) Fee row.** [`src/lib/plannedRosterStats.ts`](src/lib/plannedRosterStats.ts) extends the `PlannedRosterStats` interface with `defaultFee: number` + `positionFees: PlannedRosterPositionFee[]`; the data fetcher selects them from `League` + `LeaguePositionFee[]` in the existing `Promise.all`. Position rows whose fee matches `defaultFee` are filtered out at the data layer (rendering them would be informational noise — the default already covers them); remaining rows are sorted by position string for deterministic UI order. [`src/components/PlannedRosterStats.tsx`](src/components/PlannedRosterStats.tsx) gains a new `data-testid="player-fee-row"` block rendered below the registration deadline: primary line `Player fee` + `formatJpyFee(defaultFee)` (e.g. "¥4,000"), inline mono subtitle "(GK – ¥5,000)" per non-default position, and an explanatory line "Player fee is used to pay referee volunteers and league management work." The fee row hides when `defaultFee === 0` AND `positionFees.length === 0` (matching the panel's existing hide-empty pattern). The whole-panel hide rule extends to include `!showFee` so an empty league still renders nothing. **(2) Stefan apex-vs-preseason diagnosis.** User-reported: Stefan (admin + member) sees Classic League Homepage with "YOUR TEAM MARINERS FC" badge while another account (member of the same preseason league) sees the preseason view (CompressedMatchdaySchedule + PlannedRosterStats). Diagnosis is **NOT a code bug, NOT an admin-orthogonality violation**: `getLeagueFlags(leagueId)` is purely deterministic per leagueId and `getDefaultLeagueId()` returns the league with `isDefault: true` (no user context); Stefan's `session.teamId = mariners-fc` is a T9L 2026 Spring team and `UserTeamBadge` only renders when the team is in the rendered league's roster, so Stefan IS rendering against T9L (the default league, `preseasonMode=false`) — i.e. apex `/`. The other user must be visiting `/id/<preseason-slug>` directly. The apex `/` always serves the default league regardless of session — that's by design (per v1.53.0 subdomain teardown). **No behavioral change shipped this PR** for the apex routing — invasive for a patch. Recommended follow-up options if Stefan wants apex to be more user-aware: (a) auth-aware redirect (when user has APPROVED membership in a recruiting/preseason league AND the default league has neither flag, redirect apex → `/id/<their-league>`); (b) prefer preseason/recruiting leagues as a tiebreaker in `getDefaultLeagueId`; (c) add a "you're viewing the X league — switch to Y" UX hint. (a) is the cleanest if confirmed; (b) changes behavior for unauthenticated visitors too; (c) is safest. Confirm with Stefan which they want before shipping. **Tests:** new [`tests/unit/v167_1_player_fee_row.test.ts`](tests/unit/v167_1_player_fee_row.test.ts) (14 cases — interface declares `defaultFee` + `positionFees: PlannedRosterPositionFee[]`; helper selects defaultFee + positionFees on the league query, filters position rows whose fee equals defaultFee, sorts deterministically; component imports `formatJpyFee`, declares `showFee` gate, includes it in the all-empty hide rule, renders `player-fee-row` testid, includes the explanatory copy, renders the position-row subtitle shape `({p.position} – {formatJpyFee(p.fee)})`). Existing [`tests/unit/v167_planned_roster.test.ts`](tests/unit/v167_planned_roster.test.ts) still passes (41 cases — the v1.67.0 contracts are untouched). 1929 passed | 2 skipped. Type-check clean. Patch bump (1.67.0 → 1.67.1) — additive UX surface + diagnosis doc; no schema change, no contract change to existing props.

> v1.60.1 — **Copy fix: drop the redundant "Need an invite link" subtitle from the non-LINE no-player account-menu dropdown.** User-reported: as a guest-account session (Google/email auth, no Player linked), opening the header account menu (`LineLoginButton.tsx` dropdown) showed two near-identical stacked headers — eyebrow `Signed in as guest` + line `Need an invite to join` (the section header), then immediately below a bold `Need an invite link` subtitle, then `Ask an admin or vitoriatamachi@gmail.com.` The bold subtitle restated the section header above with no new information; in the dropdown's constrained vertical width, two headers stacked back-to-back read as a copy-paste mistake. Pre-v1.60.1 body block: `<p className="font-bold text-fg-high mb-1">Need an invite link</p><p>Ask an admin or vitoriatamachi@gmail.com.</p>`. v1.60.1 drops the bold `<p>` line entirely; body collapses to the existing one-line `Ask an admin or vitoriatamachi@gmail.com.` (linkified mailto unchanged). The eyebrow + section header (line 381–383, conditional on `!hasLine`) carries the only `Need an invite` string the user sees; the body is pure call-to-action with the contact path. The `data-testid="account-menu-need-invite"` block + `mailto:vitoriatamachi@gmail.com` link stay. **"Signed in as guest" sanity check:** the user's report flagged it as a possible display-name fallback. Audit confirms it's a static string in the eyebrow position, NOT a fallback for a missing display name — the `needsSetup` branch at [`LineLoginButton.tsx:381`](src/components/LineLoginButton.tsx) renders the literal string regardless of session shape; the logged-in branch at line 388–389 renders `session.playerName` directly via the `Playing as` block. No bug — "Signed in as guest" is intentional copy meaning "you're authenticated but haven't claimed a player yet." **Surface scope:** grep across `src/` for `Need an invite` confirmed the duplication exists only in [`LineLoginButton.tsx`](src/components/LineLoginButton.tsx); the `/assign-player` non-LINE gate ([`src/app/assign-player/page.tsx`](src/app/assign-player/page.tsx)) uses different copy that isn't internally duplicated; the `/account/player` admin-session friendly surface ([`src/app/account/player/page.tsx`](src/app/account/player/page.tsx)) renders separately and isn't affected. Other surfaces left untouched per the user's "only touch them if they're actually duplicated in the same way" directive. **Tests:** updated existing [`tests/unit/assignPlayerNonLineGate.test.ts:134`](tests/unit/assignPlayerNonLineGate.test.ts) — `Need an invite` substring assertion against the body block is replaced with three pinned assertions: header anywhere in the file carries `Need an invite to join` (eyebrow regression target — moving it back inside the block would re-introduce the duplicate); body block (post-`account-menu-need-invite` testid) carries `Ask an admin or` + `mailto:vitoriatamachi@gmail.com`; body block does NOT carry `Need an invite link` (regression target — the redundant subtitle must not reappear). Stash-pop sanity check confirmed: reverting just the [`LineLoginButton.tsx`](src/components/LineLoginButton.tsx) edit makes the new "must NOT match Need an invite link" assertion fail. Patch bump (1.60.0 → 1.60.1) — UX copy polish on existing component, no contract change to the dropdown shape, account-menu testid, mailto path, server actions, or any other surface.

> v1.60.0 — **Per-league `allowSelfLink` toggle: admins can disable the legacy `/assign-player` open picker on a league-by-league basis.** User-driven design pivot for multi-tenant maturity. Pre-v1.60.0 the `/assign-player` route was unconditionally available for any LINE-authenticated visitor against the default league — every roster slot in the default league was claimable by the first LINE user to click it. The user-facing decision: each league should be able to opt out of open self-linking and require admin-issued invite codes (the v1.34.0 `/join/[code]` redemption flow) to claim a slot. **Schema:** one additive column `League.allowSelfLink Boolean @default(true)` (purely additive — every existing league behaves exactly as today; admin flips to false to opt out). The migration `20260505100000_league_allow_self_link` is `ALTER TABLE "League" ADD COLUMN "allowSelfLink" BOOLEAN NOT NULL DEFAULT true` — no DROP, no ALTER COLUMN against existing data, no destructive backfill. **New helper** [`src/lib/leagueSelfLink.ts#getLeagueAllowSelfLink(leagueId)`](src/lib/leagueSelfLink.ts) — `unstable_cache`-wrapped Prisma read with 30s TTL under the canonical `'leagues'` tag (admin writes via the v1.16.0 `revalidate({ domain: 'admin' })` helper bust this automatically). Defaults to `true` on missing league row OR Prisma rejection — the read is the affordance, the API gate is the safety net. **Page gate** [`src/app/assign-player/page.tsx`](src/app/assign-player/page.tsx) — new `SelfLinkDisabledSurface` component fires AFTER the v1.39.2 non-LINE gate but BEFORE `getLinkedPlayerIds` / picker render. Copy points users at the admin invite-link path forward + mailto fallback (`vitoriatamachi@gmail.com`); `data-testid="assign-player-self-link-disabled"` for assertion. **API gate** [`src/app/api/assign-player/route.ts`](src/app/api/assign-player/route.ts) — POST handler returns 403 with `error: 'Self-linking is disabled for this league'` when the toggle is off; gate fires after leagueId resolution but before `getPlayerByPublicId` so disabled leagues don't pay the validation read. **DELETE handler intentionally NOT gated** — load-bearing semantic: existing linked players must always be able to unbind themselves regardless of the toggle (the toggle only controls NEW links). **New server action** [`setLeagueAllowSelfLink({ leagueId, value })`](src/app/admin/leagues/actions.ts) — `assertAdmin` + `typeof value === 'boolean'` validation + `prisma.league.update` + canonical `revalidate({ domain: 'admin', paths: [.../settings, ..., '/admin'] })` bust. **Admin UI** [`SettingsTab.tsx`](src/components/admin/SettingsTab.tsx) — new "Player self-linking" section between "Public site source-of-truth" and "Season Actions"; mirrors the existing data-source / write-mode toggle pattern (optimistic flip + rollback on rejection); both On/Off buttons disabled while saving; testids `settings-tab-self-link-section` / `settings-tab-self-link-on` / `settings-tab-self-link-off`. The v1.23.0 `getPublicLeagueData(leagueId)` already filters players to the resolved league via the PLA join, so when the toggle is ON the picker shows unlinked players from THAT league only — global cross-league bleed is impossible by construction. **Tests:** new [`tests/unit/leagueSelfLinkToggle.test.ts`](tests/unit/leagueSelfLinkToggle.test.ts) (34 cases — schema declares the field with default true (regression: default-false would silently disable every league at deploy); migration is purely additive (no DROP/ALTER COLUMN/SET-NOT-NULL/TRUNCATE/DELETE-FROM in the executable SQL, comment-stripping helper handles legitimate rollback-recipe mentions); helper exports under canonical `leagues` tag with the right defensive defaults; page imports the helper, renders SelfLinkDisabledSurface, gate fires AFTER non-LINE gate AND BEFORE getLinkedPlayerIds (regression target — wrong order would either let LINE-disabled-league users see picker or show non-LINE users the wrong message); API POST returns 403 with right copy, fires after leagueId resolution but before getPlayerByPublicId, DELETE handler MUST NOT call getLeagueAllowSelfLink (regression target); server action exists with the right signature, gates on assertAdmin, validates boolean type, writes to prisma.league.update with allowSelfLink in data, busts canonical admin cache; SettingsTab imports the action, has the testids, uses optimistic-flip-with-rollback shape, disables both buttons during save). 1560 passed | 2 skipped (1526 baseline + 34 new). Type-check clean. Local `node scripts/build.mjs` clean (with placeholder env). Stash-pop sanity check: reverting just the implementation files makes the test file fail to even load (migration.sql gone) — every assertion is a load-bearing regression target. Minor bump (1.59.1 → 1.60.0) — new admin write capability + new admin UI surface + schema addition. **Out of scope:** the modal popup in `LineLoginButton.tsx` (`AssignModal`) and the dropdown CTAs (`/assign-player` links) continue to point at the route — when self-link is OFF, users still hit the route and see the friendly message. Suppressing the popup itself client-side would require threading the flag into the session/JWT (not warranted at v1 — the route gate is the load-bearing surface). When operators want to fully hide the modal nag for a self-link-disabled league, that's a follow-up PR. The `getDefaultLeagueId()` resolution that drives `/assign-player` continues to point at the default league per v1.53.0 — multi-league self-link toggling at non-default leagues is exposed via the admin UI but only takes effect when those leagues become reachable via `/assign-player` (today only the default league is reachable; the apex page renders directly via Dashboard for non-default leagues without going through `/assign-player`).
>
> v1.59.2 — **Bug fix: `/account/player` hydration crash — moved client-side constants out of the `'use server'` actions file.** User-reported via "User reports v1.59.1 didn't fix the bug. They still get 'This page couldn't load. Reload to try again, or go back.' on /account/player after signing in via LINE as Stefan S." **Root cause (pre-existed v1.37.0; only surfaced once v1.59.1 fixed the over-aggressive admin-shell gate):** `PROFILE_PIC_MAX_BYTES` and `PROFILE_PIC_ALLOWED_TYPES` were `export const`s from [`src/app/account/player/actions.ts`](src/app/account/player/actions.ts) — a `'use server'` file. Next.js 16 converts EVERY export from a `'use server'` file into a server-action proxy on the client side via `createServerReference(...)`. The actual values never reach the browser. The compiled client bundle confirmed it: `let l = (0,n.createServerReference)("7f9584af...", n.callServer, ..., "PROFILE_PIC_ALLOWED_TYPES")` — `l` is a function, not the array. So [`AccountPlayerForm.tsx`](src/app/account/player/AccountPlayerForm.tsx) at line 207 (`accept={PROFILE_PIC_ALLOWED_TYPES.join(',')}`) called `.join` on a function during client render and threw `TypeError: PROFILE_PIC_ALLOWED_TYPES.join is not a function`. React's hydration crashed; Next 16's default global-error UI rendered with the exact text "This page couldn't load. Reload to try again, or go back." (sourced verbatim from [`node_modules/next/dist/client/components/builtin/global-error.js`](node_modules/next/dist/client/components/builtin/global-error.js)). **Why prod logs showed 200:** server-side render works because the constants are real on the server (the `'use server'` directive only matters for the client bundle's view of the module). The 200 HTML response was correct — the crash happened during client hydration. **Why v1.59.1's tests didn't catch it:** the v1.59.1 tests are structural, asserting the page source shape — they don't exercise client hydration at all. **Why this only surfaced with v1.59.1:** v1.37.0 → v1.59.0 had a broken admin-shell gate (`if (!userId) → render "Admin sessions can't edit here"`) that rejected most users before they ever reached the form. v1.59.1 fixed the gate; Stefan reached the form for the first time; the latent hydration crash surfaced. **Fix:** new [`src/app/account/player/validation.ts`](src/app/account/player/validation.ts) — plain module (no `'use server'` directive) exporting the two constants. Both `actions.ts` and `AccountPlayerForm.tsx` now import from `./validation`. The compiled client bundle now contains the real array `["image/jpeg","image/png","image/webp"]` inlined, not a server-reference proxy. **New standing rule (added to validation.ts docstring):** never `export const` (or any non-async value) from a file with `'use server'` at the top. Server-action files are async-functions-only. Constants, types, and interfaces shared between server actions and client components live in a separate neutral module. **Tests:** new [`tests/unit/accountPlayerValidationModule.test.ts`](tests/unit/accountPlayerValidationModule.test.ts) (7 cases — validation.ts has NO `'use server'` directive (regression target); validation.ts exports both canonical constants; actions.ts does NOT export them (regression target — re-exporting would re-introduce the bug); actions.ts imports from `./validation`; AccountPlayerForm imports from `./validation`, NOT from `./actions` (regression target — the `import {PROFILE_PIC_*} from './actions'` shape is what broke hydration); runtime sanity — `PROFILE_PIC_ALLOWED_TYPES` is an array with `.join`, `PROFILE_PIC_MAX_BYTES` is a number with arithmetic semantics). Verified regression coverage by temporarily reintroducing the broken state (re-adding the const exports to actions.ts, flipping the form import back to `./actions`) — 2 of the 7 structural cases failed (the actions.ts re-export check + the form-imports-from-validation check), confirming the test catches the broken state. After restoring the fix, 1533 passed | 2 skipped. Type-check clean. Local `next build` clean; bundle inspection confirms the constants are now inlined as real values (not server-reference proxies). Patch bump (1.59.1 → 1.59.2) — bug fix, no contract change to public APIs or downstream consumers.
>
> v1.59.1 — **Bug fix: `/account/player` now accepts LINE-auth users (including LINE-auth admins like Stefan S) whose JWT has no `userId`.** User-reported: "I'm accessing Stefan S who we have configured to have a Line ID that gets admin role by default. Through this player's UI I'm not allowed to open account/player." Pre-v1.59.1 [`src/app/account/player/page.tsx`](src/app/account/player/page.tsx) gated on `if (!userId)` and rendered an "Admin sessions can't edit here" message for any session without `session.userId`. But `userId` is only set on JWTs minted post-v1.28.0 (when the PrismaAdapter started creating User rows for LINE sign-ins). LINE users whose sessions predate v1.28.0 (or whose User row's `lineId` linkage drifted) carry a `lineId`-only JWT — the gate incorrectly bucketed them with admin-credentials sessions. Stefan S's case is the canonical example: he's a real player (Player.lineId matches his LINE OAuth subject; Player on a team roster) who ALSO has admin role (his LINE ID is in `ADMIN_LINE_IDS`). Admin role is orthogonal to "have a linked Player" — the page should accept anyone who can resolve to a Player record. **Fix:** the gate now resolves the linked Player via `userId` first (canonical post-α.5 binding from PR β / v1.29.0 dual-write), falling back to `lineId` (legacy pre-v1.28.0 binding via `Player.lineId @unique`). Both identifiers are minted by the auth server, so either is a safe lookup key. Looking up by `session.playerId` (slug) alone is unsafe — admin remap can leave the slug stale relative to the canonical `userId`/`lineId` binding. The branches are now: (1) no session → redirect to /auth/signin; (2) no userId AND no lineId (truly admin-credentials) → friendly "Admin-only sessions can't edit here" message with link to /admin; (3) Player resolves via userId or lineId → render edit form; (4) authenticated but Player.userId AND Player.lineId both miss → friendly "no player linked yet" message (covers both Google/email lurkers who haven't redeemed an invite AND any LINE user with a session-but-no-binding edge case). **Same shape applied to `/account/player`'s server actions** ([`src/app/account/player/actions.ts`](src/app/account/player/actions.ts)) — `requireSelfPlayerSession` now requires `userId || lineId` (not just userId); `resolveOwnedPlayerId` tries `userId` first then falls back to `lineId`; the previous `playerId` presence gate is dropped (the gate is now whether the Player resolves, not whether the session carries the slug — session.playerId can be stale post-admin-remap). **Tests:** new [`tests/unit/accountPlayerPageGate.test.ts`](tests/unit/accountPlayerPageGate.test.ts) (9 cases — page MUST NOT early-return on `!userId` alone (regression target); page reads `session.lineId`; page has `where: { lineId }` fallback findUnique; page still gates admin-credentials with combined `!userId && !lineId`; page preserves `where: { userId }` first attempt; "Admin sessions can't edit here" copy is gone (replaced with "Admin-only sessions can't edit here" — distinct trigger); actions.ts requires `userId || lineId`; actions.ts no longer requires session.playerId; actions.ts has lineId fallback in `resolveOwnedPlayerId`). [`tests/unit/accountPlayerActions.test.ts`](tests/unit/accountPlayerActions.test.ts) updated for the new contract — 4 new cases (LINE-only session resolves Player via lineId fallback; userId-first preference when both are present; falls back to lineId when userId lookup misses; admin-credentials with both null still throws); 1 removed (`'throws when authenticated lurker has no playerId'` — that gate was removed because the lookup is now via Player, not session.playerId presence). 1526 passed | 2 skipped (1515 baseline + 11 net new — 9 page gate + 4 actions − 1 removed playerId gate). Type-check clean. Patch bump (1.59.0 → 1.59.1) — bug fix on existing UX, no contract change to props or downstream APIs. **Why this surfaces as "This page couldn't load" on prod:** the original user-facing report described a generic "couldn't load" surface; the user followup clarified that the friendly "Admin sessions can't edit here" page IS what's blocking access. Either the user paraphrased the message or there's a separate generic error path being caught by Next's error boundary; the gate fix addresses the actual access-blocker either way. **Admin (Credentials) access NOT addressed in this PR:** admins authenticated via username/password (admin-credentials provider) have no auth-provider link to any Player — they manage players via /admin/leagues/[id]/players. The user clarified Issue #2 as "less critical" and explicitly scoped this PR to the LINE-auth admin case (Stefan S).
>
> v1.59.0 — **Perf pass: streaming loading skeletons for public routes + SSR-hydrated league switcher (no more `/api/me/memberships` round-trip).** User-reported: "the switch league button takes way too long to load for a multi league user" + "when I click stats, or click Assign player, it takes 1-2s for the page to change." Two root causes diagnosed via code audit; both fixed. **(1) No `loading.tsx` for any public route.** Pre-v1.59.0 only `/admin/...` had `loading.tsx`; navigation to `/`, `/stats`, `/assign-player`, `/schedule`, `/id/[slug]`, `/id/[slug]/md/[id]` blocked until the entire RSC payload resolved server-side (sequential `getDefaultLeagueId` + `getPublicLeagueData` + per-route extras = ~1-2s warm and worse on cold Neon). The `<Link>` click fired but Next held the navigation in flight until the new RSC payload arrived. v1.59.0 adds [`src/app/loading.tsx`](src/app/loading.tsx) for the apex Dashboard + matching skeletons under [`src/app/stats/loading.tsx`](src/app/stats/loading.tsx), [`src/app/schedule/loading.tsx`](src/app/schedule/loading.tsx), [`src/app/assign-player/loading.tsx`](src/app/assign-player/loading.tsx), [`src/app/id/[slug]/loading.tsx`](src/app/id/[slug]/loading.tsx), [`src/app/id/[slug]/md/[id]/loading.tsx`](src/app/id/[slug]/md/[id]/loading.tsx). Each renders a route-shaped pulse skeleton (header band at the fixed `h-12` height + content blocks roughly matching the post-render layout) so the navigation flips immediately and the swap-in is minimal-flash. The legacy redirect routes (`/[slug]`, `/league/[slug]`, `/league/[slug]/md/[id]`, `/matchday/[id]`) don't get loading files because Next.js doesn't render `loading.tsx` for 308-redirects. **(2) League switcher hidden until `/api/me/memberships` round-trip returns.** Pre-v1.59.0 [`LeagueSwitcher.tsx`](src/components/LeagueSwitcher.tsx) and [`AccountMenuLeagueSwitch.tsx`](src/components/AccountMenuLeagueSwitch.tsx) lazy-loaded memberships on mount via the public API route — for a multi-league user the chevron + "Switch league" entry didn't appear for ~300ms-1s after the page painted (cold-Vercel-lambda + Prisma round-trip). v1.59.0 hoists the fetch into the root layout: new pure helper [`src/lib/memberships.ts#getMembershipsForSession`](src/lib/memberships.ts) runs server-side after `getServerSession()` and threads the result through new [`MembershipsProvider`](src/components/MembershipsProvider.tsx) context. Both client components now read from `useMemberships()` synchronously — visibility decision happens on first paint, no flash. The `/api/me/memberships` route stays as a refresh path (delegates to the same helper) so any future client that wants on-demand reload still works. The legacy `useLeagueMemberships` hook is preserved as a thin compat shim (returns the context value, `load()` is a no-op) so external consumers compile unchanged. **(3) Static-import `getDefaultLeagueId` in JWT callback.** Pre-v1.59.0 the LINE-branch of [`src/lib/auth.ts`](src/lib/auth.ts:739) did `await import("@/lib/leagueSlug")` on every callback. Module-cache amortizes this after first hit, but it still adds an extra microtask hop on the auth hot path. Flipped to a static import at file top — straight path, no request-scope rationale to defer it. **Tests:** rewritten [`tests/unit/leagueSwitcher.test.ts`](tests/unit/leagueSwitcher.test.ts) (24 cases pinning the v1.59.0 contracts: route delegates to shared helper, helper has the right Prisma shape + dedup + sort + defensive empty-on-error, `MembershipsProvider` is the standard React-context shape, `LeagueSwitcher` reads from context (regression target — `/api/me/memberships` literal string MUST NOT appear in the file post-v1.59.0; `loadedRef` is gone), `AccountMenuLeagueSwitch` same, root layout fetches + threads via provider). New [`tests/unit/memberships.test.ts`](tests/unit/memberships.test.ts) (10 behavioral cases — auth dispatch (userId vs lineId vs neither), shape (slug + isCurrent + DEFAULT_LEAGUE_SLUG fallback + drop non-default-with-null-subdomain + dedup + sort), defensive empty on Prisma rejection). New [`tests/unit/publicRouteLoadingSkeletons.test.ts`](tests/unit/publicRouteLoadingSkeletons.test.ts) (6 cases pinning every loading.tsx file existence — regression target: dropping any of the six files would re-introduce blocking navigation on that route). 1515 passed | 2 skipped (1491 baseline + 24 net new — 24 in leagueSwitcher rewrite + 10 new in memberships + 6 new in publicRouteLoadingSkeletons − 16 from the v1.52.0 LeagueSwitcher tests that contradict v1.59.0). Type-check clean. Minor bump (1.58.1 → 1.59.0) — meaningful UX behavior change (navigation feels instant; switcher trigger renders on first paint). **Out of scope:** dynamic `loading.tsx` skeletons that show real per-route shells (would need DB read in the loading file, which Next disallows); per-league cache-bust tags (the `unstable_cache` API requires static tags; per-league busting was evaluated and skipped in v1.23.0); admin-route nav perf (already covered by existing `/admin/.../loading.tsx` files in v1.20.x).
>
> v1.58.1 — **Vercel preview-build root-cause fix: closes the Neon-Vercel race that's been forcing admin-merge fallback on every PR in the v1.50–v1.58 chain.** Sixth and final PR of the route-shortening chain. User-flagged: "every time you push something, the preview build keeps failing." **Root cause:** the v1.50.0-era `npm run build` script was a flat `prisma migrate deploy && prisma generate && next build`. Both `prisma migrate deploy` AND `prisma generate`'s schema validation require the env var declared by `directUrl = env("DATABASE_URL_UNPOOLED")` in [`prisma/schema.prisma`](prisma/schema.prisma) to merely EXIST at parse time — Prisma fails P1012 when the var is unset. The Neon-Vercel marketplace integration provisions `DATABASE_URL` + `DATABASE_URL_UNPOOLED` per git branch, but the integration races with Vercel's first build trigger: small changes / fast pushes win the race against the integration, env vars don't exist yet, build dies. **Fix shape:** new [`scripts/build.mjs`](scripts/build.mjs) replaces the inline shell chain. The orchestrator: (1) detects missing `DATABASE_URL_UNPOOLED`; (2) substitutes a `postgresql://placeholder@localhost:5432/placeholder` so `prisma generate`'s schema validation parses cleanly (placeholder is scoped to the build process — does NOT leak to Vercel runtime, which loads its own env vars from project settings); (3) skips `prisma migrate deploy` when using the placeholder (no real DB to migrate against); (4) runs `prisma generate` + `next build` regardless. Production builds with real env vars take the original code path unchanged. **Why previous workarounds were wrong:** Option B (always-set DATABASE_URL_UNPOOLED on Vercel) would re-introduce the prod-creds-on-preview risk we removed earlier; Option C (Prisma fallback in schema) isn't supported by Prisma v5 syntax; Option D (use `prisma db push`) is destructive and risks dropping columns on prod. Option A (this fix) is non-destructive: production migrations still apply normally; preview builds gracefully degrade by skipping migrations (the per-branch DB is forked from the parent which already has migrations applied, so the integration handles migration via its own provisioning flow). **Tests:** new [`tests/unit/buildScript.test.ts`](tests/unit/buildScript.test.ts) (10 cases — package.json `build` script flips to `node scripts/build.mjs`; the legacy `prisma migrate deploy` chain is gone (regression target); script handles missing-env / placeholder-set / real-env cases; `prisma generate` + `next build` always run; `child_process.execSync` with `stdio: 'inherit'` so build logs surface in Vercel). Stash-pop sanity check: reverting `package.json` + `scripts/build.mjs` fails 10/10. 1491 passed | 2 skipped (1481 baseline + 10 new). Type-check clean. Local verification: `env -u DATABASE_URL_UNPOOLED node scripts/build.mjs` activates the placeholder branch, skips migrate-deploy, runs prisma generate cleanly. Patch bump (1.58.0 → 1.58.1) — build-pipeline-only change, no app behavior shift. **Closes the route-shortening chain (v1.54.0 → v1.58.1).** Updated CLAUDE.md "Known infra issues" → "Neon-Vercel preview env race (resolved in v1.58.1)" with the fix details. The first PR pushed AFTER this lands should build cleanly on Vercel preview without admin-merge fallback. **What shipped across the 6-PR chain:** v1.54.0 route shortening (every tenant URL under `/id/<slug>`); v1.55.0 admin UI compat audit (subdomain → URL slug labels); v1.56.0 Players tab cross-league linking + "Also in: <league>" chip; v1.57.0 `/admin/users` admin list; v1.58.0 `/admin` load perf; v1.58.1 Vercel preview-build root-cause fix.
>
> v1.58.0 — **`/admin` load perf: trim `getAllLeagues` projection + skip JWT league lookup for non-LINE sessions.** Fifth of the v1.54.0–v1.58.0 chain. User-reported: "investigate why it takes so long to load t9l.me/admin." Cold-page-load measurement on prod (unauth path, which 307-redirects to `/admin/login`): cold 1.24s warm 0.4s; signed-in admin path estimated to be similar magnitude on cold. Without prod admin credentials, the diagnosis is code-audit-driven; the fix targets two clear inefficiencies. **(1) `getAllLeagues()` projection trim.** Pre-v1.58.0 the cached query on the dashboard tile grid used a flat `include: { matches: true, venue: true }` on every gameWeek, fetching all ~15 fields of every Match row (homeScore/awayScore/scoreOverride/playedAt/endedAt/kickoff/createdAt/updatedAt/etc.) just to read `match.status` (the COMPLETED-everywhere check) and `matches.length` (the "X matches scheduled" copy). v1.58.0 flips to a minimal `select` projection: League → id/name/subdomain/endDate; GameWeek → weekNumber/startDate/venue.name; Match → status only. Wire-payload + Prisma serialization both shrink proportionally. Magnitude is small (T9L typically has 1–2 leagues × 8 GWs × 3 matches = 24–48 Match rows), but compounds on every cold-cache miss; warm-path hits the 30s `unstable_cache` so steady-state is unchanged. **(2) JWT callback short-circuit for non-LINE sessions.** Pre-v1.58.0 the callback always ran `await getDefaultLeagueId()` (cached but fires on every request) AND the `getPlayerMapping` block, regardless of whether the session even had a `lineId`. Google/email/admin-credentials sessions have no lineId — their player mapping is null by definition (until they redeem an invite); running the league + mapping lookup for them is wasted I/O. v1.58.0 adds an `if (!token.lineId)` early-branch that nulls all four fields (leagueId / playerId / playerName / teamId) and skips both lookups. LINE sessions go through the existing path unchanged. The biggest win is for admin-credentials sessions hitting `/admin` — the JWT callback now does NO Prisma or Redis work beyond the `isAdmin` env-var check. **Diagnosis approach used:** code audit + cold/warm prod TTFB sampling on the unauth `/admin` redirect path (since auth cred for direct measurement isn't available in this session). The two findings above were the only obvious issues in the hot-path stack (admin layout `getServerSession` → JWT callback → admin/page.tsx `getAllLeagues`); the rest of the path (Vercel cold-lambda spin-up, Next.js framework overhead, Upstash REST round-trip on warm Redis) is infrastructure-level and not addressable in app code. **Tests:** new [`tests/unit/adminLoadPerf.test.ts`](tests/unit/adminLoadPerf.test.ts) (7 cases — getAllLeagues uses `select:` not `include:`, `matches: true` is gone from the getAllLeagues block (regression target — would catch a future PR re-broadening the include), required field selection (id/name/subdomain/endDate), unstable_cache wrapper preserved with 30s + leagues tag; JWT callback `if (!token.lineId)` gate exists with correct token-null wiring, LINE branch still reaches getDefaultLeagueId + getPlayerMapping). Stash-pop sanity check confirmed: reverting `admin-data.ts` fails 3/7 cases. 1481 passed | 2 skipped (1474 baseline + 7 new). Type-check clean. Patch-or-minor judgment: minor bump because the JWT callback semantics change is observable (non-LINE sessions now have leagueId=null on session — pre-v1.58.0 they had the default leagueId but null playerId/teamId). The admin/page.tsx render output is byte-equivalent. Minor bump (1.57.0 → 1.58.0). **Out of scope (deferred to PR 6):** Vercel preview-build root-cause fix (the Neon-Vercel race that's been forcing admin-merge fallback on every PR in the v1.50–v1.58 chain). User explicitly added that as an additional task to slot after the chain.
>
> v1.57.0 — **`/admin/users` admin list: every User in the system with auth providers + linked Player + leagues membership.** Fourth of the v1.54.0–v1.58.0 chain. Adds the missing "what users do we have, what's their auth state" admin surface. **New route** [`src/app/admin/users/page.tsx`](src/app/admin/users/page.tsx) — server component, hard-gated to admin role (`!session?.isAdmin → redirect('/admin/login')`); fetches via `getAllUsersForAdmin()` and renders `<UsersList>`. **New data helper** [`getAllUsersForAdmin()`](src/lib/admin-data.ts) — single Prisma query selecting User + accounts.provider + linked Player + linked Player's active leagues + LineLogin.lastSeenAt; provider list deduped via Set + sorted for stable display; ISO-string serialization at the boundary (Date → string for cache safety). **New component** [`UsersList.tsx`](src/components/admin/UsersList.tsx) — client list with: avatar + display name + email + "Admin" pill (when role=ADMIN); auth-provider pills with LINE/Google/Email icons + labels; linked-Player cell with "Also in: <leagues>" context (or "No player linked" italic when null); last-LINE-seen timestamp via `formatJstDayMonth`; per-row Unlink action gated on the player being linked. Search filters by display name / email / linked-player name (case-insensitive substring); linked-state filter (All / Linked / Unlinked) trims the list to common admin chores like "show me users without players". Desktop grid layout (`40px 1fr 180px 200px 100px 80px`) + mobile cards layout (avatar + flex-row stack) — same data, two layouts. **New server action** [`adminUnlinkUserFromPlayer({ userId })`](src/app/admin/leagues/actions.ts) — clears the v1.27.0 dual-write `User.playerId` ↔ `Player.userId` 1:1 binding inside a single Prisma `$transaction` so partial-write states never survive. Idempotent (no-op when User.playerId is already null). Both rows survive — only the binding is cleared. Does NOT touch `Player.lineId` (legacy mirror; identity-rework stage 4 territory). Revalidates `domain: 'admin'` with the `/admin/users` path. **`AdminNav` integration** — new "Users" link added to both desktop horizontal nav and mobile drawer (between "All Players" and "Venues"). **Out of scope (not shipped):** User disable (would need a schema column — `User.disabledAt DateTime?` — deferred to a follow-up); Provider unlink (deleting an `Account` row — admin ergonomics tricky around what happens to a user with only one provider; deferred); Bulk operations on users (unlink-many / delete-many — also deferred). The "View linked Player" deep-link target is conservatively pointed at `/admin` since post-v1.56.0 there's no canonical "primary league for player X" route — admins typically know which league they're managing. **Tests:** new [`tests/unit/adminUnlinkUserFromPlayer.test.ts`](tests/unit/adminUnlinkUserFromPlayer.test.ts) (6 cases — happy path with both clears + revalidate; idempotent no-op on already-unlinked; rejection on missing User; rejection on empty userId; single-transaction atomicity; doesn't touch Player.lineId regression target). New [`tests/unit/adminUsersList.test.ts`](tests/unit/adminUsersList.test.ts) (24 cases — route admin gate + redirect + fetch + render; UsersList component shape including testids + filter logic + search predicates + admin-pill conditional render + provider labels; getAllUsersForAdmin Prisma include shape + dedup + LineLogin join + ISO serialization; AdminNav integration). Stash-pop sanity check confirmed: reverting just `AdminNav.tsx` fails 2/24 cases. 1474 passed | 2 skipped (1444 baseline + 30 new). Type-check clean. Minor bump (1.56.0 → 1.57.0) — new admin route + new admin write capability + new admin nav link. **Out of scope (later PRs in the chain):** PR 5 (v1.58.0) `/admin` load perf (TTFB diagnosis + targeted fix); PR 6 (v1.58.x) Vercel preview-build root-cause fix (Neon-Vercel race that's been forcing admin-merge fallback on every PR).
>
> v1.56.0 — **Players tab cross-league linking + per-row "Also in: <league>" differentiation cue.** Third of the v1.54.0–v1.58.0 chain. Adds the missing cross-league roster operation: an admin can attach an EXISTING global Player to a new league's roster (instead of creating a duplicate Player record per league). **New server actions** in [`src/app/admin/leagues/actions.ts`](src/app/admin/leagues/actions.ts): `adminLinkExistingPlayer({ leagueId, playerId, leagueTeamId, fromGameWeek? })` validates the leagueTeam belongs to the supplied league (cross-league isolation), validates the Player exists and isn't ALREADY active on this league's roster (no double-roster — admins use `transferPlayer` to move between teams within a league), creates a new `PlayerLeagueAssignment` row tagged `joinSource: 'ADMIN'` (same audit signal as `adminCreatePlayer`'s pre-stage flow), revalidates per the canonical helper. `adminLinkExistingPlayersBulk({ leagueId, items, fromGameWeek? })` runs the single-link action sequentially per item (bounded at 100 items per batch — same cap as the v1.33.0 bulk-invite action), returns per-row results so the dialog can surface partial-failure feedback. **New data helpers** in [`src/lib/admin-data.ts`](src/lib/admin-data.ts): `getLinkablePlayersForLeague(leagueId)` returns global Players NOT currently active on this league's roster, annotated with the names of OTHER leagues each is in (drives the dialog's candidate list); `getPlayerOtherLeaguesForLeague(leagueId)` returns the same context for players ALREADY on this league's roster (drives the per-row "Also in: <league>" differentiation cue). Both are uncached (small cardinality bounded by ~50 players today; admin writes already revalidate `domain: 'admin'` which busts the leagues tag — adding finer-grained cache keys would force a per-leagueId tag bust on every league-scoped admin write). **New component** [`LinkExistingPlayerDialog.tsx`](src/components/admin/LinkExistingPlayerDialog.tsx) — modal with search + per-row team-picker + per-row checkbox for bulk-select; mounts as a sibling toolbar action alongside `AddPlayerDialog`. Two flows: single-link (1 selected → calls `adminLinkExistingPlayer` for cleaner error surfacing) and bulk-link (>1 selected → calls `adminLinkExistingPlayersBulk` and surfaces success/partial-failure counts). Admin must pick a team for each selected player before submit (no league-wide team default since different humans go to different teams). Each candidate row shows position + an "Also in: <league names>" chip when the player has active assignments in other leagues, or "Not on any active roster" when truly orphan. Per the user's "holistic redesign latitude" note: the existing PlayersTab UX (avatar + sign-in status + kebab + edit-mode panel from v1.41.0) was kept intact — it's a well-tuned design and the new linking surface fits cleanly as a sibling action without requiring layout reshape. **PlayersTab integration**: new `linkableCandidates?: LinkablePlayerRow[]` prop (optional with default `[]` so existing call sites don't break); `PlayerRow.otherLeagues?: string[]` field for the differentiation cue rendered as a small mono-font chip below the player name on both mobile (`data-testid="player-other-leagues-mobile-{id}"`) and desktop (`data-testid="player-other-leagues-{id}"`); chip only renders when non-empty so cross-league info is visual signal, not chrome. **Page-level wiring** [`src/app/admin/leagues/[id]/players/page.tsx`](src/app/admin/leagues/[id]/players/page.tsx): two new helpers added to the existing `Promise.all` block, results threaded through the playerMap construction. **Tests:** new [`tests/unit/adminLinkExistingPlayer.test.ts`](tests/unit/adminLinkExistingPlayer.test.ts) (16 cases — happy path with `joinSource: ADMIN`, fromGameWeek default + explicit + non-positive coercion, every validation gate including the load-bearing cross-league isolation check + double-roster rejection, query shape pinning the `toGameWeek: null` + `leagueTeam.leagueId` predicate; bulk happy path with sequential creates + final revalidate, partial-failure path returning per-row error strings, 100-item cap, empty-batch no-op, fromGameWeek thread-through). New [`tests/unit/playersTabCrossLeagueLink.test.ts`](tests/unit/playersTabCrossLeagueLink.test.ts) (25 cases — LinkExistingPlayerDialog component shape + testids + modal a11y + ADMIN dispatch logic; PlayersTab integration with the new prop + dialog mount + otherLeagues chip on both layouts; page-level Promise.all wiring; admin-data helper exports + filter shape + leagueId-exclusion predicate). Stash-pop sanity check confirmed: reverting just `PlayersTab.tsx` fails 7/25 cases. 1444 passed | 2 skipped (1403 baseline + 41 net new — 16 actions + 25 structural). Type-check clean. Minor bump (1.55.0 → 1.56.0) — new admin write capability + new admin UI surface + per-row differentiation cue. **Out of scope (later PRs in the chain):** PR 4 (v1.57.0) `/admin/users` admin list (every User in the system, with auth providers + linked Player + leagues membership); PR 5 (v1.58.0) `/admin` load perf (TTFB diagnosis + targeted fix).
>
> v1.55.0 — **Admin UI compat audit: subdomain → URL slug labels + `/id/<slug>` URL previews.** Second of the v1.54.0–v1.58.0 chain. PR 1 (v1.54.0) shipped the route shortening; PR 2 (v1.55.0) audits the admin surfaces that referenced the legacy subdomain UX and flips them to the v1.54.0 canonical form. Four user-visible surfaces updated: **(1) [`SettingsTab.tsx`](src/components/admin/SettingsTab.tsx)** — League settings page label flipped from "Subdomain" to "URL slug"; URL preview flipped from `<slug>.t9l.me` to `/id/<slug>`; error copy "This subdomain is already taken" → "This slug is already taken"; data-source helper copy "subdomain leagues already read from the database" → "non-default leagues already read from the database"; new `data-testid` hooks `settings-tab-slug-input` + `settings-tab-slug-helper`. **(2) [`admin/page.tsx`](src/app/admin/page.tsx)** — admin dashboard tile per-league URL preview flipped from `<slug>.t9l.me ↗` to `/id/<slug> ↗`; pre-existing bug fixed where the slug was derived from `toSlug(league.name)` heuristic instead of reading the configured `League.subdomain` column (now `league.subdomain ?? toSlug(league.name)` with the heuristic as fallback for legacy unconfigured rows); new `data-testid` hook `admin-dashboard-league-slug`. **(3) [`admin/leagues/[id]/layout.tsx`](src/app/admin/leagues/[id]/layout.tsx)** — per-league admin shell title-row link flipped from `https://<slug>.t9l.me` to `/id/<slug>` (apex-relative, no protocol); same toSlug-heuristic bug fix; new `data-testid` hook `admin-league-shell-public-link`. **(4) [`join/[code]/welcome/page.tsx`](src/app/join/[code]/welcome/page.tsx)** — post-redemption `homeUrl` flipped from `https://${league.subdomain}.t9l.me/` to `/id/${league.subdomain}` (apex stays as fallback when the league has no configured slug — only the default league surfaces the apex shortcut). **Internal references intentionally left in place:** `League.subdomain` DB column name (column rename to `slug` is deferred per the v1.50.0 path-routing scaffold note — a column rename requires schema migration + Prisma client regen + every selector update; cosmetic-only with no behavior payoff today); `SubdomainStatus` type alias in SettingsTab (internal name aligned with the DB column); `/api/subdomains/check` API route name (renaming would force a coordinated client-side update across SettingsTab + CreateLeagueModal — legacy compat preserved via the docstring note that the value is now treated as a slug). The v1.24.0 cross-subdomain JWT cookie scoping in `auth.ts` is also preserved — technically unnecessary post-v1.54.0 since there are no more subdomains, but removing it would force every authenticated user to re-authenticate with no architectural payoff. **Tests:** new [`tests/unit/adminUiCompatAudit.test.ts`](tests/unit/adminUiCompatAudit.test.ts) (15 cases — SettingsTab label flip + URL preview + error copy + helper-copy testid + data-source-section copy; admin/page.tsx tile preview + toSlug-fallback fix; admin/leagues/[id]/layout.tsx href + visible text + toSlug-fallback fix; join/[code]/welcome homeUrl form). Stash-pop sanity check confirmed: reverting just `SettingsTab.tsx` fails 5/15 cases (regression catches the broken behavior). 1403 passed | 2 skipped (1388 baseline + 15 new). Type-check clean. Minor bump (1.54.0 → 1.55.0) — admin-facing UX behavior change (label + URL form). **Out of scope (later PRs in the chain):** PR 3 (v1.56.0) Players tab cross-league linking with holistic redesign latitude; PR 4 (v1.57.0) `/admin/users` admin list; PR 5 (v1.58.0) `/admin` load perf.
>
> v1.54.0 — **Route shortening: every tenant URL now namespaced under `/id/<slug>`.** First of a 5-PR chain (route shortening / admin UI compat audit / players tab cross-league / users admin list / admin load perf). User-driven design pivot from the v1.50.0–v1.53.2 path-routing chain: instead of `/<slug>` and `/league/<slug>` as the canonical tenant URL forms, every tenant URL is now namespaced under `/id/<slug>` (and `/id/<slug>/md/<id>`) so league slugs can never shadow top-level platform routes. **New canonical routes:** [`src/app/id/[slug]/page.tsx`](src/app/id/[slug]/page.tsx) renders the per-league Dashboard via `getLeagueIdBySlug`; [`src/app/id/[slug]/md/[id]/page.tsx`](src/app/id/[slug]/md/[id]/page.tsx) renders the per-matchday Dashboard with `initialMatchdayId` (preserving v1.49.1's case-insensitive matchday-id contract). **Legacy 308-redirects:** [`src/app/league/[slug]/page.tsx`](src/app/league/[slug]/page.tsx) and [`src/app/league/[slug]/md/[id]/page.tsx`](src/app/league/[slug]/md/[id]/page.tsx) (canonical pre-v1.54.0) plus [`src/app/[slug]/page.tsx`](src/app/[slug]/page.tsx) (the v1.50.0 short alias) are now thin 308-redirects to the new `/id/` namespace. [`src/app/matchday/[id]/page.tsx`](src/app/matchday/[id]/page.tsx) (legacy `/matchday/<id>` redirect chain) updated to forward to `/id/<slug>/md/<id>`. **Apex `/` unchanged** — still renders the default league directly without redirect (per the v1.50.0 alias-trio contract; today the trio is `/` + `/id/t9l`). **Reserved-word slim-down:** [`src/lib/leagueSlug.ts`](src/lib/leagueSlug.ts) `RESERVED_LEAGUE_SLUGS` collapses from a 13-entry list tracking every top-level route segment to a single recursive guard: `['id']`. The route-shadow problem the comprehensive list guarded against is gone (every tenant URL is namespaced under `/id/`, so a league slug "admin" lives at `/id/admin` and never collides with `/admin`). `validateLeagueSlug('admin')` / `validateLeagueSlug('matchday')` / `validateLeagueSlug('auth')` now return `ok: true`. The `'id'` recursive guard prevents visually confusing `/id/id` URLs. **Internal callers updated:** [`CopyMatchdayLink`](src/components/CopyMatchdayLink.tsx) builds `${origin}/id/<slug>/md/<id>`; [`LeagueSwitcher`](src/components/LeagueSwitcher.tsx) `router.push('/id/<slug>')`; [`AccountMenuLeagueSwitch`](src/components/AccountMenuLeagueSwitch.tsx) `<Link href="/id/<slug>">`; [`CreateLeagueModal`](src/components/admin/CreateLeagueModal.tsx) URL preview prefix `/id/<slug>` (was `/league/<slug>`); modal warning copy updated from "Reserved words (admin, auth, api, ...) are not allowed" to "The slug 'id' is reserved". **Tests:** new [`tests/unit/routeShortening.test.ts`](tests/unit/routeShortening.test.ts) (22 cases — pins the new canonical render at `/id/[slug]` + `/id/[slug]/md/[id]`, asserts every legacy entry point 308-redirects to the new form (no Dashboard render), apex stays render-not-redirect, every internal caller (CopyMatchdayLink / LeagueSwitcher / AccountMenuLeagueSwitch / CreateLeagueModal) uses the new URL form, regression targets that the pre-v1.54.0 forms are gone, sanity assertions on the route file tree). [`tests/unit/leagueSlug.test.ts`](tests/unit/leagueSlug.test.ts) updated for the slim reserved set (admin/matchday/auth now pass validation; 'id' recursive guard pins). [`tests/unit/reservedSlugRouteGuard.test.ts`](tests/unit/reservedSlugRouteGuard.test.ts) rewritten as a CI sanity check: reserved set is exactly `{id}`; `app/id/` has only the `[slug]` dynamic segment (no static siblings to shadow it); top-level platform routes still exist (route-tree didn't regress). [`tests/unit/pathRoutingScaffold.test.ts`](tests/unit/pathRoutingScaffold.test.ts) updated for the new canonical render at `/id/[slug]` and the legacy redirects at `/league/[slug]` + `/[slug]`. [`tests/unit/matchdayPage.test.ts`](tests/unit/matchdayPage.test.ts) updated for the v1.54.0 NEW_PAGE_PATH (canonical render moved to `/id/[slug]/md/[id]`) and a new block pinning the `/league/[slug]/md/[id]` redirect contract. [`tests/unit/leagueSwitcher.test.ts`](tests/unit/leagueSwitcher.test.ts) and [`tests/unit/createLeaguePolish.test.ts`](tests/unit/createLeaguePolish.test.ts) updated for the new URL forms. 1388 passed | 2 skipped (1366 baseline + 22 new). Type-check clean. Stash-pop sanity check confirmed: reverting just `CopyMatchdayLink.tsx` fails the regression. Minor bump (1.53.2 → 1.54.0) — public-URL contract change (canonical form moved; legacy URLs preserved via 308-redirect). **Out of scope (later PRs in the chain):** PR 2 (v1.55.0) admin UI compat audit — find/replace remaining "subdomain" labels with "slug" + audit any internal admin URL constructions; PR 3 (v1.56.0) Players tab cross-league linking with optional holistic redesign; PR 4 (v1.57.0) `/admin/users` admin list; PR 5 (v1.58.0) `/admin` load perf.
>
> v1.53.2 — **Reserved-word route conflict guard (PR 6 of 6 — closes the path-routing chain).** Final safety test for the chain. Two layers of defense protect against a malformed `League.subdomain` value shadowing a top-level route via the `/[slug]` catch-all: (1) **Routing layer (Next.js)** — static segments win over dynamic ones, so requests for `/admin`, `/auth`, `/account`, etc. always hit the dedicated route file regardless of any League row's slug; (2) **Data layer** — `validateLeagueSlug` rejects reserved slugs so `getLeagueIdBySlug` returns null for them, the `/[slug]` catch-all 404s before reaching `Dashboard`. **New test file** [`tests/unit/reservedSlugRouteGuard.test.ts`](tests/unit/reservedSlugRouteGuard.test.ts) (39 cases) pins both layers: for every entry in `RESERVED_LEAGUE_SLUGS`, EITHER a matching top-level directory exists in `src/app/` with a route file (page.tsx / route.ts / layout.tsx, or a sub-directory with one), OR the slug is documented in a small `DATA_LAYER_ONLY_RESERVED` allowlist (today: just `md` — reserved because `/league/<slug>/md/<id>` uses it mid-path but there's no top-level `/md` route, and a league named "md" would be confusing). PLUS for every reserved slug: `validateLeagueSlug` returns `ok: false` AND `getLeagueIdBySlug` returns null without hitting Prisma. **Adding a new top-level route requires:** (a) update `RESERVED_LEAGUE_SLUGS` in [`src/lib/leagueSlug.ts`](src/lib/leagueSlug.ts), (b) verify this test still passes — failure surfaces as "reserved slug 'X' has a real route" red. Stash-pop sanity check confirmed: adding a fake reserved slug without a matching route fails the routing-layer assertion. 1365 passed | 2 skipped (1326 baseline + 39 new). Type-check clean. Patch bump (1.53.1 → 1.53.2). **Closes the path-routing chain.** What shipped across PR 1–6: alias trio (`/`, `/<slug>`, `/league/<slug>`); canonical matchday URL (`/league/<slug>/md/<id>`) with legacy 308-redirect; header + account-menu league switcher with `/api/me/memberships`; subdomain teardown (deleted `getLeagueFromHost.ts`); admin reserved-word validation (server + client); reserved-word route conflict guard.
>
> v1.53.1 — **Admin CreateLeagueModal polish + server-side reserved-word validation (PR 5 of 6 in the path-routing chain).** Patch follow-up to PR 4. The admin "Create League" modal now reflects the path-based routing model end-to-end. **Server-side validation:** [`createLeague`](src/app/admin/leagues/actions.ts) and [`updateLeagueInfo`](src/app/admin/leagues/actions.ts) call `validateLeagueSlug` before hitting Prisma; throw with a clear `Invalid league slug: <reason>` message. updateLeagueInfo only validates when the slug is being changed (allows clearing or leaving unchanged). **API route polish:** [`/api/subdomains/check`](src/app/api/subdomains/check/route.ts) now returns `{ available, reason? }` where `reason` is the discriminated-union value from `validateLeagueSlug` (`empty | too-short | too-long | invalid-format | reserved | in-use`) so the modal can surface targeted error copy. Reserved + malformed slugs are rejected at the validator without hitting the DB. **Modal UX:** [`CreateLeagueModal.tsx`](src/components/admin/CreateLeagueModal.tsx) — label flipped from "Subdomain" to "URL slug"; URL preview flipped from `<slug>.t9l.me` to `/league/<slug>` (regression target — the legacy subdomain UX is gone post-PR 4); new warning copy "Cannot be changed after creation. Lowercase letters, numbers, and hyphens. 3–30 characters. Reserved words (admin, auth, api, ...) are not allowed." surfaced under the input; `subStatus` discriminated union expanded with `'invalid'` branch carrying the failure reason; client-side `validateLeagueSlug` runs BEFORE the debounced fetch so malformed input gets instant feedback without a server round-trip; `REASON_COPY` map renders human-readable copy per reason code; submit button disabled when status is `'invalid'`, `'taken'`, or `'checking'`; new `data-testid` hooks (`create-league-slug-input`, `create-league-slug-warning`, `create-league-slug-status`) for e2e + unit testing. **Tests:** new [`tests/unit/createLeaguePolish.test.ts`](tests/unit/createLeaguePolish.test.ts) (21 cases — server-side validation in createLeague + updateLeagueInfo with the only-on-change semantics; API route reason-keyed responses across all 6 branches; modal: validateLeagueSlug import, /league/<slug> URL preview, no <slug>.t9l.me legacy form, warning copy presence + testid, REASON_COPY surfacing, submit-disabled states, label rename, client-side-validation-before-fetch ordering). 1326 passed | 2 skipped (1305 baseline + 21 new). Type-check clean. Patch bump (1.53.0 → 1.53.1) — UX polish on existing admin component + tighter validation contracts; no schema change. **Out of scope (last PR in the chain):** PR 6 (v1.53.2) reserved-word route conflict guard test — a safety test that asserts every entry in `RESERVED_LEAGUE_SLUGS` has a real route or static page that wins over the `/[slug]` catch-all (or is otherwise non-collidable).
>
> v1.53.0 — **Subdomain teardown (PR 4 of 6 in the path-routing chain).** Strips the host-header-derived league resolver introduced in v1.22.0–v1.26.0. After PR 4, the league context for a request comes exclusively from the URL path: apex `/` and global pages (`/schedule`, `/stats`, `/assign-player`, `/account/player`) always render the default league via the new `getDefaultLeagueId()` helper from [`src/lib/leagueSlug.ts`](src/lib/leagueSlug.ts); per-league rendering lives at `/league/<slug>` (PRs 1–3 of this chain). **Files deleted:** [`src/lib/getLeagueFromHost.ts`](src/lib/getLeagueFromHost.ts) (the `getLeagueIdFromRequest` / `extractSubdomain` helpers + cached `getLeagueIdBySubdomainCached` / `getDefaultLeagueIdCached` lookups) and [`tests/unit/getLeagueFromHost.test.ts`](tests/unit/getLeagueFromHost.test.ts). **Files updated:** every `getLeagueIdFromRequest()` call site flips to `getDefaultLeagueId()` from `@/lib/leagueSlug` — the apex page (`src/app/page.tsx`), `/schedule`, `/stats`, `/assign-player`, `/account/player`, `/api/rsvp` POST, `/api/assign-player` POST + DELETE, and the JWT callback in [`src/lib/auth.ts`](src/lib/auth.ts). The apex page's "League not found" surface for unknown subdomains is replaced with the standard "Data unavailable" surface (the only failure mode now is "no default league configured" — a catastrophic-config scenario, not a routing one). API routes return 404 with `error: 'No default league configured'` instead of `'League not found for this host'` when `getDefaultLeagueId()` returns null. **JWT callback simplification:** pre-v1.53.0 the callback resolved leagueId from the host header and threaded it into the per-league `getPlayerMapping(lineId, leagueId)` Redis lookup; post-PR 4 it uses the default league. Implication: `session.{playerId, teamId}` reflect the user's default-league mapping. Users browsing a non-default league via `/league/<slug>` see that league's data at the page level; their session-derived chrome (UserTeamBadge, RsvpBar, etc.) shows their default-league team. Dashboard's existing render-null branches handle the cross-league mismatch gracefully (UserTeamBadge hides when `userTeamId` isn't in the rendered league's team list). **API limitations documented:** `/api/rsvp` and `/api/assign-player` always write to the default league; multi-league API support is deferred (would require clients to pass `leagueSlug` in the request body — out of scope for the path-routing chain). Today only one league has public matchdays so this matches observed behavior. **Tests:** updated 6 existing test files (`assignPlayerBackgroundPic`, `assignPlayerCachePrewarm`, `assignPlayerWriteInversion`, `rsvpRouteIntegration`, `rsvpRouteWriteInversion`, `rendererConvergence`) to mock `@/lib/leagueSlug#getDefaultLeagueId` instead of the deleted `@/lib/getLeagueFromHost#getLeagueIdFromRequest`. Rewrote [`tests/unit/jwtLeagueContext.test.ts`](tests/unit/jwtLeagueContext.test.ts) (8 cases — pre-v1.53.0 was 12 v1.26.0 cases that pinned the per-host resolution; post-v1.53.0 the contract simplifies to `getDefaultLeagueId` + `getPlayerMapping(lineId, leagueId)` + admin short-circuit + session callback surface). Deleted `tests/unit/getLeagueFromHost.test.ts` entirely (16 cases — the helper file is gone). 1305 passed | 2 skipped (1322 baseline − 16 deleted-file cases − 1 from rendererConvergence simplification + 0 net new). Type-check clean. Minor bump (1.52.0 → 1.53.0) — internal architectural change; user-visible behavior is byte-equivalent on apex (the only deployed surface today). **Out of scope (later PRs in the chain):** PR 5 (v1.53.1) admin CreateLeagueModal warning copy + reserved-word validation surfacing; PR 6 (v1.53.2) reserved-word route conflict guard test.
>
> v1.52.0 — **League switcher UI (PR 3 of 6 in the path-routing chain).** Two new surfaces let users with multiple league memberships move between them at the path level. **Header dropdown** [`src/components/LeagueSwitcher.tsx`](src/components/LeagueSwitcher.tsx) — a small chevron next to the brand title; on click, shows the user's leagues; click → `router.push('/league/<slug>')`. Hidden when the user has < 2 memberships (single-league users see no extra chrome). **Account-menu inline section** [`src/components/AccountMenuLeagueSwitch.tsx`](src/components/AccountMenuLeagueSwitch.tsx) — mounted inside the existing `LineLoginButton` dropdown above the install + admin + sign-out section. Same memberships list as the header dropdown; `<Link>` per league instead of `router.push` so the browser handles native navigation (better for opening in new tabs); current league highlighted with vibrant-pink. **Shared `useLeagueMemberships` hook** exported from `LeagueSwitcher.tsx` — both surfaces consume it; lazy-loads from new API route on dropdown open. **API route** [`src/app/api/me/memberships/route.ts`](src/app/api/me/memberships/route.ts) GET — resolves the signed-in user via `session.userId` (canonical post-α.5) with `session.lineId` fallback; queries `Player.leagueAssignments → leagueTeam → league`; dedupes by `leagueId` (a player with multiple PLAs across timespans surfaces as one row); falls back to `DEFAULT_LEAGUE_SLUG` for the default league when its `subdomain` column is null; marks `isCurrent` from `session.leagueId`; sorts by name. Returns `{ memberships: [] }` for unauthenticated / unlinked / admin-credentials sessions — the components render nothing for empty + single-league lists. **Lazy-loaded only** — the JWT callback is NOT bloated with memberships data; the dropdown fetches on first open. Caching is per-component-instance via a `loadedRef` so re-opening the dropdown doesn't re-fetch within the same session. **Tests:** new [`tests/unit/leagueSwitcher.test.ts`](tests/unit/leagueSwitcher.test.ts) (33 cases — API route handler shape: GET export / auth via getServerSession+authOptions / empty-array on unauth / userId-then-lineId resolution / Prisma join through leagueAssignments→leagueTeam→league / dedup-by-leagueId / DEFAULT_LEAGUE_SLUG fallback / isCurrent semantics / sort order; LeagueSwitcher: 'use client' / hook export / fetch URL / loadedRef caching / unauth-renders-nothing / <2-memberships-renders-nothing / router.push to /league/<slug> / outside-click + Escape close / testids; AccountMenuLeagueSwitch: 'use client' / shared hook reuse / <2-memberships-renders-nothing / Link href shape / testids / current-league highlighting / dropdownOpen-gated lazy load; Header mounts LeagueSwitcher after brand Link; LineLoginButton imports + mounts AccountMenuLeagueSwitch with dropdownOpen + onNavigate props). 1322 passed | 2 skipped (1289 baseline + 33 new). Type-check clean. Minor bump (1.51.0 → 1.52.0) — new user-visible UI affordance + new API route. **Out of scope (later PRs in the chain):** PR 4 (v1.53.0) subdomain teardown + JWT host-header reference cleanup; PR 5 (v1.53.1) admin CreateLeagueModal warning copy + reserved-word validation surfacing; PR 6 (v1.53.2) reserved-word route conflict guard test.
>

> **Older ledger entries (condensed; full descriptions live in git log + GitHub PR bodies):**
>
> - v1.51.0 — Path-based matchday route + legacy `/matchday/[id]` 308-redirect (PR 2/6 in path-routing chain)
> - v1.50.0 — Path-based routing scaffold; `/<slug>` + `/league/<slug>` (PR 1/6 in path-routing chain)
> - v1.49.2 — Submit Goal CTA: AA-compliant green + helper text removed
> - v1.49.1 — `/matchday/[id]` honors URL-pre-selected matchday + case-insensitive slug
> - v1.49.0 — Server-side session hydration (eliminate post-paint auth-UI flash)
> - v1.48.1 — Submit-Goal CTA singleton across matchday changes + bold matchday label
> - v1.48.0 — Submit-Goal expanded + homepage/matchday convergence + open-attribution + click-to-copy section headers
> - v1.47.0 — Matchday page redesign mirroring homepage layout + Submit-a-goal modal
> - v1.46.1 — Match events backfill applied to prod + Guest seeder + resolver upgrades
> - v1.46.0 — Epic match events PR ζ: player self-report goal submission
> - v1.45.0 — Epic match events PR ε: per-matchday public page
> - v1.44.0 — Epic match events PR δ: public read-flip + per-match goal highlights
> - v1.43.0 — Epic match events PR γ: admin events CRUD on StatsTab
> - v1.42.1 — Epic match events PR β: backfill historical goals from `GoalsRaw`
> - v1.42.0 — Epic match events PR α: additive `MatchEvent` schema + `Match.scoreOverride`
> - v1.41.2 — Header wraps to two rows on iPhone-width viewports (mobile padding/gaps trim)
> - v1.41.1 — Invisible "Go to homepage" button text on `/join/[code]` (token fix)
> - v1.41.0 — Admin Players tab: per-row Edit-mode redesign + `adminUpdatePlayerPosition`
> - v1.40.0 — Invite-page inline auth: provider buttons on `/join/[code]` itself
> - v1.39.x — Identity unification audit + `linkUserToPlayer` invariant-clearing fix; non-LINE picker gate
> - v1.38.0 — PR κ: admin player list redesign (avatar / sign-in status / kebab collapse)
> - v1.37.0 — PR ι: user self-service Change Player Details + `Player.profilePictureUrl`
> - v1.36.0 — PR θ: admin Reset Onboarding action; closes the δ→θ onboarding chain
> - v1.35.0 — PR η: ID upload step + admin viewer + purge (`Player.idFrontUrl/idBackUrl/idUploadedAt`)
> - v1.34.0 — PR ζ: redemption page foundation `/join/[code]` + onboarding form + `OnboardingStatus`/`JoinSource` enums
> - v1.33.x — PR ε: admin invite generation (Add Player / per-row Generate Invite / bulk CSV / `skipOnboarding`)
> - v1.32.x — Multi-provider login lightbox; LINE primary + optional Google + email magic-link
> - v1.31.0 — Admin can clear matchday date; public shows TBD for empty date and venue
> - v1.30.x — Identity rework γ: User-side resolver behind `Setting('identity.read-source')` flag
> - v1.29.0 — Identity rework β: User ↔ Player dual-write at every link site
> - v1.28.0 — Multi-provider auth foundation: NextAuth PrismaAdapter + Google OAuth + email magic-link
> - v1.27.0 — Identity rework α: additive User↔Player schema + new `LeagueInvite` table
> - v1.26.0 — JWT league context: per-league `playerId/teamId` + per-league Redis keys
> - v1.25.0 — Renderer convergence: apex + subdomain unified onto `Dashboard` (PR β / multi-tenant launch)
> - v1.24.0 — Multi-tenant prep γ: cross-subdomain JWT cookie + Domain migration runbook
> - v1.23.0 — Multi-tenant prep β: parameterize `getPublicLeagueData(leagueId?)`
> - v1.22.0 — Multi-tenant prep α: `getLeagueIdFromRequest()` + RSVP route league context
> - v1.21.x — Schedule tab visual taxonomy overhaul + status decoupled from score; time-only kickoff picker
> - v1.20.0 — Admin can rename players via `text`-variant `PillEditor`; `adminUpdatePlayerName`
> - v1.19.0 — Pill-style editors for date/venue/kickoff in admin schedule tab
> - v1.18.0 — All-Venues admin page + venue dropdown for matchday data
> - v1.17.x — Loading toast on `/assign-player` link/unlink; `lastSeenAt.toISOString` crash fix
> - v1.16.0 — Consolidate revalidation primitives behind `revalidate({ domain })` + lint guard
> - v1.15.0 — Extract `MatchScoreEditor` from `ScheduleTab.tsx`
> - v1.14.0 — Drop ratings entirely + dedupe redundant league lookup in `lib/publicData.ts`
> - v1.13.0 — Admin Redis pre-warm deferred via `waitUntil`; `__seeded` re-assert collapse
> - v1.12.x — Cleanup: shared `ids.ts` + `rsvpStoreSchema.ts`, archive `backfillRedisLineMap`, safer `dataSource` default
> - v1.11.x — Homepage `UserTeamBadge` above `NextMatchdayBanner`
> - v1.10.0 — Admin Players LINE info column + per-row Unlink + Remap
> - v1.9.x — Canonical JST date/time helpers + admin schedule TZ-skew bug fix
> - v1.8.x — Public-write hot paths invert: Redis sync, Prisma deferred via `waitUntil`; drop public-data revalidation from assign-player
> - v1.7.x — RSVP-on-Redis architectural inversion: `rsvpStore` primary, Prisma `Availability` durable secondary
> - v1.6.x — Auto-navigate + Sonner toast on assign-player success; sync `router.push('/')` + fire-and-forget API
> - v1.5.0 — Architectural inversion of lineId→Player auth: Redis canonical, Prisma durable secondary; 24h sliding TTL
> - v1.4.x — Optimistic UI rewrite of public assign-player flow; filter linked players from picker; Illegal-invocation fix
> - v1.3.x — Drop redundant `router.refresh()`; fire-and-forget `update()`; Vercel Blob upload off API critical path via `waitUntil`
> - v1.2.x — Saving-stuck UX fix v2 (`redirecting` state); Prisma-on-every-JWT perf fix (Upstash mapping cache); post-write pre-warm
> - v1.1.x — Saving-stuck fix; admin assign-player Flow B + B1 Redis→Prisma migration + `LineLogin` model
> - v1.0.x — Sheets→DB cutover (PRs 1–4): schema additions, backfill+adapter+dispatcher, toggle UI + RSVP dual-write, operational `dataSource='db'` flip


> **Version-bump rule:** Every PR bumps `APP_VERSION` in `src/lib/version.ts` as part of the change.
> - Patch bump (1.1.0 → 1.1.1) — fixes, chores, refactors, docs.
> - Minor bump (1.1.0 → 1.2.0) — new user-visible features.
> - Major bump (1.1.0 → 2.0.0) — breaking changes / migrations of public contracts.
> The bump lives in the same commit as the change. The matching test in `tests/unit/version.test.ts` updates in the same commit. After merge, the autonomy post-merge sequence pushes an annotated release tag `v<APP_VERSION>` at the merge SHA — separate from the rollback tag (`v-pre-pr-N-...`).

> **Maintenance rule:** Whenever an architectural decision is made — new component, changed data flow, new API route, new Prisma model or column, UX philosophy change — update this file **in the same PR** as the change. PRs that touch architecture without updating CLAUDE.md should be sent back. This file is the single source of truth for how the project works.
>
> **Test rule:** Every PR that adds or changes behavior ships with at least one test that proves the new behavior. Unit tests for pure functions (Vitest), e2e tests for user-visible flows (Playwright). The CI workflow at `.github/workflows/test.yml` runs Vitest + tsc on every PR; merge is blocked on red. See [Testing](#testing) below for what to add per change-type.
>
> **End-to-end verification rule:** Every PR's tests must verify the BEHAVIOR the PR claims to fix, not just that the code compiles. For perf claims, time before/after on dev preview and capture in the PR description. For UX claims, Playwright assertions on the user-visible outcome (not the implementation detail). For correctness claims, a regression test that fails on the broken state — e.g. for the v1.9.0 JST fix, a test that takes "14:30 JST" through parse → store → display and asserts the displayed time is "14:30", which would have failed on the v1.8.x V8/Vercel TZ=UTC bug. "Doesn't crash" is not verification; "produces the claimed outcome" is.
>
> **Autonomy rule:** The Claude Code harness reads `.claude/settings.json` (committed) and `.claude/settings.local.json` (gitignored, per-developer override). The committed file pre-approves routine read/edit/grep/install/test/git/gh/vercel/neonctl tools and explicitly **denies** destructive Bash patterns (`git push --force*`, `git reset --hard*`, `rm -rf*`, `prisma migrate reset*`, `neonctl branches delete*`, raw SQL `DROP/TRUNCATE/DELETE FROM`). If a routine command is hitting an approval prompt, propose adding it to `permissions.allow` in `.claude/settings.json` rather than `settings.local.json` so the whole team benefits.
>
> **Cache invalidation rule (v1.16.0):** Cache invalidation goes through [`src/lib/revalidate.ts#revalidate({ domain })`](src/lib/revalidate.ts). Direct `revalidateTag` / `revalidatePath` / `updateTag` calls outside that file are forbidden. The lint guard at [`tests/unit/revalidatePrimitivesGuard.test.ts`](tests/unit/revalidatePrimitivesGuard.test.ts) fails CI if any new primitive call leaks. Domains are `'public' | 'admin' | 'settings' | 'all'`; modes are `'action'` (default; uses `updateTag` for read-your-own-writes inside server actions) and `'route'` (uses `revalidateTag(tag, { expire: 0 })` for route handlers). Pass `paths: [...]` to add per-action `revalidatePath` busts. See the helper's docstring for the full domain → tag-set table.
>
> **Admin-orthogonal-UX rule (v1.67.0):** Admin role is ORTHOGONAL to user-facing UX. Admin users see the EXACT same frontend as regular users. The ONLY admin-specific UI differences allowed are: (1) an "Admin" link/button in nav (entry to `/admin`); (2) auto-authentication when accessing `/admin` routes. ALL other UX gates must be based on auth state, player linkage, league membership, or membership status — NEVER on `session.isAdmin`. An admin user who's a Player + active PLM in a league sees State A in the recruiting banner exactly like a non-admin would; an admin with no Player sees State C exactly like a non-admin would. UX gates that branch on `isAdmin` are an anti-pattern and should be rejected on review. The lone non-`/admin` permitted reference is the dropdown "Admin" link in [`LineLoginButton.tsx`](src/components/LineLoginButton.tsx). If a future PR adds another `session.isAdmin` check in non-admin code, it almost certainly should be expressed via a different signal (e.g. is-PLM-active, is-allow-self-link, is-recruiting-PLM-pending) instead.
>
> **Plan-then-ship autonomy rule (v1.9.0):** Plans grounded in code audit + tests that verify the planned behavior may proceed to ship without orchestrator ack. The audit-grounded plan IS the gate — once you've surfaced scope, identified the load-bearing bug, and laid out tests that would fail on the broken state, ship sequentially through the planned PR list (commit → push → admin-merge per Neon-Vercel race fallback → tag → ledger → smoke), then report the chain at the end. The dev environment is yours to instrument, deploy to, and modify for verification.
>
> **Per-push reporting rule (v1.11.2):** Plan-then-ship does NOT mean silent batched delivery. Surface a one-line status to the orchestrator at three points per PR so the user can track progress in real time:
> 1. **On `git push` of the PR branch:** `> PR #N pushed: <one-line description>. <merge state — admin-merging now / waiting for CI>. Next: <PR M or smoke step>.`
> 2. **On merge:** `> PR #N merged at <SHA>. Tag v<X.Y.Z> pushed. Waiting for prod deploy.` (or `Prod deploy clean.`)
> 3. **On prod live (apex serving the new version):** `> v<X.Y.Z> live on apex.` (one line, no elaboration)
>
> Keep each surface short — one or two sentences max. Don't elaborate beyond what the user needs to track progress. The full PR description, test counts, and verification details live in the GitHub PR body and the final-chain report; the per-push surfaces are just signal markers.
>
> **Stop conditions still apply** even under plan-then-ship autonomy: data destruction (Layer 3 / 4 / 5 / 5b / 5c rollback paths), schema irreversibility (DROP COLUMN, type changes that lose data), prod write surprise (a PR turns out to write to prod via an unexpected code path), or ambiguity that's actually a product decision (UX flow that has multiple defensible answers and the user hasn't picked one). Surface these IMMEDIATELY rather than guessing. The Neon-Vercel preview-env race is NOT a stop condition — it's the documented race in [Known infra issues](#known-infra-issues), and the runbook fallback is `gh pr merge <num> --admin --merge` once Unit + type-check is green on the PR.


> **Working with subagents (v1.70.4 — added after CLAUDE.md trim).** Practices that keep the main session's context lean and let subagents do most of the work.
>
> 1. **Match the model to the task.** Trivial CSS / copy / single-line config edits run on Sonnet or Haiku. Reserve Opus (especially opus-4.7-1M) for substantial refactors, multi-file architecture decisions, or anything where context can balloon. Cheap models for cheap work; expensive models where breadth or depth justifies the cost.
> 2. **CLAUDE.md trim discipline.** Every PR that adds a rule should also remove or condense redundant text elsewhere. Bias toward leaner. The file is loaded into every session — every line of bloat is a tax on every future agent.
> 3. **Smaller, focused PRs in parallel.** Break work into the smallest sensible PR units. Different subagents in parallel beat one large sequential agent — different cheap models doing different cheap things finish in a fraction of the wall-clock time and cost.
> 4. **Diagnose-then-execute split.** When fixing a bug, the research/audit/debug phase is its own session. Findings (root cause + fix spec) get passed to a separate executor subagent. Keeps context lean and lets the executor run on a smaller model.
> 5. **PLAN.md and TODO.md at repo root.** Subagents reference and rewrite them as they ship PRs and make architectural decisions. PLAN.md = upcoming work + open architectural questions. TODO.md = operator-side follow-ups (env vars to set, scripts to run, manual data fixes). Keep both scannable; no verbose preamble.
> 6. **Subagent context isolation.** Use subagents heavily so the main session's context stays clean. A subagent can read 10 files, diff against `main`, run tests, and return a 5-line summary — the orchestrator sees only the summary, not the noise.
> 7. **Concise responses.** Skip non-essential context. Keep examples minimal. Bias terse over thorough. Long explanations should live in PR descriptions and the GitHub PR body, not in CLAUDE.md or chat replies.

## Project

T9L.me — mobile-first website for the Tennozu 9-Aside League, a recreational football league in Tokyo. Players can log in via LINE, assign themselves to their roster entry, RSVP availability for upcoming matchdays, and view live league data backed by Postgres (Neon-hosted) via Prisma.

## Stack

- Next.js (App Router, server + client components, ISR)
- TypeScript (strict mode)
- Tailwind CSS v4
- `@prisma/client` + `@neondatabase/serverless` — Postgres ORM (Neon-hosted) — single source of truth for the public site (Google Sheets surface retired in v1.71.0)
- `next-auth` v4 — LINE OAuth (public players) + Credentials provider (admin)
- `@upstash/redis` — Canonical store for lineId→Player mapping (v1.5.0) and per-GameWeek RSVP signals (v1.7.0). Also caches Vercel Blob URLs for player pics.
- `@vercel/blob` — Player profile picture storage (LINE-CDN mirror)
- `sonner` — Toast notifications (global Toaster mounted in app/layout.tsx)
- Vitest (unit) + Playwright (e2e) — see [Testing](#testing)
- Deployed to Vercel; Neon-Vercel marketplace integration auto-provisions a Neon branch DB per Vercel preview branch

## Architecture Overview

```
Postgres (Neon, source of truth via Prisma)
       ↓ read (lib/dbToPublicLeagueData.ts)
  lib/publicData.ts → lib/stats.ts
       ↓
  app/page.tsx (server component, dynamic — reads host header)
       └─ getLeagueIdFromRequest() → getPublicLeagueData(leagueId)
              → components/Dashboard.tsx  (v1.25.0 — apex + subdomain unified)
                  ├── NextMatchdayBanner       (Home — match info + selector)
                  ├── MatchdayAvailability     (Home — per-team attendance)
                  ├── RsvpBar                  (Home — fixed-bottom RSVP)
                  ├── UserTeamBadge            (Home — "your team is X")
                  └── GuestLoginBanner         (Home — guest CTA)
       (apex / dev base / localhost / vercel preview → default league)
       (known subdomain → that league)
       (unknown subdomain → 404 surface)

  /schedule  → MatchdayCard list (subdomain-aware via getLeagueIdFromRequest)
  /stats     → LeagueTable + TopPerformers + StatsDashboard (subdomain-aware)

LINE OAuth → next-auth JWT callback → src/lib/playerMappingStore.ts (Upstash, primary)
                                       └ on store error: defensive Prisma fallback
Player link → api/assign-player → setMappingOrThrow (sync, Redis canonical)
                                  └ waitUntil(prisma.$transaction)  # v1.8.0 deferred
RSVP writes  → api/rsvp → setRsvpOrThrow (sync, Redis canonical)
                          └ waitUntil(prisma.availability.upsert)   # v1.8.0 deferred
RSVP reads   → src/lib/publicData.getRsvpData → rsvpStore (HGETALL/GW)
                                       └ on miss: Prisma fallback + Redis pre-warm
                                       └ on error: Prisma fallback (no write-back)
i18n → `googtrans` cookie set by LanguageToggle → Google Translate (no app-side translation pipeline)
Player pics → /stats reads Redis `player-pic:<slug>` → Vercel Blob URL (LINE-CDN mirror)
              Public dashboard uses PlayerAvatar's static fallback chain at render time.
```

**RSVP store (PR 19 / v1.7.0).** RSVP signals (`rsvp` + `participated`) are read from Redis on every public-dashboard render, not from Prisma. `src/lib/rsvpStore.ts` is the canonical store; `prisma.Availability` is the durable secondary that backs admin queries and the recovery script. Key shape `t9l:rsvp:gw:<gameWeekId>` — Redis HASH with `__seeded=1` sentinel + `<playerSlug>:rsvp` + `<playerSlug>:p` fields. Absolute TTL (EXPIREAT) anchored at `max(gw.startDate, now()) + 90 days` — never sliding, since RSVP data has a natural lifetime tied to the matchday. Read path: `getRsvpForGameWeek(gwId, gwStartDate)` returns a discriminated `{ status: 'hit'|'miss'|'error' }`; `lib/publicData.ts#getRsvpData` runs the batched read + applies the v1.7.0 policy: `hit` → use it (including legitimate empty-with-sentinel), `miss` → Prisma fallthrough + Redis pre-warm, `error` → Prisma fallthrough WITHOUT write-back (don't amplify Upstash blips into write storms). Write sites: `api/rsvp/route.ts` calls `setRsvp(gwId, gwStartDate, playerSlug, rsvp)` after the Prisma upsert in the same try block (failure of either propagates as 500). Admin GameWeek lifecycle: `createGameWeek` calls `seedGameWeek` to mark the hash initialized; `deleteGameWeek` calls `deleteGameWeek` (Redis) for cleanup.

**Auth/perf — Redis-primary mapping store (PR 16 / v1.5.0).** The lineId→Player lookup on every JWT callback resolves through `src/lib/playerMappingStore.ts` (Upstash, **24h sliding TTL**, `t9l:auth:map:` namespace, null sentinel for unmapped LINE IDs). **Redis is canonical, Prisma is the durable secondary.** Pre-v1.5.0 the file was named `playerMappingCache` and acted as a 60s cache-aside in front of Prisma — every miss fell through to Prisma. PR 16 inverts that: a `miss` returns `null` directly (orphan; the user must re-link), and only an `error` (Upstash unreachable) falls through to Prisma defensively so a transient outage doesn't null every authenticated session. The `getMapping` return shape is now a discriminated union `{ status: 'hit'|'miss'|'error' }` so the auth callback can distinguish these branches. The sliding TTL means active users (any auth refresh inside 24h) effectively never expire; long-inactive users → key expires → next access → orphan.

**Writes pre-warm, they do NOT delete-then-cold-read** — every site that mutates `Player.lineId` (`api/assign-player` POST/DELETE, `admin/actions.ts#updatePlayer/createPlayer`, `admin/leagues/actions.ts#adminLinkLineToPlayer`) calls `setMapping(lineId, freshMapping)` after the Prisma write, using the exported `getPlayerMappingFromDb` helper for shape parity. This is load-bearing under v1.5.0 because there's no longer a Prisma fallback in the happy-path read — a forgotten `setMapping` would silently leave the user as orphan until an admin write touches them or the 24h sliding TTL pulls them back to a fresh re-link. The OLD lineId on `updatePlayer` deliberately uses `deleteMapping` (formerly `invalidate`) — that user's mapping has just been transferred to a different player, and a fresh miss-then-orphan is the correct post-write state.

**Recovery procedure.** If Upstash ever loses data or the namespace is cleared, run `npx tsx scripts/backfillRedisFromPrisma.ts --dry-run` to see the rebuild plan, then `--apply` to actually rebuild. The script reads `Player.lineId IS NOT NULL` rows, computes the canonical mapping via the same slug-stripping helper as the JWT path, and SETs each entry into Redis with the 24h TTL. Idempotent — safe to re-run. Per-row decisions: CREATE (Redis missing), MATCH (Redis = Prisma), DRIFT-OVERWRITE (Redis ≠ Prisma; Prisma wins). Pure decision helper in `decideBackfillAction` is unit-tested.

### State-based data on Redis (v1.5.0 architectural pattern)

Two stores in this codebase follow a shared shape: Redis is the canonical read source, Prisma is the durable secondary. Player mapping (PR 16 / v1.5.0) was the first; RSVP (PR 19 / v1.7.0) is the second. New stores added under this pattern should match these nine principles so the operational story stays uniform.

1. **Redis is canonical for the read path.** The hot read goes Redis-direct. No `unstable_cache` wrapper on top of Redis (the cache TTL would just be a second layer of staleness on top of an already-fast store).
2. **Prisma is the durable backup.** Same data is persisted to Postgres in the same write — failure of either propagates as an error to the user, partial-write states do not survive. Prisma exists for two reasons: (a) the recovery script reads from it to rebuild Redis on data loss, (b) admin queries that join across many domains (e.g. "show all linked players + their last login") use Prisma where Redis would be a poor index shape.
3. **Tri-state read result.** `{ status: 'hit' | 'miss' | 'error' }` discriminated union, so the caller can apply different policies per branch. Pre-v1.5.0 code collapsed miss/error into a single nullish return and made the policies indistinguishable; never repeat that.
4. **Miss policy is per-domain.** Player mapping: miss returns `null` (orphan; no Prisma fallback in the happy path). RSVP: miss falls through to Prisma + writes back to Redis (cold-cache fill). The choice depends on whether "missing in Redis" is a normal steady state (player mapping: yes — orphan users exist) or always an anomaly (RSVP: a GameWeek not yet seeded should self-heal).
5. **Error policy is universal-defensive.** Any `error` (Upstash unreachable / unconfigured / hgetall threw) falls through to Prisma so a transient outage doesn't break authenticated sessions or null out the dashboard. **Error reads do NOT trigger a write-back** — don't amplify a half-broken connection into a write storm.
6. **TTL is per-domain.** Player mapping uses a sliding 24h TTL (every read fires a fire-and-forget `expire` so active users never expire). RSVP uses an absolute TTL anchored at `max(matchday, now()) + 90 days` — RSVP data has a natural lifetime tied to a real-world event.
7. **Per-write Redis pre-warm.** Every write site that mutates the canonical state does Prisma-first-then-Redis in the same try block. Forgetting the Redis write would leave the store stale until the natural TTL pulls the entry back to a fresh fall-through; load-bearing for read-your-own-writes consistency.
8. **One backfill script per store.** `scripts/backfillRedis<X>FromPrisma.ts`, `--dry-run` / `--apply` modes, idempotent, per-row CREATE/MATCH/DRIFT-OVERWRITE decisions, pure `decideBackfillAction` exported for unit testing. Run dry-run against prod BEFORE merging the no-fallback code so the cutover doesn't trigger a transient cold-Postgres storm.
9. **Tests pin the policy seam.** Each store ships with a Vitest suite that exercises hit/miss/error branches independently, namespace isolation, and the `RedisLike` injection seam. The test seam is `__setRedisClientForTesting(client | null)` — production code never calls it.
10. **Public-write hot paths invert: Redis sync, Prisma deferred via `waitUntil`** (PR 20 / v1.8.0). Public-facing write sites (`/api/assign-player`, `/api/rsvp`) write Redis on the response critical path and defer the durable Prisma write to background via `waitUntil` from `@vercel/functions`. The Redis write is the gate (canonical store consulted by reads); the Prisma write is recoverable on failure via the audit script. Admin write sites stay Prisma-first because admin pages re-read Prisma directly on `revalidatePath('/admin/...')` — deferring there would surface stale state on the admin's own next render. **Throwing variants required.** Public hot paths use `setMappingOrThrow` / `setRsvpOrThrow` (the throwing variants); the silent `setMapping` / `setRsvp` would 200-OK with no durable write landing anywhere on Redis failure (since Prisma is now deferred). No-client (KV env unset) remains silent — that's a dev-only condition. **Drift on background-Prisma failure recovers via two layers.** Inline `console.error('[v1.8.0 DRIFT] kind=<domain> ...')` log lines emit per-failure for operator grep + replay; the per-domain backfill script (`backfillRedis<X>FromPrisma`) covers Redis-side drift (read-side); a new audit script (`auditRedisVsPrisma.ts`) covers the inverse direction (write-side, `--repair-prisma` flag).

11. **Writes to Redis-canonical state should NOT invalidate the static `public-data` cache** (PR 21 / v1.8.2). State that lives in Redis (player mapping, RSVP) is its own read path — `getMapping` / `getRsvpForGameWeeks` are called at session-aware boundaries (JWT callback, dashboard render), NOT through `unstable_cache`. The static `public-data:db` cache holds team / player / matchday / match / goal / rating data — none of which depend on the link state of any session or the RSVP signals of any player. Calling `revalidatePath('/')` + `revalidateTag('public-data', { expire: 0 })` on a Redis-canonical write therefore forces a needless full re-derivation of the entire `LeagueData` blob (Prisma + RSVP fanout) on the user's next dashboard render — measurably ~580ms warm and multi-second cold per write. The cache-bust shape is reserved for writes that mutate the static fields themselves (admin actions on Team / Player / Match / Goal / Venue, picture-mirror Blob URL updates) and runs from those write sites, not from public hot paths. Concretely: `/api/rsvp` (v1.7.0) and `/api/assign-player` (v1.8.2) drop `revalidatePath('/')` and `revalidateTag('public-data')` from their synchronous response paths. The picture-mirror `uploadAndPersistLinePic` waitUntil keeps its `revalidateTag('public-data', { expire: 0 })` because it does mutate a static field (`Player.pictureUrl`), and that bust runs in the background after the response has already returned. **Audit before adding a `revalidateTag('public-data')` to any new write site:** if the field being written is reachable from `dbToPublicLeagueData` / the `getFromDb` Prisma include, the bust is correct; if not, drop it.

**Stores under this pattern:**

| Store | File | Namespace | TTL | Backfill script |
|---|---|---|---|---|
| Player mapping | `src/lib/playerMappingStore.ts` | `t9l:auth:map:` | 24h sliding | `scripts/backfillRedisFromPrisma.ts` |
| RSVP per GameWeek | `src/lib/rsvpStore.ts` | `t9l:rsvp:gw:` | matchday + 90d (absolute) | `scripts/backfillRedisRsvpFromPrisma.ts` |

**Stores explicitly NOT under this pattern (considered and decided against):**

- **Goals / scores** — admin-write-only, not on the public read hot path; the existing `unstable_cache(getFromDb, 30s)` is fine. Match scores change on a human timescale, not a per-request timescale.
- **Profile pics (Vercel Blob URLs)** — already cached in Redis as a value (not a structured store), populated as a side-effect of `assign-player`. The lookup is by player slug and never falls through anywhere — no need for the tri-state policy.
- **Settings (`Setting` table)** — admin-flip cadence is once-a-week or rarer; the `unstable_cache(getDataSource, 30s)` wrapper is sufficient and the cache-bust path on flip is well-covered.
- **LineLogin** — write-only from the JWT callback for admin Flow B; never read on the public hot path.

When in doubt about whether a new domain belongs in Redis-canonical: if the read frequency is **public dashboard render** (every page view, every authenticated user) AND the data has natural cardinality bounded per league/event/user (not unbounded relations), the Redis-canonical pattern fits. Otherwise the `unstable_cache` shape is simpler and adequate.

### Subdomain Routing

`page.tsx` reads the `host` request header and resolves the active league via [`getLeagueIdFromRequest()`](src/lib/getLeagueFromHost.ts) (apex/dev/localhost → default league; known subdomain → that league; unknown subdomain → `null`). The resolved id is passed to `getPublicLeagueData(leagueId)` and the same `Dashboard` renders for both apex and subdomain (v1.25.0 — pre-v1.25.0 a separate `LeaguePublicView` rendered for subdomains).

- `lib/admin-data.ts#getLeagueBySubdomain(subdomain)` — cached Prisma query (revalidate=60, tag=leagues)
- Known non-league hostnames skipped: `www`, `dev`, `localhost`, `t9l`, `127`, empty
- v1.25.0 — `Dashboard` is the single public renderer; the prior subdomain-only `LeaguePublicView` 3-tab view is gone. Subdomain users get RSVP + next-matchday + UserTeamBadge + per-team availability via the same component the apex uses.

## Time handling

T9L is a Tokyo-based football league. Every match kickoff, matchday date, and RSVP deadline displayed to a player or entered by an admin is **JST clock time** (Asia/Tokyo, UTC+9, no DST). Postgres stores `DateTime` columns as UTC instants (TIMESTAMPTZ); the bridge between "UTC instant in DB" and "JST clock time on screen / in form" is the only thing the canonical helpers in [`src/lib/jst.ts`](src/lib/jst.ts) do.

**The V8 / Vercel TZ=UTC trap (the bug v1.9.0 / PR A fixed).** On Vercel, the Node.js process runs with `process.env.TZ` defaulting to UTC. That makes `new Date("2026-04-16T14:30")` in a server action parse the string as UTC clock time — per the ECMAScript spec for ISO-without-Z forms, V8 uses the host TZ; on Vercel that's UTC. So an admin in JST who types "14:30" into the admin schedule editor used to have their submission interpreted as 14:30 UTC = 23:30 JST — a 9-hour skew on every match scheduled by any non-JST admin (and silently hitting JST admins too because the round-trip through the DB then displays back as 23:30 in `Date#getHours()` browser-local rendering, which then made it look "right" to a JST viewer in the input form even though the stored value was wrong).

**Browser `<input type="datetime-local">`** is the same trap from the other end: the input value is the user's *local* clock time as a tz-naive string, so the server cannot infer the user's timezone from the wire format alone. Convention enforced by `lib/jst.ts`: every `<input type="datetime-local">` value (rendered + parsed) represents JST clock time, regardless of the browser's local TZ.

### Convention

1. **Every `<input type="datetime-local">` value (rendered + parsed) represents JST clock time** regardless of browser local TZ. Use `formatJstDateTimeLocal(date)` to populate the input and `parseJstDateTimeLocal(string)` on the server to interpret submissions.
2. **Every `<input type="date">` value is a JST calendar date** stored as UTC midnight (`Date.UTC(y, m, d)`). Helper: `parseJstDateOnly(string)` for the server, `formatJstDate(date)` for the client.
3. **Every display string of a date/time goes through one of the `formatJst*` helpers** in `lib/jst.ts` — never through bare `toLocaleString` / `toLocaleDateString` / `toLocaleTimeString` without an explicit `timeZone: 'Asia/Tokyo'`.
4. **Avoid `Date#getHours()` / `getMinutes()` / `getDate()`** in component code — these read the host's local TZ, not JST. Use `formatJstTime(date)` etc.
5. **Avoid `toISOString().slice(0, 16)` to populate datetime-local inputs** — that's UTC clock time, not JST. Use `formatJstDateTimeLocal(date)`.

### Canonical helpers (from `lib/jst.ts`)

| Helper | Purpose |
|---|---|
| `formatJstDate(d)` | "YYYY-MM-DD" in JST. For `<input type="date">` values + display. |
| `formatJstTime(d)` | "HH:MM" 24h in JST. For display + `<input type="time">` values. |
| `formatJstDateTimeLocal(d)` | "YYYY-MM-DDTHH:mm" in JST. For `<input type="datetime-local">` values. |
| `formatJstFriendly(d, locale)` | "Apr 16 (Thu)" or "4月16日（木）" — replaces `formatMatchDate`. |
| `formatJstShort(d)` | "Sat 16 Apr" admin dashboard short date. |
| `formatJstDayMonth(d)` | "16 Apr" no weekday. |
| `parseJstDateTimeLocal(s)` | Inverse of `formatJstDateTimeLocal`. The load-bearing parser — server actions must use this for `playedAt` / `endedAt`. |
| `parseJstDateOnly(s)` | "YYYY-MM-DD" → UTC midnight Date. |
| `combineJstDateAndTime(date, time)` | "YYYY-MM-DD" + "HH:MM" → UTC Date. |
| `jstIsoString(date, time)` | "YYYY-MM-DDTHH:MM:00+09:00" for use with countdown timers etc. |

### Existing-rows-may-be-skewed risk

Pre-v1.9.0, any admin not in JST (or any admin in JST who used the "Add Match" form when the picker incorrectly displayed UTC) may have produced `Match.playedAt` rows that are 9 hours off — stored as 14:30 UTC instead of 14:30 JST. v1.9.0 stops the bleeding (new edits land correctly), but does NOT bulk-fix existing skewed rows. **Operator action after v1.9.0 deploy:** spot-check `Match.playedAt` rows and re-save any that look wrong via the admin UI (which now displays JST correctly). Bulk-fix scripts are out of scope here because we can't programmatically distinguish "stored correctly as JST 14:30" from "stored skewed as UTC 14:30 (= JST 23:30)" without external context.

### Internationalization (i18n)

i18n is currently delivered via Google Translate's `googtrans` cookie set by [`src/components/LanguageToggle.tsx`](src/components/LanguageToggle.tsx) — there's no custom translation pipeline despite older CLAUDE.md descriptions of `I18nProvider` / `useT()`. The `formatJstFriendly` helper accepts a `'en' | 'ja'` locale parameter so it can render "4月16日（木）" when the caller knows the locale; client components currently default to `'en'`. The Anthropic-translation pipeline described in earlier ledger entries is not present in the current code; ignore.

## Data Source

All public-site data is read from Postgres (Neon-hosted) via Prisma. Admin writes mutate Postgres directly; the apex Dashboard reads through `lib/publicData.ts#getPublicLeagueData(leagueId?)` which pulls the cached static blob from `lib/dbToPublicLeagueData.ts` and merges live RSVP signals from Redis. The legacy Google Sheets parser surface was retired in v1.71.0.

- Caching: `unstable_cache` 30s TTL on the static read (`public-data` + `leagues` tags); admin writes bust via `revalidate({ domain: 'admin' })`.
- RSVP signals: read directly from Redis at dispatch time (uncached) per the v1.7.0 architecture; Postgres `Availability` is the durable secondary written via `waitUntil` (v1.8.0).

### Environment Variables

```
# LINE OAuth (next-auth)
LINE_CLIENT_ID
LINE_CLIENT_SECRET
NEXTAUTH_SECRET
NEXTAUTH_URL                   # https://t9l.me in prod, http://localhost:3000 in dev

# Google OAuth (next-auth) — separate from the retired Sheets API; OAuth-only via next-auth/providers/google
GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET

# Upstash Redis — canonical store for lineId→Player mapping (v1.5.0) and
# per-GameWeek RSVP signals (v1.7.0). Also caches player-pic Blob URLs.
KV_REST_API_URL
KV_REST_API_TOKEN

# Vercel Blob (player profile pictures — LINE-CDN mirror)
BLOB_READ_WRITE_TOKEN

# Postgres (Neon-Vercel marketplace integration provisions both per branch)
DATABASE_URL                   # Pooled connection (PgBouncer)
DATABASE_URL_UNPOOLED          # Direct connection (used by `prisma migrate deploy` + Prisma `directUrl`)
```

Auth features (RSVP, player assignment) degrade gracefully when KV/Blob vars are missing.

### Public id conventions

- `Player.id` — slug shape via `lib/data.ts#slugify`. Example: "Ian Noseda" → `ian-noseda`.
- `Team.id` — slug shape. Example: "Mariners FC" → `mariners-fc`.
- DB-level ids are prefixed (`p-<slug>` / `t-<slug>` / `lt-...` / `m-...` / `g-...`) per `lib/ids.ts`. The slug↔DB-id helpers (`playerIdToSlug`, `slugToPlayerId`, etc.) bridge the two namespaces.

## File Structure

```
src/
├── app/
│   ├── layout.tsx                    # RootLayout: AuthProvider, ThemeProvider, Sonner Toaster (PR 17 / v1.6.0 — global toast surface that survives route navigation)
│   ├── page.tsx                      # Server component: resolves leagueId via getLeagueIdFromRequest, fetches via publicData, renders Dashboard (v1.25.0 — apex + subdomain unified)
│   ├── globals.css                   # Tailwind + custom design tokens
│   ├── admin/                        # Admin shell (Credentials provider) — settings, players, schedule, etc.
│   ├── assign-player/page.tsx        # Player-link picker (Server) → AssignPlayerClient
│   ├── auth-error/, dev-login/       # Auth error UI + dev-only login bypass
│   ├── schedule/, stats/             # Public sub-routes
│   └── api/
│       ├── auth/[...nextauth]/       # next-auth handler
│       ├── assign-player/route.ts    # POST/DELETE: link/unlink lineId → playerId. Redis-canonical (v1.8.0); Prisma deferred via waitUntil.
│       └── rsvp/route.ts             # POST: link RSVP. Redis-canonical (v1.7.0); Prisma deferred (v1.8.0).
├── components/
│   ├── Dashboard.tsx                 # Client: 3-tab home (default-league public view from Postgres)
│   ├── NextMatchdayBanner.tsx        # Match cards above the fold
│   ├── MatchdayAvailability.tsx      # Per-team attendance pitch view
│   ├── LeagueTable.tsx, TopPerformers.tsx, SquadList.tsx, MatchdayCard.tsx, …
│   ├── LanguageToggle.tsx            # EN/JP switch (sets `googtrans` cookie; Google Translate)
│   ├── PlayerAvatar.tsx              # Avatar with fallback chain (LINE CDN → /player_pics/{slug}.png → default)
│   ├── RsvpBar.tsx, RsvpButton.tsx   # In-Dashboard RSVP UX
│   ├── LineLoginButton.tsx, GuestLoginBanner.tsx
│   ├── AssignPlayerClient.tsx        # /assign-player client form
│   ├── UserTeamBadge.tsx             # "Your team is X" pill (v1.11.0)
│   ├── VersionFooter.tsx             # Renders APP_VERSION
│   ├── ThemeProvider.tsx, ThemeToggle.tsx
│   └── admin/, ui/                   # Admin tabs + shadcn-style primitives
├── lib/
│   ├── ids.ts                        # Shared "p-"/"t-" prefix constants + slug↔DB-id helpers (v1.12)
│   ├── data.ts                       # `slugify` (the only surviving helper post-v1.71.0)
│   ├── stats.ts                      # computeLeagueTable/computePlayerStats/findNextMatchday
│   ├── jst.ts                        # Canonical JST date/time helpers (v1.9.0)
│   ├── auth.ts                       # next-auth authOptions (LINE provider, JWT, Redis-primary lookup)
│   ├── playerMappingStore.ts         # Upstash-canonical store for lineId→Player (v1.5.0) — 24h sliding TTL, tri-state get
│   ├── rsvpStore.ts                  # Upstash-canonical store for per-GameWeek RSVP (v1.7.0) — HASH per GW, absolute TTL = matchday + 90d
│   ├── rsvpStoreSchema.ts            # Pure schema constants (key prefix, sentinel, TTL) shared with backfillRedisRsvpFromPrisma (v1.12)
│   ├── rsvpMerge.ts                  # Pure helpers — mapAvailability + mergeRsvpData merge Redis RSVP into LeagueData
│   ├── publicData.ts                 # Cached static read + uncached RSVP read merged into LeagueData
│   ├── dbToPublicLeagueData.ts       # Prisma → LeagueData adapter (static fields only post-v1.7.0)
│   ├── settings.ts                   # `Setting` helpers (identity / playerData read-source flags)
│   ├── linkedPlayers.ts              # /assign-player picker: Prisma `Player.lineId` fetch — filters already-linked
│   ├── optimisticLink.ts, assignSubmit.ts, assignToast.ts, assignButtonLabel.ts  # /assign-player UX helpers
│   ├── userTeam.ts                   # pickUserTeam helper for UserTeamBadge (v1.11.0)
│   ├── admin-data.ts                 # Cached Prisma queries for admin tabs + getOrphanLineLogins
│   ├── revalidate.ts                 # revalidate({ domain }) — single entry point for cache busts
│   ├── getLeagueFromHost.ts          # Subdomain extraction for app/page.tsx
│   ├── prisma.ts                     # Prisma client singleton
│   └── version.ts                    # APP_VERSION single-source constant
└── types/
    └── index.ts                      # All TypeScript interfaces
```

### Internationalization

i18n is delivered via Google Translate's `googtrans` cookie set by [`src/components/LanguageToggle.tsx`](src/components/LanguageToggle.tsx). Despite older ledger entries, there is no custom translation pipeline (`I18nProvider` / `useT()` / Anthropic translate cache). The `formatJstFriendly` helper accepts a `'en' | 'ja'` locale parameter so server-side date rendering can match the page language; client components currently default to `'en'`.

## Commands

```bash
npm run dev          # Local dev (uses mock data if env vars absent)
npm run build        # Production build
npm run lint         # ESLint
```

## Important Notes

- **4 teams, ~53 players, 8 matchdays, 24 matches**, 33-minute match duration
- FC Torpedo players have no positions stored — display "—"
- Matchday dates can be null. Display "TBD" when null (per v1.31.0).
- Match scores derive from `MatchEvent` rows post-v1.42.0; a match's score is computed live from its events (filtered to GOAL types and applied via the `OWN_GOAL` flip).
- Player pictures: `/stats` reads Redis `player-pic:<slug>` → Vercel Blob URL (mirror of the LINE display picture, written by `/api/assign-player`'s background `uploadAndPersistLinePic`). Other public surfaces use `PlayerAvatar`'s static fallback chain (LINE CDN URL → `/player_pics/{slug}.png` → `default.png`).
- The `/minato` route redirects to the team's AppSheet data-entry form

## Prisma schema (admin-side / DB cutover target)

`prisma/schema.prisma` defines the DB-backed admin and (post-cutover) public-site source of truth. Key models:

- **`User`** — admin/viewer accounts (`role: ADMIN|VIEWER`). LINE-linked via `lineId @unique`.
- **`League`** — single league instance (e.g. "T9L 2026 Spring"). Has `subdomain @unique` for subdomain-based public routing. **Restored fields** `isDefault Boolean`, `primaryColor String?`, `accentColor String?` were re-added to the schema in PR 1 to match prod's actual columns; they're orphan from a rolled-back per-league-branding feature, populated only on `T9L 2026 Spring` (`isDefault: true`) and `Test League 2025` (`isDefault: false`). No code reads them today.
- **`Team`** + **`LeagueTeam`** — teams are global brand identities (`Team`); a team's participation in a league is `LeagueTeam`. PR 1 added `Team.shortName String?` and `Team.color String?`. `Team.name` is **not `@unique`** because the test-league seed introduced duplicates (`Storm United`, `Phoenix FC`); upserts key on `id` (slug) instead.
- **`Player`** + **`PlayerLeagueAssignment`** — players are global; league participation is recorded with `fromGameWeek` / `toGameWeek`. PR 1 added `Player.position String?` (FC Torpedo players are intentionally null).
- **`Venue`** — PR 1 added `url String?`, `courtSize String?`, and `name @unique`.
- **`GameWeek`** — `(leagueId, weekNumber) @@unique`. Has optional `venueId`.
- **`Match`** — PR 1 added `@@unique([gameWeekId, homeTeamId, awayTeamId])` so the upcoming backfill can `prisma.match.upsert` by natural key. Constraint is safe under T9L's 4-team round-robin (each pair plays once per MD); revisit if format ever allows the same pair to play twice in one MD.
- **`Goal`** + **`Assist`** — `Goal` cascades on `Match` delete; **does not cascade on `Player` delete** (deleting a Player with goals will FK-fail — admin "remove from league" only deletes `PlayerLeagueAssignment`, not `Player`).
- **`Availability`** *(new in PR 1)* — RSVP per `(playerId, gameWeekId) @@unique`. Two enums: `RsvpStatus { GOING, UNDECIDED, NOT_GOING }` and `ParticipatedStatus { JOINED, NO_SHOWED }`. Cascades on Player and GameWeek delete. Public-site RSVP route will dual-write to this table starting PR 3.
- **`Setting`** *(new in PR 1)* — `(category, key, leagueId) @@unique` composite. `leagueId IS NULL` rows are global; per-league rows override. Originally used to store the `dataSource` / `writeMode` cutover toggles (retired in v1.71.0); current consumers are the `identity.read-source` and `playerData.read-source` flags. Cascades on League delete.
- **`LineLogin`** *(new in PR 6)* — Tracks every distinct LINE user that has authenticated against the public site, regardless of whether they've been linked to a Player record. Upserted from `lib/auth.ts#trackLineLogin` on every JWT callback that has a `lineId`. `lineId @unique` plus `@@index([lastSeenAt])` for sorted-by-recency orphan queries. Drives the admin "Assign Player" Flow B dropdown — orphan = `LineLogin` row whose `lineId` is not currently set on any `Player.lineId`. Powers `lib/admin-data.ts#getOrphanLineLogins()`.

The `directUrl` connection in `datasource db { ... }` reads from `DATABASE_URL_UNPOOLED` (matches the Neon-Vercel integration's canonical var name). The legacy `DIRECT_URL` var is also still set on production and Preview (dev) for backwards compatibility but no longer referenced by the schema.

## Testing

Two tools, both wired into `package.json` and CI.

| Stack | Purpose | Where |
|---|---|---|
| **Vitest** | Pure-function and module-level tests; run in CI on every PR | `tests/unit/**/*.test.ts(x)`; config in `vitest.config.ts` |
| **Playwright** | End-to-end tests against a base URL (defaults to `https://t9l.me`); run locally pre-merge for PR 3+ user-flow changes | `tests/e2e/**/*.spec.ts`; config in `playwright.config.ts`; override base URL with `BASE_URL=https://<preview>.vercel.app npm run test:e2e` |

Scripts:

```bash
npm test            # vitest watch
npm run test:run    # vitest one-shot (same as CI)
npm run test:e2e    # playwright against $BASE_URL (default https://t9l.me)
npm run test:ci     # vitest only (e2e is opt-in in CI for now)
```

CI workflow `.github/workflows/test.yml` runs `npm ci`, `prisma generate` (placeholder URLs — no DB connection needed for codegen), `tsc --noEmit`, then `vitest run`. PRs are merge-blocked on red.

What to add per change-type:

- **Pure-function or library change** (e.g. `lib/data.ts`, `lib/stats.ts`, parsers, mappers) → Vitest unit test with explicit input/output. Example: `tests/unit/slugify.test.ts`.
- **API route or server action change** → Vitest test that calls the handler directly (mock `next-auth` session if auth-gated); assert response status + body shape.
- **Public UI flow change** (anything visible at apex `/`, `/schedule`, `/stats`, `/admin`) → Playwright e2e covering the user-visible behavior. Run against the PR's preview URL (`BASE_URL=<preview>`) before requesting merge.
- **Backfill / migration script change** → Vitest unit tests for row mappers + decision helpers. Integration test that runs the full backfill against the per-PR Neon branch DB and asserts row counts + spot-check fields.
- **Schema change** → No new test required for the migration itself (Prisma's `migrate deploy` covers correctness), but any code that reads new fields needs a unit or e2e test.

## Backups & rollback runbook

Every PR that ships ≥ PR 2 has four parallel rollback paths. Sequencing of *which* path to invoke depends on what broke.

### Layer 1 — Git tag reset (fastest if the bad change is the latest commit on `main`)
- Each PR's merge commit is tagged `v-pre-pr-<N+1>-<slug>` (e.g. `v-pre-pr-2-backfill` is the last-known-good before PR 2 merges).
- To revert main locally: `git fetch origin --tags && git reset --hard v-pre-pr-2-backfill`. **Do not push --force to main** — instead create a revert PR: `git revert <merge-sha>` and merge that.
- Tags are immutable; check listing with `git tag -l "v-pre-pr-*" -n1`.

### Layer 2 — Vercel deploy promotion (fastest if you need prod restored without waiting for CI)
- Vercel retains every prior production deploy. Each PR's "last good prod deploy URL" is recorded in this section as it lands.
- To promote a previous prod deploy back to current: `vercel promote <deploy-url>` (e.g. `vercel promote https://t9l-website-784a0vmrz-t9l-app.vercel.app`).
- This rolls back code + the running build only. Schema/data are unaffected — pair with Layer 3 if the issue was DB-side.

### Layer 3 — Neon branch restore (when the DB itself is wrong)
- Before each PR ≥ 2 merges, a snapshot Neon branch is cut from `production`: `neonctl branches create --name pre-pr-<N>-<slug> --parent production --project-id young-lake-57212861`. This captures the DB state pre-PR.
- To restore prod to the snapshot state: `neonctl branches restore production --source pre-pr-<N>-<slug> --project-id young-lake-57212861`. This is destructive on the current `production` branch — confirm with the operator before invoking.
- The connection URI for a snapshot branch (read-only inspection) is fetchable via `neonctl connection-string <branch-id> --project-id young-lake-57212861`.
- Project ID: `young-lake-57212861`. Default branch on Neon is named `production` (not `main`). Org: `org-floral-feather-76166317`.

### Layer 4 — Sheets snapshot (retired in v1.71.0)
- The Google Sheets surface was retired entirely in v1.71.0 — no public read path or RSVP write path touches the spreadsheet anymore. This layer no longer applies. Historical snapshots taken during the cutover (file ID `1BLTV9v518fEi3DXRA-qcYY3bLDm_qftNoY_5SNzjKSc`) are kept for archival reference only.

### Layer 5 — Redis player-mapping store rebuild (PR 16 / v1.5.0)
- The lineId→Player auth lookup is now Redis-canonical (`src/lib/playerMappingStore.ts`, namespace `t9l:auth:map:`, 24h sliding TTL). If Upstash ever loses data — accidental wipe, namespace cleared, region failover with stale state, etc. — every authenticated session degrades to "orphan" until the store is rebuilt. Prisma `Player.lineId` is the durable secondary that the rebuild reads from.
- Recovery procedure: pull the prod env into the local shell so `KV_REST_API_URL`, `KV_REST_API_TOKEN`, and `DATABASE_URL` point at production, then run:
  ```
  npx tsx scripts/backfillRedisFromPrisma.ts --dry-run --verbose   # plan
  npx tsx scripts/backfillRedisFromPrisma.ts --apply --verbose     # execute
  ```
  The script is idempotent — safe to run repeatedly. Per-row decisions are CREATE (Redis missing the entry), MATCH (Redis = Prisma), DRIFT-OVERWRITE (Redis ≠ Prisma; Prisma wins on rebuild). The dry-run mode reports counts + drift detail; review before applying. Apply takes ~1 second per Player row at typical roster size (~30 entries).
- The auth callback's defensive Prisma fallback (`error: redis-error` branch in `getPlayerMapping`) keeps existing JWTs working through a transient Upstash blip — but a sustained outage where the cache is intact-but-different (e.g. someone manually wrote bad keys) would still serve incorrect data. Rebuild from Prisma when in doubt.

### Layer 5b — Redis RSVP store rebuild (PR 19 / v1.7.0)

- The per-GameWeek RSVP read path is now Redis-canonical (`src/lib/rsvpStore.ts`, namespace `t9l:rsvp:gw:`, absolute TTL = matchday + 90 days). If Upstash ever loses data — accidental wipe, namespace cleared, region failover — the public dashboard would render with empty availability for affected GameWeeks until the publicData backfill self-heals on the next render (Prisma fallback + write-back). The recovery script forces a full eager rebuild instead of waiting on per-render self-heal.
- Recovery procedure: pull the prod env into the local shell so `KV_REST_API_URL`, `KV_REST_API_TOKEN`, and `DATABASE_URL` point at production, then run:
  ```
  npx tsx scripts/backfillRedisRsvpFromPrisma.ts --dry-run --verbose   # plan
  npx tsx scripts/backfillRedisRsvpFromPrisma.ts --apply --verbose     # execute
  ```
  The script is idempotent — safe to run repeatedly. Per-GameWeek decisions: CREATE (Redis hash absent), MATCH (Redis = Prisma), DRIFT-OVERWRITE (Redis ≠ Prisma; Prisma wins on rebuild — DELs the existing hash first to ensure onlyInRedis fields don't survive). The dry-run reports counts + per-GW field-level diff (`onlyInPrisma` / `onlyInRedis` / `differing`); review before applying. Pure decision helper `decideBackfillAction` is unit-tested in `tests/unit/backfillRedisRsvpFromPrisma.test.ts`.
- Cutover protocol (mandatory at v1.7.0 deploy time): run `--dry-run` against prod Upstash BEFORE merging the v1.7.0 code. All decisions should be CREATE on a fresh prod (Redis namespace empty pre-deploy). After operator review, run `--apply`. Then merge. Reverse order means the first dashboard render post-deploy hits miss for every GW and falls through to Prisma simultaneously — transient cold-Neon storm.

### Layer 5c — Write-side drift recovery: Redis → Prisma audit (PR 20 / v1.8.0)

- v1.8.0 inverts the public-write hot path: `/api/assign-player` POST/DELETE and `/api/rsvp` POST write Redis synchronously and defer the Prisma write via `waitUntil`. If the deferred Prisma write fails (cold-Neon timeout, transient connection issue), the route emits a `[v1.8.0 DRIFT]` log line — but the durable Prisma row is still missing (or stale).
- Layers 5 + 5b cover the OPPOSITE direction (Redis stale, Prisma fresh — the read-side drift case from v1.5.0/v1.7.0). Layer 5c handles write-side drift.
- Recovery procedure: pull prod env into the local shell so `KV_REST_API_URL`, `KV_REST_API_TOKEN`, and `DATABASE_URL` point at production, then run:
  ```
  npx tsx scripts/auditRedisVsPrisma.ts                      # dry-run, both domains
  npx tsx scripts/auditRedisVsPrisma.ts --domain=playerMapping --verbose
  npx tsx scripts/auditRedisVsPrisma.ts --domain=rsvp --verbose
  npx tsx scripts/auditRedisVsPrisma.ts --repair-prisma      # actually repair
  ```
  The script scans `t9l:auth:map:*` and `t9l:rsvp:gw:*` namespaces, compares to Prisma `Player.lineId` and `Availability` rows, and reports per-row state. Categories: MATCH (both align), REDIS-ONLY (v1.8.0 drift — Redis canonical, Prisma missing — repair fixes), PRISMA-ONLY (read-side drift; Layer 5/5b territory, not repaired here), DIFFERING (same row exists on both sides with different values; Redis canonical wins on repair), MALFORMED (admin-garbage Redis values, reported only). Idempotent — safe to re-run. Pure decision helpers are unit-tested in `tests/unit/auditRedisVsPrisma.test.ts`.
- When to run: after a Vercel logs check that surfaces any `[v1.8.0 DRIFT]` log lines. Or as a periodic operator audit (recommended monthly until the v1.8.0 deferred-write path has demonstrated stable durability — the script is read-only by default so a dry-run is cheap).


### Per-PR snapshot ledger

**Recent PRs (last 10 retained in full; earlier rows live in git log + tag history). Rollback-target convention:** each tag in row N points to the commit *before PR (N+1) was merged* — i.e. it's where you'd reset main to undo PR (N+1).

| PR | Merge commit | Git tag | Prod deploy URL | Schema delta / rollback |
|---|---|---|---|---|
| 49 (PR ζ onboarding redemption: `/join/[code]` + onboarding form + `OnboardingStatus`/`JoinSource` enums — v1.34.0) | TBD | `v-pre-pr-50-next` | TBD | Additive: 2 new enums + 3 new columns. Rollback: drop columns + drop types + revert. |
| 50 (PR η ID upload: `Player.idFrontUrl/idBackUrl/idUploadedAt` + `/join/[code]/id-upload` route — v1.35.0) | TBD | `v-pre-pr-51-next` | TBD | Additive: 3 nullable columns. Rollback: drop columns + revert. Operator gate: `BLOB_READ_WRITE_TOKEN`. |
| 51 (PR θ admin Reset Onboarding action — v1.36.0) | TBD | `v-pre-pr-52-next` | TBD | Code-only. Closes δ→θ onboarding chain. |
| 52 (PR ι: user self-service `/account/player` + `Player.profilePictureUrl` — v1.37.0) | `72bd7c8` | `v-pre-pr-53-next` | https://t9l-website-pd8exllie-t9l-app.vercel.app | Additive: 1 nullable column. Operator gate: `BLOB_READ_WRITE_TOKEN`. |
| 53 (PR κ: admin player list redesign — v1.38.0) | `338e898` | `v-pre-pr-54-next` | https://t9l-website-q7c8mtqt2-t9l-app.vercel.app | Code-only. |
| 54 (PR λ: identity unification audit + `linkUserToPlayer` — v1.39.0) | `5189050` | `v-pre-pr-55-next` | https://t9l-website-c105hqsto-t9l-app.vercel.app | Code-only. |
| 55 (non-LINE picker gate fix — v1.39.2) | `aba56ba` | `v-pre-pr-56-next` | https://t9l-website-onpacn77l-t9l-app.vercel.app | Code-only. |
| 56 (invite-page inline auth: `JoinInlineAuth` component — v1.40.0) | `e6cf759` | `v-pre-pr-57-next` | https://t9l-website-mrb2wzrxa-t9l-app.vercel.app | Code-only. |
| 57 (admin Players tab edit-mode redesign + `adminUpdatePlayerPosition` — v1.41.0) | TBD | `v-pre-pr-58-next` | TBD | Code-only. |
| 58 (per-league `allowSelfLink` toggle — v1.60.0) | TBD | `v-pre-pr-59-next` | TBD | Additive: 1 column on League with default true. Rollback: drop column + revert. |
| v1.71.0 (Sheets surface retirement) | TBD | `v-pre-v1.72-next` | TBD | Code-only: deletes Sheets client + parsers + admin toggles + `googleapis` dep. No schema delta. Rollback: revert. |

Keep this table append-only; future PRs add a row.

**Neon branch hygiene rule.** Snapshots older than 5 PRs ago can be retired by the active session to free Neon branch slots; Layers 1–2 (git tag + Vercel deploy promotion) still cover older windows.

### Operational events

One-shot ops on shared systems (Redis cleanup, manual DB writes outside a migration) get a dated line here. Most-recent-5 retained; older entries live in git history.

- **2026-04-30** — v1.25.0 deploy (renderer convergence): apex + subdomain unified onto `Dashboard`; `LeaguePublicView` deleted. Vercel preview hit Neon-Vercel race; admin-merged. Prod confirmed via `data-version-footer`.
- **2026-04-30** — v1.21.x deploys (schedule tab visual taxonomy + time-only kickoff picker). Both admin-merged through Neon-Vercel race fallback.
- **2026-04-29** — v1.12.0 cleanup deploy (PR 81 / merge `3aa30858`). Shared `lib/ids.ts` + `rsvpStoreSchema.ts`, archived `backfillRedisLineMap`, flipped `dataSource` fallback to `'db'`.
- **2026-04-28** — Pre-merge prod cutover for v1.7.0 (RSVP-on-Redis): ran `backfillRedisRsvpFromPrisma --apply`, 12 GameWeeks seeded. Caught + fixed Upstash REST `__seeded` string-vs-number quirk pre-merge (commit `b90b5f0`).
- **2026-04-28** — Dropped orphan Redis `line-player-map` entries surfaced by PR 6 backfill (Aleksandr Ivankov dup, Nikolai Akira Kawabata not in roster). HLEN 33 → 31.

## Domain migration runbook

If the apex domain ever migrates from `t9l.me` to a different domain, the following surfaces all need to be touched. Same shape as the [Backups & rollback runbook](#backups--rollback-runbook) above — each layer has a specific action; some can be done independently, others must be sequenced.

### Layer 1 — DNS

Update DNS at the domain registrar:
- Apex `A` (or `ALIAS`/`ANAME`) record pointing at Vercel (`76.76.21.21` for the IP-based config, or the canonical Vercel-managed CNAME).
- Wildcard `*.<newdomain>` `CNAME` pointing at Vercel so per-league subdomains resolve.
- Keep the old domain's DNS in place during cutover — Vercel can serve both simultaneously while traffic shifts.

### Layer 2 — Vercel project config

In the Vercel dashboard for the `t9l-website` project:
- Add the new domain (`<newdomain>`) and the wildcard (`*.<newdomain>`) under Settings → Domains.
- Verify the SSL certificate provisions for both (Let's Encrypt via Vercel auto-provisioning).
- Optionally set the new domain as the production redirect target so the old domain auto-redirects post-cutover.
- Remove the old domain only after Layer 3–8 are complete and validated.

### Layer 3 — `NEXTAUTH_URL` env var

Update on **prod AND preview** Vercel envs:
```
NEXTAUTH_URL=https://<newdomain>
```
This is the load-bearing var for [`getAuthCookieDomain()`](src/lib/auth.ts) — see Layer 5. NextAuth v4 also uses `NEXTAUTH_URL` to construct the OAuth callback URL.

Local dev (`.env.local`): leave as `http://localhost:3000` — the cookie helper falls through to host-only on localhost.

### Layer 4 — LINE OAuth callback URL

In the LINE Developer Console (channel for the T9L app):
- Add `https://<newdomain>/api/auth/callback/line` to the Callback URL allowlist.
- Keep the old `https://t9l.me/api/auth/callback/line` until Layer 8 (the cutover commit) ships and you've verified the new flow end-to-end.
- Remove the old callback URL only after the cutover is fully complete.

### Layer 5 — NextAuth cookie domain

Update [`src/lib/auth.ts#getAuthCookieDomain()`](src/lib/auth.ts) — flip the apex match from `t9l.me` to the new apex:
```diff
- if (host === "t9l.me" || host.endsWith(".t9l.me")) {
-   return ".t9l.me";
+ if (host === "<newdomain>" || host.endsWith(".<newdomain>")) {
+   return ".<newdomain>";
  }
```
Update the test in [`tests/unit/authCookieDomain.test.ts`](tests/unit/authCookieDomain.test.ts) to match. Bump `APP_VERSION`.

### Layer 6 — Hardcoded URL literals

Grep for the old domain across the entire repo:
```bash
grep -rn "t9l\.me" --include="*.ts" --include="*.tsx" --include="*.json" --include="*.md" .
```
Replace each occurrence with the new domain. Surfaces to check:
- `src/` — code (auth helpers, components that hardcode share URLs, etc.)
- `scripts/` — deploy/seed/backfill scripts
- `prisma/seed.ts` if any
- Tests (`tests/unit/**`, `tests/e2e/**`) — many tests reference `t9l.me` literally as fixtures
- `CLAUDE.md` (this file) — every occurrence of the domain name
- `README.md` (if it references the domain)
- `package.json` `homepage` / `repository` / `bugs` URLs
- `playwright.config.ts` `baseURL` default
- `next.config.{js,ts,mjs}` `images.domains` / `headers` if any

Note: `extractSubdomain` in `src/lib/getLeagueFromHost.ts` does not literally contain `t9l.me` — it's domain-agnostic and just splits on dots. But verify the `RESERVED_SUBDOMAIN_LABELS` set still applies (`dev`/`www`/`staging` are infrastructure labels that should remain reserved on any domain).

### Layer 7 — Other env vars referencing the domain

Audit for env vars that contain the old domain:
- `NEXT_PUBLIC_SITE_URL` (if set — currently not used, but flag for the future)
- `NEXT_PUBLIC_BASE_URL` (same)
- Any Vercel-managed Preview-env URLs (these are auto-generated by Vercel, no action needed)
- `BLOB_READ_WRITE_TOKEN` and `KV_REST_API_URL` are domain-independent, no change needed.

### Layer 8 — Cache + session invalidation

Once the cutover code merges and prod is serving from the new domain:
- Players currently signed in via LINE OAuth will need to re-authenticate. The session JWT cookie was scoped to `.t9l.me`; on `.<newdomain>` it's a fresh cookie scope. Communicate this to operators / players ahead of the cutover.
- Optionally flush prod Upstash (`t9l:auth:map:*` namespace) — not strictly required since the mappings are domain-agnostic, but reasonable as a clean reset if the cutover is part of a broader infrastructure refresh.
- Neon `Player.lineId` rows are domain-agnostic — no action needed.
- The `googtrans` cookie (Google Translate, set by `LanguageToggle`) is host-scoped by default; users will need to re-pick their language preference on first visit to the new domain.

### Layer 9 — CLAUDE.md + multi-tenant-readiness.md

Update this file's references to the apex domain (Layer 6 grep should catch most of them, but double-check section headings, code comments, and example URLs).

If the migration is part of a broader product change, update the in-scope audit doc (e.g. `outputs/multi-tenant-readiness.md`) to reflect the new apex.

### Sequencing

Layers 1–4 are prep work and can land independently in any order. Layer 5 (cookie domain) is the load-bearing code change — ship it AS the cutover commit, with the corresponding `NEXTAUTH_URL` flip in the same Vercel deploy. Layers 6–9 are cleanup and can land before, during, or after the cutover.

The cutover sequence that minimizes downtime:
1. Layers 1–2 (DNS + Vercel domain attached, both old and new active).
2. Layer 4 (LINE OAuth callback added; old callback retained).
3. Layer 6 (code references updated, but Layer 5 cookie domain still on the old domain — deploy this).
4. Layer 5 + Layer 3 in the same commit + Vercel env update — this is the cutover.
5. Layer 8 — operator action: communicate re-auth, flush caches if desired.
6. Layer 4 — remove old LINE OAuth callback.
7. Layer 2 — remove old domain from Vercel.


## Sheets→DB migration (complete; surface retired in v1.71.0)

The Sheets→DB cutover was operationally complete on 2026-04-27 16:48 UTC (`dataSource='db'` flip). v1.71.0 deleted the dormant Sheets surface entirely — `src/lib/sheets.ts`, `src/lib/mock-data.ts`, `scripts/importFromSheets.ts`, the `googleapis` dependency, the `dataSource` / `writeMode` Setting toggles + admin UI, and the related test mocks. The legacy backfill scripts ([`scripts/_archive/sheetsToDbBackfill.ts`](scripts/_archive/sheetsToDbBackfill.ts), [`scripts/_archive/backfillMatchEventsFromSheet.ts`](scripts/_archive/backfillMatchEventsFromSheet.ts)) are archived for historical reference but no longer typecheck against the live tree (excluded via `tsconfig.scripts.json`'s `exclude: ["scripts/_archive"]`). Both scripts hard-code the legacy Sheet ID and would need significant work to be revived; if a fresh Sheets export is ever needed, expect to rewrite from scratch against the post-v1.71.0 schema.

The PR 6 Redis→Prisma migration that populated `Player.lineId` and `LineLogin` from the legacy `line-player-map` hash completed 2026-04-28 (34 scanned / 31 linked / 1 conflict / 1 missing). The script lives at [`scripts/_archive/backfillRedisLineMap.ts`](scripts/_archive/backfillRedisLineMap.ts) for historical record only.

## Known infra issues

### Neon-Vercel preview env race (resolved in v1.58.1)

Vercel preview builds used to fail on the first deploy of new PR branches with `P1012: Environment variable not found: DATABASE_URL_UNPOOLED` because the Neon-Vercel marketplace integration's per-branch DB provisioning hadn't completed yet. v1.58.1 closed the root cause: builds now route through [`scripts/build.mjs`](scripts/build.mjs) which substitutes a placeholder for missing `DATABASE_URL_UNPOOLED` (so `prisma generate` parses cleanly) and skips `prisma migrate deploy` when the placeholder is active. Production builds with real env vars take the original code path. Test pin: [`tests/unit/buildScript.test.ts`](tests/unit/buildScript.test.ts).

If a preview build still fails on the race for some reason, the documented fallback is `gh pr merge <num> --admin --merge` once Unit + type-check is green — the merge to main triggers a prod deploy that uses the always-provisioned production Neon env.

### Neon free-tier branch limit

Project `young-lake-57212861` is capped at **10 concurrent Neon branches** (free-tier limit). When `neonctl branches create` returns `branches limit exceeded`:

- **Additive-only PRs** may proceed without a Layer-3 snapshot, with a "Snapshot not taken" note + rollback recipe (`DROP TABLE/COLUMN ...` + code revert) in the ledger row. Layers 1–2 (git tag + Vercel promotion) still apply.
- **Non-additive PRs** (column drops, type changes, data migrations) must wait for a snapshot to be retired before merging.
